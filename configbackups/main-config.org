# :PROPERTIES:
:ID:       ff3ba8d9-69c5-47c9-8a78-ebaebfcef668
:END:
#+STARTUP: content
#+PROPERTY: header-args:emacs-lisp :tangle ~/.emacs.d/init.el


* Config Overview
This config is my personal config and is evolving over time. You can find the updated version here: [[https://github.com/Timmymayes/emacsone/blob/main/OrgFiles/emacsconf.org][Emacsone]].

*Trying to push this update to git*

This is my first run through configuring emacs so there is probably a lot of inefficiency in the layout and possibly some redundancy that needs cleaning up. That being said, this is a living document that will probably be evolving for some time before it gets a pure reboot. 

** Make this natiavly installable

*** Issue list
Having issues with magit buffer losing focus
Seems to go to dashboard



* Initialization
:PROPERTIES:
:ID:       c8dcfaaf-c40c-4d6f-b7dd-f494eff9148c
:END:
Initial setup for the core of Emacs. Mostly low level system type elements and package usage. 
#+BEGIN_SRC emacs-lisp
;; -*- lexical-binding: t; -*-
#+END_SRC

** DEBUG on error
:PROPERTIES:
:ID:       fb4825f5-6f61-44f7-8575-e5089a509e5c
:END:
#+BEGIN_SRC emacs-lisp
  					;(setq debug-on-error t)
  					;another comment
#+END_SRC

** UTF-8 buffer revert comand
:PROPERTIES:
:ID:       83c05245-25a9-4e02-9d13-80b2fe81e04a
:END:
On the rare occasion utf-8 encoding is lost this is a note for myself on how to fix it.

- (revert-buffer-with-coding-system (C-x ret r) then select utf-8

  #+BEGIN_SRC emacs-lisp
    (set-language-environment "UTF-8")

  #+END_SRC

** System Definitions for easy single environment setup
:PROPERTIES:
:ID:       219db535-e7b1-44ed-b45f-4283ae5c1663
:END:
#+BEGIN_SRC emacs-lisp
(defconst my/windows-p (eq system-type 'windows-nt))
(defconst my/linux-p (eq system-type 'gnu/linux))
#+END_SRC

#+RESULTS:
: my/linux-p

** Linux Tools on Windows
:PROPERTIES:
:ID:       a2ad5430-adaf-4a74-81b7-4aa8e9e79cf8
:END:
#+BEGIN_SRC emacs-lisp
  (when my/windows-p
    ;; define grep to be rg (download via scoop on first install)
    (setq grep-program "rg")

    ;; Use PowerShell 7 (pwsh) for interactive M-x shell
    (setq explicit-shell-file-name "pwsh.exe")
    (setq explicit-pwsh.exe-args '("-NoLogo" "-NoExit"))
    (add-hook 'shell-mode-hook 'ansi-color-for-comint-mode-on)

    ;; add git to path
    (add-to-list 'exec-path "C:/git/bin")
    (setenv "PATH" (concat "C:\\git\\bin;" (getenv "PATH")))

    ;; add scoop shims to path
    (add-to-list 'exec-path "C:/Users/timmy/scoop/apps/gnupg/current/bin")
    (setenv "PATH" (concat "C:\\Users\\timmy\\scoop\\apps\\gnupg\\current\\bin;" (getenv "PATH"))))

#+END_SRC

#+RESULTS:
: C:\Users\timmy\scoop\apps\gnupg\current\bin;C:\git\bin;/home/tyler/miniconda3/bin:C:\git\bin;C:\WINDOWS\system32;C:\WINDOWS;C:\WINDOWS\System32\Wbem;C:\WINDOWS\System32\WindowsPowerShell\v1.0\;C:\WINDOWS\System32\OpenSSH\;C:\Program Files\NVIDIA Corporation\NVIDIA App\NvDLISR;C:\Program Files (x86)\NVIDIA Corporation\PhysX\Common;C:\Program Files\Tailscale\;C:\Program Files\dotnet\;C:\Users\timmy\AppData\Local\Programs\Python\Python310\Scripts\;C:\Users\timmy\AppData\Local\Programs\Python\Python310\;C:\Users\timmy\scoop\apps\miniconda3\current\scripts;C:\Users\timmy\scoop\apps\miniconda3\current\Library\bin;C:\Users\timmy\scoop\apps\mpv\current;C:\Users\timmy\scoop\shims;C:\Users\timmy\AppData\Local\Programs\Python\Python313\Scripts\;C:\Users\timmy\AppData\Local\Programs\Python\Python313\;C:\Users\timmy\AppData\Local\Programs\Python\Launcher\;C:\WINDOWS\system32;C:\WINDOWS;C:\WINDOWS\System32\Wbem;C:\WINDOWS\System32\WindowsPowerShell\v1.0\;C:\WINDOWS\System32\OpenSSH\;C:\Program Files\NVIDIA Corporation\NVIDIA App\NvDLISR;C:\Program Files (x86)\NVIDIA Corporation\PhysX\Common;C:\Program Files\Tailscale\;C:\Users\timmy\AppData\Local\Microsoft\WindowsApps;C:\git\cmd;C:\Users\timmy\AppData\Local\Microsoft\WinGet\Links;C:\Users\timmy\AppData\Local\Programs\Ollama

** Set default directory for find-file
:PROPERTIES:
:ID:       0288f636-ba07-455d-ace5-43f19f7eef2a
:END:
#+BEGIN_SRC emacs-lisp
  (setq-default default-directory "~/")
#+END_SRC
** Load additional Lisp directory
:PROPERTIES:
:ID:       046af46f-1aef-4c34-b75b-4704d06a488f
:END:
#+BEGIN_SRC emacs-lisp
  (add-to-list 'load-path "~/.emacs.d/lisp")
  (add-to-list 'load-path (expand-file-name "configbackups/lisp" "~/dotfiles"))
  ;;tangle test
#+END_SRC
** Setup Custom.el file
:PROPERTIES:
:ID:       2538d948-f3a8-4c5a-9986-70a49c010dbd
:END:
#+BEGIN_SRC emacs-lisp
  ;; Keep customize's auto-generated output (safe-local-variable-values, etc.)
  ;; out of the tangled init.el, since that file gets overwritten on every tangle.
  (setq custom-file (expand-file-name "custom.el" user-emacs-directory))
  (when (file-exists-p custom-file)
    (load custom-file))
#+END_SRC
** Setup Hyper & Super on Windows
:PROPERTIES:
:ID:       8643cfdc-f594-45ad-bfb5-6a7df3843daa
:END:
#+BEGIN_SRC emacs-lisp
  ;; setup passthrough keys
  (setq w32-pass-apps-to-system nil)
  (setq w32-apps-modifier 'hyper)
  (setq w32-pass-lwindow-to-system nil)
  (setq w32-pass-rwindow-to-system nil)
  (setq w32-lwindow-modifier 'super)
  (setq w32-rwindow-modifier 'super)
#+END_SRC

#+RESULTS:
: super
** Set python to path per system
:PROPERTIES:
:ID:       07f9e20d-b5aa-4b59-9722-2ca9fa4fa8b1
:END:
#+BEGIN_SRC emacs-lisp
  (when my/windows-p
    (setq python-shell-interpreter "py"
          python-shell-interpreter-args "-i"))

  (when my/linux-p
    (setq python-shell-interpreter "python3"))

#+END_SRC

#+RESULTS:

** Undo Limit
:PROPERTIES:
:ID:       60921ee6-6ba8-4555-a770-6b17e8665513
:END:
#+BEGIN_SRC emacs-lisp
  (setq undo-limit 160000)


#+END_SRC

#+RESULTS:
: 50

** Server Setup
:PROPERTIES:
:ID:       fa13ef84-3d1c-45c9-b531-7be1232711e2
:END:
Need to setup runemacs --daemon shortcut  in windows startup (win+r > shell::startup to launch the folder)

Then setup your client using emacsclient in programfiles with
"C:\Program Files\Emacs\emacs-XX.X\bin\emacsclient.exe" -c -n --alternate-editor=""  in the target window

:PROPERTIES:
:ID:       5921305d-d6fa-4c30-aa55-2843ffcb3839
:END:

** Package Setup
:PROPERTIES:
:ID:       e38df1ee-24bf-414e-a872-02139e77adc0
:END:
Require package and setup all package archives. Note changes to mode repositories has some adjusted code in there that may change. 

#+BEGIN_SRC emacs-lisp
  (require 'package)
  (setq package-archives '(("melpa" . "https://melpa.org/packages/")
                           ;; old org link commented out for deprication 
                           ;;("org" . "https://orgmode.org/elpa/")
                           ("gnu" . "https://elpa.gnu.org/packages/")
                           ("nongnu" . "https://elpa.nongnu.org/nongnu/")))

  (setq package-check-signature nil)
  (package-initialize)
  (assq-delete-all 'org package--builtins)
  (assq-delete-all 'org package--builtin-versions)
  (unless package-archive-contents
    (package-refresh-contents))

  (setq warning-suppress-log-types '((org) (emacs)))
  (message "Package contents loaded and refreshed")
#+END_SRC

#+RESULTS:

** Use Package Setup
:PROPERTIES:
:ID:       6c530129-7efe-4484-a3eb-c43b473bee3a
:END:
Install use-package and enable ':ensure t' globally. the ':ensusre' keyword causes the package(s) within use-package statements to be installed automatically if not already present on your system. To avoid having to add it to every statement we use use-package-always-ensure.
#+NAME: Install
#+begin_src emacs-lisp

  ;; Initialize use-package on non-Linux platforms

  (unless (package-installed-p 'use-package)
    (package-install 'use-package))

  (require 'use-package)
  (setq use-package-always-ensure t)

#+end_src

#+RESULTS:
: t

** Keep Folders Clean
:PROPERTIES:
:ID:       b71bdda7-04f9-4703-b192-5a056673167c
:END:
Move all backup files into a single directory. Another option is to turn off backups completely but I don't mind having backups in case I screw something up. 
#+BEGIN_SRC emacs-lisp
  (setq backup-directory-alist '(("." . "~/.emacs.d/tmp/backups/")))
  (setq auto-save-file-name-transforms
        '((".*" "~/.emacs.d/tmp/auto-saves/" t)))

#+END_SRC

#+RESULTS:
: ((. . ~/.emacs.d/tmp/backups/))
** Section Loaded Msg
:PROPERTIES:
:ID:       0b4f8c88-316f-43e7-9d9d-8cb586ae8bab
:END:
#+BEGIN_SRC emacs-lisp
  (message "Initilization finished")
#+END_SRC
* UI Config
:PROPERTIES:
:ID:       b8f49388-2a31-40d7-a7dd-d95cf48e40df
:END:
** Basic UI Config
:PROPERTIES:
:ID:       97718043-db39-4f3f-b6a6-4ea4540afb70
:END:
Basic setup for cleaning up the ui.

#+begin_src emacs-lisp

  (setq inhibit-startup-message t) ;inhibit start up
  (scroll-bar-mode -1)   ;Disable visible scrollbar
  (tool-bar-mode -1)     ;Disable toolbar
  (tooltip-mode -1)      ;Disable tooltips11
  (set-fringe-mode 10)   ;Give some breathing room
  (menu-bar-mode -1)     ;Disable menu bar
  (setq visible-bell t)  ;setup the visible bell

  ;; setup window splitting so its side by side usually. 
  (setq split-width-threshold 80)

  					; (set-face-attribute
  					;  'default nil :font "Fira Code Retina" :height 140)  ; set font
  ;; turn on hydras CONFIG-TODO: Create a Hydra Section
  					;  (load-theme 'tango-dark)

  (set-face-attribute
   'default nil :font "Roboto Mono" :height 140)

  (use-package hydra)

  ;; using eshell to run bash script on startup setting up hyper and F keys


  
#+end_src

#+RESULTS:
** Nord Theme Color Vars
:PROPERTIES:
:ID:       0d456fde-e97e-4515-861b-e13cb58943c4
:END:
#+BEGIN_SRC emacs-lisp
(defconst nord0  "#2E3440")  ; dark bg
(defconst nord1  "#3B4252")
(defconst nord2  "#434C5E")
(defconst nord3  "#4C566A")
(defconst nord4  "#D8DEE9")
(defconst nord5  "#E5E9F0")
(defconst nord6  "#ECEFF4")  ; light fg
(defconst nord7  "#8FBCBB")
(defconst nord8  "#88C0D0")  ; frost cyan
(defconst nord9  "#81A1C1")  ; frost blue
(defconst nord10 "#5E81AC")
(defconst nord11 "#BF616A")  ; red
(defconst nord12 "#D08770")  ; orange
(defconst nord13 "#EBCB8B")  ; yellow
(defconst nord14 "#A3BE8C")  ; green
(defconst nord15 "#B48EAD")  ; purple

;; semantic aliases — use these at call sites
(defconst nord-red    nord11)
(defconst nord-orange nord12)
(defconst nord-yellow nord13)
(defconst nord-green  nord14)
(defconst nord-purple nord15)
(defconst nord-cyan   nord8)
(defconst nord-blue   nord9)

#+END_SRC

#+RESULTS:
: nord-blue

** Dictionary fix
:PROPERTIES:
:ID:       0b1e0a50-b184-493d-bea5-931790f92629
:END:
#+BEGIN_SRC emacs-lisp
;; Fix dictionary-mode word-definition face on Windows: DejaVu Serif isn't
;; bundled with Windows and isn't guaranteed installed, causing
;; dictionary-word-definition-face to fail to resolve *any* glyph
;; (shows as tofu boxes, even for plain ASCII).
;;(when (eq system-type 'windows-nt)
  ;;(set-face-attribute 'dictionary-word-definition-face nil :family "Consolas"))
#+END_SRC
** Mixed Pitch
:PROPERTIES:
:ID:       1fbb9862-a776-47df-85c2-119b8d93187a
:END:
Fixes tables/code rendering fixed-pitch inside variable-pitch org
prose. Supersedes the plain :inherit 'fixed-pitch approach, which
loses to any explicit :family the theme already set on that face.
#+begin_src emacs-lisp
  (use-package mixed-pitch
    :after org
    :config
    ;; org-block's fixed-pitch face is an org 9.0+ addition; mixed-pitch's
    ;; own default list may not include it depending on version, and it's
    ;; a face you're explicitly styling already — include it to be safe.
    (add-to-list 'mixed-pitch-fixed-pitch-faces 'org-block))
#+end_src
#+RESULTS:
: t

** Themes
:PROPERTIES:
:ID:       20003db0-5453-4a7f-ac38-7bd78e832f5b
:END:
Read more about this here: [[https://github.com/rougier/nano-modeline][nano-modeline git hub with info]]
#+BEGIN_SRC emacs-lisp

  (use-package nano-theme
    :defer t)

  (defun my/apply-theme-and-faces (&optional frame)
    "Apply theme, Nord face attributes, and transparency.
        Safe to call from frame-creation hooks."
    (when frame (select-frame frame))
    (load-theme 'nano-dark t)
    (set-face-attribute 'default nil :font "Roboto Mono" :height 140)
    (set-face-attribute 'font-lock-keyword-face nil :foreground "#B48EAD" :weight 'bold)
    (set-face-attribute 'font-lock-type-face nil :foreground "#F5C518" :weight 'normal)
    (set-face-attribute 'font-lock-function-name-face nil :foreground "#7FD1ae" :weight 'bold)
    (set-face-attribute 'font-lock-string-face nil :foreground "#A3BE8C")
    (set-face-attribute 'font-lock-comment-face nil :foreground "#616E88" :slant 'italic)
    (set-face-attribute 'font-lock-builtin-face nil :foreground "#5E81AC")
    (set-face-attribute 'font-lock-constant-face nil :foreground "#D08770")
    (set-face-attribute 'italic nil :inherit nil :foreground 'unspecified :slant 'italic)

    ;; Org heading fonts — must run AFTER load-theme, since reloading the
    ;; theme resets these back to nano-theme's own (bold) definitions.
    (dolist (face '((org-level-1 . 1.2)
                    (org-level-2 . 1.1)
                    (org-level-3 . 1.05)
                    (org-level-4 . 1.0)
                    (org-level-5 . 1.1)
                    (org-level-6 . 1.1)
                    (org-level-7 . 1.1)
                    (org-level-8 . 1.1)))
      (set-face-attribute (car face) nil :font "Roboto Mono" :weight 'regular :height (cdr face)))
    (set-face-attribute 'org-block nil :foreground 'unspecified :background "#3b4252" :inherit 'fixed-pitch)
    (set-face-attribute 'org-table nil :inherit 'fixed-pitch)
    (set-face-attribute 'org-formula nil :inherit 'fixed-pitch)
    (set-face-attribute 'org-code nil :inherit '(shadow fixed-pitch))
    (set-face-attribute 'org-verbatim nil :inherit '(shadow fixed-pitch))
    (set-face-attribute 'org-special-keyword nil :inherit '(font-lock-comment-face fixed-pitch))
    (set-face-attribute 'org-meta-line nil :inherit '(font-lock-comment-face fixed-pitch))
    (set-face-attribute 'org-checkbox nil :inherit 'fixed-pitch)
    (set-face-attribute 'org-column nil :inherit 'fixed-pitch)
    (set-face-attribute 'org-agenda-clocking nil :foreground nord-green :background 'unspecified)
    (set-face-attribute 'org-column-title nil :inherit 'fixed-pitch)
    (set-face-attribute 'org-verbatim nil :foreground nord-green :background 'unspecified)
    (set-face-attribute 'org-code nil :foreground nord-orange :background 'unspecified)



    (set-face-attribute 'highlight-function-calls-face nil :foreground "#88C0D0" :underline nil)
    (set-face-attribute 'highlight-quoted-symbol nil :foreground "#E8CB8B")
    (set-face-attribute 'highlight-quoted-quote nil :foreground "#88C0D0")

    (with-eval-after-load 'nano-modeline
      (set-face-attribute 'nano-modeline-active nil :background 'unspecified)
      (set-face-attribute 'nano-modeline-status nil :background 'unspecified)
      (set-face-attribute 'nano-modeline-button-inactive-face nil :background 'unspecified))
    
    ;; Transparency: 60% opaque focused / 45% opaque unfocused
    (set-frame-parameter (selected-frame) 'alpha '(90 . 60)))

  ;; Also cover future frames created without the server hook
  (add-to-list 'default-frame-alist '(alpha . (90 . 60)))

  (if (daemonp)
      (add-hook 'server-after-make-frame-hook #'my/apply-theme-and-faces)
    (my/apply-theme-and-faces))

  ;; (use-package nano-theme
  ;;   :config
  ;;   (load-theme 'nano-dark t)
  ;;   ;; font-lock-faces
  ;;   (set-face-attribute 'font-lock-keyword-face nil :foreground "#81A1C1" :weight 'bold)
  ;;   (set-face-attribute 'font-lock-type-face nil :foreground "#EBCB8B" :weight 'normal)
  ;;   (set-face-attribute 'font-lock-function-name-face nil :foreground "#6dBFB8" :weight 'bold)
  ;;   (set-face-attribute 'font-lock-string-face nil :foreground "#A3BE8C")
  ;;   (set-face-attribute 'font-lock-comment-face nil :foreground "#616E88" :slant 'italic)
  ;;   (set-face-attribute 'font-lock-builtin-face nil :foreground "#B48EAD")
  ;;   (set-face-attribute 'font-lock-constant-face nil :foreground "#D08770")

  ;;   )
  ;; define org stylizing colors
  (with-eval-after-load 'org
    (set-face-attribute 'org-code nil
                        :foreground "#D08770"   ;; orange (Nord12)
                        :inherit 'fixed-pitch)
    (set-face-attribute 'org-verbatim nil
                        :foreground "#A3BE8C"   ;; Aurora Green (Nord14)
                        :inherit 'fixed-pitch)
    (set-face-attribute 'org-link nil
                        :foreground "#81A1C1"   ;; light blue (Nord9)
                        :underline t
                        :weight 'normal))

  ;; load magit correctly
  (with-eval-after-load 'magit
    (set-face-attribute 'magit-diff-added nil :foreground "#A3BE8C" :background "#2A3B2A")
    (set-face-attribute 'magit-diff-added-highlight nil :foreground "#A3BE8C" :background "#2D3F2D")
    (set-face-attribute 'magit-diff-removed nil :foreground "#BF616A" :background "#3B2A2A")
    (set-face-attribute 'magit-diff-removed-highlight nil :foreground "#BF616A" :background "#3F2D2D")
    (set-face-attribute 'magit-diff-hunk-heading nil :foreground "#88C0D0")
    (set-face-attribute 'magit-diff-hunk-heading-highlight nil :foreground "#88C0D0" :weight 'bold))

  (use-package nano-modeline
    :hook
    (prog-mode . nano-modeline-prog-mode)
    (text-mode . nano-modeline-text-mode)
    (org-mode  . nano-modeline-org-mode)
    (term-mode . nano-modeline-term-mode)
    (messages-buffer-mode . nano-modeline-message-mode))

  (setq display-time-format "%H:%M"
        display-time-default-load-average nil
        display-time-24hr-format t)
  (display-time-mode 1)




#+END_SRC

#+RESULTS:
: t
** Cursor Animation
*** Comet Trail
:PROPERTIES:
:ID:       926e3f19-8b48-49b2-bbe4-539bf9e1b3c4
:END:
#+BEGIN_SRC emacs-lisp
;; disabled for now(note global-trail-mode 0) as it's a little laggy

  (load "comet-trail")
  (use-package comet-trail
    ;;  :vc (:url "https://github.com/emacsmirror/comet-trail" :rev :newest)
    :custom
    (comet-trail-minimum-distance 5)
    (comet-trail-tick-interval 0.04)
    :config
    (set-face-attribute 'comet-trail-highlight nil :background nord-purple))

  (define-globalized-minor-mode global-comet-trail-mode
    comet-trail-mode
    (lambda () (comet-trail-mode 1)))

  (global-comet-trail-mode 0)
#+END_SRC

#+RESULTS:

*** Beacon
:PROPERTIES:
:ID:       e844bdee-9a4b-4709-8193-3d6d43ba4ee7
:END:
#+BEGIN_SRC emacs-lisp
(use-package beacon
  :ensure t
  :config
  (beacon-mode 1))

(setq beacon-color nord-purple)   ; Nord frost blue, to match your theme
(setq beacon-blink-duration 0.3)
(setq beacon-blink-delay 0.3)
(setq beacon-size 40)
(setq beacon-push-mark 35)      ; also blink when the mark is pushed
#+END_SRC
** Transparency
:PROPERTIES:
:ID:       b96cd425-0268-442e-a831-d63de1964916
:END:
Transparency toggle for when you want to have a cool hacker mode.
#+BEGIN_SRC emacs-lisp

  (defun my/toggle-transparency ()
    "Toggle transparency across all frames."
    (interactive)
    (let* ((alpha (frame-parameter nil 'alpha))
           (current (cond ((numberp alpha) alpha)
                          ((numberp (cdr alpha)) (cdr alpha))
                          ((numberp (cadr alpha)) (cadr alpha))))
           (new-alpha (if (eql current 100) '(85 . 60) '(100 . 100))))
      (dolist (frame (frame-list))
        (set-frame-parameter frame 'alpha new-alpha))))

  ;; testing if this works to set transparency to full on startup
  (global-set-key (kbd "M-<f12>") 'my/toggle-transparency)

  (add-to-list 'default-frame-alist '(alpha . (85 . 60)))
  (set-frame-parameter nil 'alpha '(85 . 60))

#+END_SRC

#+RESULTS:

** Section Loaded Msg
:PROPERTIES:
:ID:       ee3bb1c4-7e59-4bdb-9b01-9519fb8eb78e
:END:
#+BEGIN_SRC emacs-lisp
  (message "UI Config Section Finished Loading")
#+END_SRC

* Emacs Usability
Packages that make emacs a little easiler to use. Finding out more about commands, completion and command descriptions in autocomplete buffer.
** Helpful
:PROPERTIES:
:ID:       2bd6547f-f15b-4112-b27a-d48db39a7f3a
:END:
#+begin_src emacs-lisp
  (use-package helpful
    :bind
    ([remap describe-function] . helpful-callable)
    ([remap describe-command] . helpful-command)
    ([remap describe-variable] . helpful-variable)
    ([remap describe-key] . helpful-key))
#+end_src

#+RESULTS:
: helpful-key

** Whichkey
:PROPERTIES:
:ID:       3e2e354f-c866-430b-8e3c-9b388eae68f3
:END:
#+begin_src emacs-lisp
  (use-package which-key
    :init (which-key-mode)
    :diminish (which-key-mode)
    :config
    (setq which-key-idle-delay 1))

#+end_src

#+RESULTS:
: t

** Completion
:PROPERTIES:
:ID:       9bab75f8-9255-4091-8aeb-435b972f57b6
:END:
#+begin_src emacs-lisp

  (use-package vertico
    :ensure t
    :custom
    (vertico-cycle nil)
    (vertico-count 13)
    (vertico-resize t)
    :init
    (vertico-mode)
    :config
    (setq vertico-posframe-width 100)
    )

  (use-package vertico-multiform
    :ensure nil
    :after vertico
    :config
    ;; (setq vertico-multiform-commands
    ;;       '((org-roam-search buffer)
    ;;         (consult-outline buffer ,(lambda (_) (text-scale-set -1)))
    ;;         (t posframe)))


    ;; (defun my/adjust-posframe-for-monitor (frame)
    ;;   (with-selected-frame frame
    ;;     (setq vertico-posframe-poshandler
    ;; 	    (if (> (frame-pixel-width) 2000)
    ;; 		(lambda (info)
    ;; 		  (let ((pos (posframe-poshandler-frame-center info)))
    ;; 		    (cons (- (car pos) 700)
    ;; 			  (cdr pos))))
    ;; 	      #'posframe-poshandler-frame-center))))



    ;; (add-function :after after-focus-change-function
    ;; 		(lambda ()
    ;; 		  (when (frame-focus-state)
    ;; 		    (my/adjust-posframe-for-monitor (selected-frame)))))

    ;; (my/adjust-posframe-for-monitor (selected-frame))
    ;; (add-hook 'move-frame-functions #'my/adjust-posframe-for-monitor)
    ;; (add-hook 'after-make-frame-functions #'my/adjust-posframe-for-monitor)
    
    (vertico-multiform-mode 1))

  ;; Commnds for switching  while in the vertico mini-buffer
  ;; ~Last word of each command can be used in the multiform commands variable above~
  ;; M-B -> vertico-multiform-buffer
  ;; M-F -> vertico-multiform-flat
  ;; M-G -> vertico-multiform-grid
  ;; M-R -> vertico-multiform-reverse
  ;; M-U -> vertico-multiform-unobtrusive
  ;; M-V -> vertico-multiform-vertical
  ;; M-P -> vertico-multiform-posframe

  (message "Checkpoint 5")

  (use-package savehist
    :init
    (savehist-mode))

  (use-package marginalia
    :after vertico
    :ensure t
    :custom
    (marginalia-max-relative-age 0)
    (marginalia-align 'center)
    (marginalia-annotators '(marginalia-annotators-heavy marginalia-annotators-light nil))
    :init
    (marginalia-mode))

  ;; turn on all the icons for completions
  (use-package all-the-icons-completion
    :after(marginalia all-the-icons)
    :hook (marginalia-mode . all-the-icons-completion-marginalia-setup)
    :init
    (all-the-icons-completion-mode))

  (use-package consult
    :ensure t
    :bind
    (("C-M-." . consult-ripgrep)
     ("C-c j" . consult-imenu)
     ("M-y" . consult-yank-pop)
     ("C-H-b" . consult-buffer)
     ("H-l" . consult-line)))


  ;; Option B: rg.el — gives you a grep-buffer-style results UI
  (use-package rg
    :config (rg-enable-default-bindings(kbd "C-c C-s")))  ; C-c s prefix 

  (use-package orderless
    :ensure t
    :custom
    (completion-styles '(orderless basic))
    (completion-category-overrides '((file (styles basic partial-completion)))))

  (use-package consult-org-roam
    :after consult
    :ensure t)
  ;; ;; Function for star rating

  ;; (defun my/star-rating (n)
  ;;   "Function to insert a NUMBER star rating"
  ;;   (interactive "N")
  ;;   (defvar h)
  ;;   (setq h 5)
  ;;   (while (> n 0)
  ;;     (insert (all-the-icons-faicon "star"))
  ;;     (setq n (1- n))
  ;;     (setq h (1- h)))
  ;;   (while (> h 0)
  ;;     (insert (all-the-icons-faicon "star-o"))
  ;;     (setq h (1- h))))

  (defun my/star-rating (n)
    "Insert an N-star rating out of 5 using plain Unicode stars."
    (interactive "N")
    (let ((h 5))
      (while (> n 0)
        (insert "★")
        (setq n (1- n))
        (setq h (1- h)))
      (while (> h 0)
        (insert "☆")
        (setq h (1- h)))))





#+end_src

#+RESULTS:
: my/star-rating

** Expand Region
:PROPERTIES:
:ID:       378673df-df5b-4406-a475-bc87ac311195
:END:
#+begin_src emacs-lisp
  (use-package expand-region
    :bind (
  	 ("H-]" . er/expand-region)
           ("H-[" . er/contract-region)))
#+end_src

#+RESULTS:
: er/contract-region

** Yas Mode
:PROPERTIES:
:ID:       69305290-c734-4f52-8794-b9d0767eb4d8
:END:
Turn on Yas global mode

#+BEGIN_SRC emacs-lisp

  (use-package yasnippet)
  (yas-global-mode 1)
  (if (eq system-type 'windows-nt)
      (progn
        (setq yas-snippet-dirs '("c:/Users/Tyler/snippets" "~/.emacs.d/snippets" "~/Snippets"))
        (yas-reload-all))
    (progn
      (setq yas-snippet-dirs '("~/snippets" "~/.emacs.d/snippets"))     
      (yas-reload-all))

    )
#+END_SRC

#+RESULTS:
: [yas] Prepared just-in-time loading of snippets successfully.
** Scale Text Hydra
:PROPERTIES:
:ID:       8638752e-9cb0-479e-a130-6589aa01983d
:END:
#+BEGIN_SRC emacs-lisp
  (defhydra hydra-zoom (global-map "<f16>")
    "Zoom"
    ("+" text-scale-increase "in")
    ("=" text-scale-decrease))

#+END_SRC

#+RESULTS:
: hydra-zoom/body
** Section Loaded Message
:PROPERTIES:
:ID:       b4915a56-e059-4192-a1dd-6dfe33c8821e
:END:
#+BEGIN_SRC emacs-lisp
  (message "Usabiliy Section Finished Loading")
#+END_SRC

* GPG Setup
:PROPERTIES:
:ID:       d9a71869-7a02-4345-bf70-db3615cb5b69
:END:
#+BEGIN_SRC emacs-lisp
  (require 'epa-file)
  (epa-file-enable)
  (setq epa-file-select-keys 'silent)
  (setq epa-file-encrypt-to '("64B7FA4669B10335" "EE16A50B5AFCE6C6"))
  (setq epg-pinentry-mode 'loopback)
#+END_SRC

#+RESULTS:
: loopback

* LLM Tooling

* LLM Setup
:PROPERTIES:
:ID:       751c6110-f494-4bf4-95cf-e6531870194c
:END:

#+BEGIN_SRC emacs-lisp

  (use-package gptel
    :config
    (setq gptel-default-mode 'org-mode)
    (setq auth-sources '("~/.authinfo.gpg"))
    (setq gptel-directives
          (append gptel-directives
                  '((mentor . "You are a programming mentor. Your role is to guide and coach, not to do the work. Ask clarifying questions, point me toward the right concepts, suggest what to look up, and give hints rather than solutions. If I'm going in the wrong direction, tell me why without correcting it for me.")
                    (fix . "You are a precise code fixer. I will send you broken or incorrect code. Return only the corrected code with minimal explanation unless the fix requires context. Be terse.")
                    (review . "You are a code reviewer. Succinctly identify problems, bugs, gotchas, edge cases, and design issues. Use a bullet list. Do not rewrite the code unless asked. Prioritize by severity."))))

    
    ;; ;; OpenAI (existing) OLD
    ;; (setq gptel-api-key
    ;;       (auth-source-pick-first-password :host "api.openai.com"))
    (setq gptel-api-key
    	(lambda () (auth-source-pick-first-password :host "api.openai.com")))

    ;; make oolama
    (gptel-make-ollama "Ollama"
      :host "localhost:11434"
      :models '(llama3.1
      	      qwen2.5-coder:14b)
      :stream t)
    
    ;; ;; Claude - store the backend in a variable

    
    ;; (let ((claude (gptel-make-anthropic "Claude"
    ;;                 :stream t
    ;;                 :key (auth-source-pick-first-password :host "api.anthropic.com"))))
    ;;   ;; Set it as the default backend
    ;;   (setq gptel-backend claude
    ;;         gptel-model 'claude-sonnet-4-6))


    ;;             :stream t
    ;;             :key (lambda () (auth-source-pick-first-password :host "api.anthropic.com")))))

    ;; Claude - store the backend in a variable
    (let ((claude (gptel-make-anthropic "Claude"
                    :stream t
                    :key (lambda () (auth-source-pick-first-password :host "api.anthropic.com")))))
      ;; Set it as the default backend
      (setq gptel-backend claude
            gptel-model 'claude-sonnet-4-6))

    (setq gptel-log-level 'info)
    ;; Core keymap
    (defvar my/gptel-map (make-sparse-keymap)
      "Keymap for gptel commands under C-c g")
    (global-set-key (kbd "C-c g") my/gptel-map)

    ;; Core commands
    (define-key my/gptel-map (kbd "RET") #'gptel)
    (define-key my/gptel-map (kbd "s")   #'gptel-send)
    (define-key my/gptel-map (kbd "m")   #'gptel-menu)
    (define-key my/gptel-map (kbd "a")   #'gptel-add)
    (define-key my/gptel-map (kbd "x x") #'gptel-context-remove-all)
    (define-key my/gptel-map (kbd "f")   #'gptel-add-file)

    ;; Model switching (1=cheap → 3=expensive)
    ;; Model switching (1=local → 2=cheap → 3=mid → 4=expensive)
    ;; Model switching (1-2=local → 3=cheap → 4=mid → 5=expensive)
    (define-key my/gptel-map (kbd "1") #'my/gptel-use-llama)
    (define-key my/gptel-map (kbd "2") #'my/gptel-use-qwen)
    (define-key my/gptel-map (kbd "3") #'my/gptel-use-haiku)
    (define-key my/gptel-map (kbd "4") #'my/gptel-use-sonnet)
    (define-key my/gptel-map (kbd "5") #'my/gptel-use-opus)

    ;; Directives
    (define-key my/gptel-map (kbd "d d") #'my/gptel-directive-default)
    (define-key my/gptel-map (kbd "d m") #'my/gptel-directive-mentor)
    (define-key my/gptel-map (kbd "d f") #'my/gptel-directive-fix)
    (define-key my/gptel-map (kbd "d r") #'my/gptel-directive-review)
    (define-key my/gptel-map (kbd "d p") #'my/gptel-directive-programming)
    (define-key my/gptel-map (kbd "d w") #'my/gptel-directive-writing)
    (define-key my/gptel-map (kbd "d c") #'my/gptel-directive-chat)

    ;; Memory injection
    (define-key my/gptel-map (kbd "c c") #'my/gptel-inject-core)
    (define-key my/gptel-map (kbd "c e") #'my/gptel-inject-emacs)
    (define-key my/gptel-map (kbd "c g") #'my/gptel-inject-gamedev)
    (define-key my/gptel-map (kbd "c d") #'my/gptel-inject-devops)

    ;; Rewrite
    (define-key my/gptel-map (kbd "r r") #'gptel-rewrite))

  ;; Custom Functions
  (defun my/gptel-use-llama ()
    "Switch to local Llama 3.1 8B via Ollama."
    (interactive)
    (setq gptel-backend (gptel-get-backend "Ollama")
          gptel-model 'llama3.1
          gptel-max-tokens 4096)
    (message "GPTel: switched to local Llama 3.1"))

  (defun my/gptel-use-qwen ()
    "Switch to local Qwen 2.5 14B via Ollama."
    (interactive)
    (setq gptel-backend (gptel-get-backend "Ollama")
          gptel-model 'qwen2.5-coder:14b
          gptel-max-tokens 4096)
    (message "GPTel: switched to local Qwen 2.5-coder 14B"))

  (defun my/gptel-use-haiku ()
    "Switch to Claude Haiku (fast/cheap)."
    (interactive)
    (setq gptel-backend (gptel-get-backend "Claude")
          gptel-model 'claude-haiku-4-5
          gptel-max-tokens nil)
    (message "GPTel: switched to Claude Haiku"))

  (defun my/gptel-use-sonnet ()
    "Switch to Claude Sonnet (balanced)."
    (interactive)
    (setq gptel-backend (gptel-get-backend "Claude")
          gptel-model 'claude-sonnet-4-6
          gptel-max-tokens nil)
    (message "GPTel: switched to Claude Sonnet"))

  (defun my/gptel-use-opus ()
    "Switch to Claude Opus (powerful/expensive)."
    (interactive)
    (setq gptel-backend (gptel-get-backend "Claude")
          gptel-model 'claude-opus-4-6
          gptel-max-tokens nil)
    (message "GPTel: switched to Claude Opus"))

  (defvar my/gptel-current-directive 'default
    "Tracks the currently active gptel directive.")

  (defun my/gptel-directive-default ()
    "Switch to default gptel directive."
    (interactive)
    (setq gptel--system-message
          (alist-get 'default gptel-directives))
    (setq my/gptel-current-directive 'default)
    (message "GPTel: directive → default"))

  (defun my/gptel-directive-mentor ()
    "Switch to mentor directive - coaching mode."
    (interactive)
    (setq gptel--system-message
          (alist-get 'mentor gptel-directives))
    (setq my/gptel-current-directive 'mentor)
    (message "GPTel: directive → mentor"))

  (defun my/gptel-directive-fix ()
    "Switch to fix directive - terse code correction."
    (interactive)
    (setq gptel--system-message
          (alist-get 'fix gptel-directives))
    (setq my/gptel-current-directive 'fix)
    (message "GPTel: directive → fix"))

  (defun my/gptel-directive-review ()
    "Switch to review directive - succinct code review."
    (interactive)
    (setq gptel--system-message
          (alist-get 'review gptel-directives))
    (setq my/gptel-current-directive 'review)
    (message "GPTel: directive → review"))

  (defun my/gptel-directive-programming ()
    "Switch to programming directive."
    (interactive)
    (setq gptel--system-message
          (alist-get 'programming gptel-directives))
    (setq my/gptel-current-directive 'programming)
    (message "GPTel: directive → programming"))

  (defun my/gptel-directive-writing ()
    "Switch to writing directive."
    (interactive)
    (setq gptel--system-message
          (alist-get 'writing gptel-directives))
    (setq my/gptel-current-directive 'writing)
    (message "GPTel: directive → writing"))

  (defun my/gptel-directive-chat ()
    "Switch to chat directive."
    (interactive)
    (setq gptel--system-message
          (alist-get 'chat gptel-directives))
    (setq my/gptel-current-directive 'chat)
    (message "GPTel: directive → chat"))







#+END_SRC

#+RESULTS:
| :eval | (format  [%s:%s] (my/gptel-model-short-name) (my/gptel-directive-short-name)) |
** Misc bindings for now
:PROPERTIES:
:ID:       01bcd447-67c4-4355-b771-5b6187ca904f
:END:
#+BEGIN_SRC emacs-lisp

#+END_SRC
** Modeline Setup
:PROPERTIES:
:ID:       18b50228-e487-40dd-839d-fa514c8f6e55
:END:
#+BEGIN_SRC emacs-lisp
  ;; Display model and directive in modeline
  (defun my/gptel-model-short-name ()
    "Return a short display name for the current gptel model."
    (pcase gptel-model
      ('claude-haiku-4-5  "Haiku")
      ('claude-sonnet-4-5 "Sonnet")
      ('claude-sonnet-4-6 "Sonnet")
      ('claude-opus-4-5   "Opus")
      (_ (symbol-name gptel-model))))

  (defun my/gptel-directive-short-name ()
    "Return the current directive as a capitalized string."
    (capitalize (symbol-name my/gptel-current-directive)))

  (defvar my/gptel-mode-line-segment
    '(:eval (format " [%s:%s]"
                    (my/gptel-model-short-name)
                    (my/gptel-directive-short-name)))
    "Mode-line segment showing current gptel model and directive.")

  ;; Add to global mode-line
  (add-to-list 'global-mode-string my/gptel-mode-line-segment t)

  ;; Poll nvidia smi and put the mem usage int he modeline 
  (defvar my/gpu-vram-string ""
    "Current VRAM usage string for mode-line display.")

  (defun my/update-gpu-vram ()
    "Query nvidia-smi and update `my/gpu-vram-string'."
    (let ((output (ignore-errors
                    (with-temp-buffer
                      (call-process "nvidia-smi" nil t nil
                                    "--query-gpu=memory.used,memory.total"
                                    "--format=csv,noheader,nounits")
                      (string-trim (buffer-string))))))
      (setq my/gpu-vram-string
            (if (and output (string-match "\\([0-9]+\\), *\\([0-9]+\\)" output))
                (format " GPU:%s/%sMB"
                        (match-string 1 output)
                        (match-string 2 output))
              " GPU:N/A"))))

  (defvar my/gpu-vram-timer nil)

  (defun my/gpu-vram-mode-line-enable ()
    "Start periodic VRAM polling for the mode-line."
    (interactive)
    (my/update-gpu-vram)
    (setq my/gpu-vram-timer
          (run-with-timer 0 5 #'my/update-gpu-vram)) ; every 5 seconds
    (unless (memq 'my/gpu-vram-string global-mode-string)
      (add-to-list 'global-mode-string 'my/gpu-vram-string t)))

  (defun my/gpu-vram-mode-line-disable ()
    (interactive)
    (when my/gpu-vram-timer
      (cancel-timer my/gpu-vram-timer)
      (setq my/gpu-vram-timer nil))
    (setq global-mode-string (remove 'my/gpu-vram-string global-mode-string)))
#+END_SRC
** Section Loaded Message
:PROPERTIES:
:ID:       3d6134cd-b503-4ee9-9f8b-572e7393f3ed
:END:
#+BEGIN_SRC emacs-lisp
  (message "LLM Setup finished Loading")
#+END_SRC
* Window Control & Config
** Avy & Ace
:PROPERTIES:
:ID:       d5ad94ef-ba4f-43b9-abd8-295569670b7b
:END:
Great article on Avy - https://karthinks.com/software/avy-can-do-anything/
#+BEGIN_SRC emacs-lisp
  (use-package ace-window)
  (custom-set-faces
   '(aw-leading-char-face
     ((t (:inherit ace-jump-face-foreground :height 3.0)))))
  ;;

  (use-package avy
    :config (progn
              (setq avy-background nil)
              (setq avy-all-windows 'all-frames)
              (setq avy-styles-alist '((avy-goto-char-2 . at)
                                       (avy-goto-char-timer . at)))))

  ;; function to jump to the end of a word
  (defun my/avy-end()
    "run avy and jump to the end of the word. "
    (interactive)
    (progn
      (avy-goto-char-timer)
      (forward-word)))


  (global-set-key (kbd "M-.") 'my/avy-end)
  (global-set-key (kbd "M-,") 'avy-goto-char-timer)
  (global-set-key (kbd "C-M-,") 'avy-copy-region)
  (global-set-key (kbd "C-M-]") 'ace-window)

  (define-key isearch-mode-map (kbd "M-,") 'avy-isearch)


  ;; unbund c-] from abort-recursive-edit
  (global-set-key (kbd "C-+") 'smartscan-symbol-go-backward)
  (global-set-key (kbd "C-=") 'smartscan-symbol-go-forward)




#+END_SRC

#+RESULTS:
: smartscan-symbol-go-forward

#+RESULTS:[]
: counsel-ibuffer
** Frame Control
:PROPERTIES:
:ID:       a253cf0a-ce85-4a5b-a0b2-39a8b3fded82
:END:
#+BEGIN_SRC emacs-lisp
  ;; Frame delete is C-H-esc

  (defvar my/monitor-slots-alist
    '(("tyler-winhome"
       (1 . "\\\\.\\DISPLAY3")   ; vertical ASUS on left
       (2 . "\\\\.\\DISPLAY1")   ; top Dell ultrawide
       (3 . "\\\\.\\DISPLAY2"))) ; bottom Dell ultrawide (origin)
    "Per-machine mapping of monitor slot numbers to monitor names.")



  (defun my/frame-on-slot (slot)
    "Create a frame on the monitor mapped to SLOT for the current machine."
    (interactive "nMonitor slot: ")
    (let* ((machine (system-name))
           (slots (cdr (assoc machine my/monitor-slots-alist)))
           (monitor (cdr (assoc slot slots))))
      (my/make-frame-on-monitor monitor)))

  (defun my/make-frame-on-monitor (monitor-name)
    "Create a new frame on MONITOR-NAME, apply theme, faces, and transparency."
    (interactive)
    (let* ((monitors (display-monitor-attributes-list))
           (target (when monitor-name
                     (seq-find (lambda (m)
                                 (string= (cdr (assq 'name m)) monitor-name))
                               monitors)))
           (workarea (when target (cdr (assq 'workarea target))))
           (frame (if workarea
                      (make-frame
                       `((left . (+ ,(nth 0 workarea)))
                         (top . (+ ,(nth 1 workarea)))
                         (width . 120)
                         (height . 40)
                         (alpha . (60 . 45))))
                    (make-frame '((alpha . (60 . 45)))))))
      (my/apply-theme-and-faces frame)
      (select-frame-set-input-focus frame)
      frame))



  (global-set-key (kbd "C-H-1") (lambda () (interactive) (my/frame-on-slot 1)))
  (global-set-key (kbd "C-H-2") (lambda () (interactive) (my/frame-on-slot 2)))
  (global-set-key (kbd "C-H-3") (lambda () (interactive) (my/frame-on-slot 3)))
  (global-set-key (kbd "C-H-5") 'delete-frame)


#+END_SRC

#+RESULTS:
: delete-frame
** Calendar
*** Put Calendar below capture window
:PROPERTIES:
:ID:       bd16fc5c-9477-40ab-a814-60694f5d201e
:END:
#+BEGIN_SRC emacs-lisp

  (add-to-list 'display-buffer-alist
               `(,(rx string-start "*Calendar*" string-end)
                 (display-buffer-below-selected)))
#+END_SRC
*** Set location for sunrise/sunset
:PROPERTIES:
:ID:       534ec8ec-e24b-4e41-98a1-015330c89ffb
:END:

#+BEGIN_SRC emacs-lisp
  (setq calendar-latitude 33.19)
  (setq calendar-longitude -117.24)
  (setq calendar-location-name "Vista, CA")
  (message "Finished loading block 4")
#+END_SRC

#+RESULTS:
: Vista, CA
** Section Loaded Message
:PROPERTIES:
:ID:       013701dd-bfc9-4e0f-9c68-e881eb0125ff
:END:
#+BEGIN_SRC emacs-lisp
  (message "Window Control & Config Section Loaded")
#+END_SRC
* Org Config Setup
*** Custom org- setup function
:PROPERTIES:
:ID:       ee35b528-f7b3-4179-a6da-b2f4221be614
:END:
#+begin_src emacs-lisp
                             ;;;;; Org mode setup ;;;;;

                                          ;require tempo

  (defun org-mode-setup()
    (org-indent-mode)
    (mixed-pitch-mode 1) 
    (auto-fill-mode 0)
    (visual-line-mode 1))
#+end_src

#+RESULTS:
: org-mode-setup


#+begin_src emacs-lisp
  (use-package org
    :hook (org-mode . org-mode-setup)
    :bind (
           :map org-mode-map
           ("H-e" . org-narrow-to-element)
           ("C-H-r" . org-narrow-to-subtree)
           ("C-H-e" . org-narrow-to-block)
           ("H-o" . consult-org-heading)
           ("M-s o" . my/org-occur)
           ("H-," . 'org-promote-subtree)
           ("H-." . 'org-demote-subtree)
           ("s-b" . 'org-tree-to-indirect-buffer)
           ("C-M-<right>" . org-demote-subtree)
           ("C-M-<left>" . org-promote-subtree)
           ("M-p" . org-previous-visible-heading) 
           ("M-n" . org-next-visible-heading)
           ("C-c C-x l" . my/toggle-logbook)
           ("C-c M-a" . org-attach-screenshot)
           ("C-M-i" . org-indent-block)
           ("C-t". org-toggle-checkbox))
    :config

    ;; use ids for linking
    (setq org-id-link-to-org-use-id t)
    ;; set agenda files
    (setq org-agenda-files
          (quote ("~/Orgfiles"
                  "~/Orgfiles/Productivity/Goals"
                  "~/Orgfiles/Productivity/Review"
                  )))

    (setq org-agenda-clockreport-parameter-plist
          '(:link t :maxlevel 2 :fileskip0 t))
    
    (setq org-attach-method 'cp)

    ;; Toggle functions for opening and closing property and logbook drawers

    (defun my/toggle-logbook ()
      "Open the logbook drawer of the current heading."
      (interactive)
      (save-excursion
        (org-back-to-heading t)
        (re-search-forward ":LOGBOOK:" nil t)
        (org-hide-drawer-toggle )))

    (defun my/toggle-properties ()
      "Open the properties drawer of the current heading."
      (interactive)
      (save-excursion
        (org-back-to-heading t)
        (re-search-forward ":PROPERTIES:" nil t)
        (org-hide-drawer-toggle )))

    ;; Redefine org-meta to use my custom meeting context version

    ;; set org-habit colors to fit with Nano Theme
    (with-eval-after-load 'org-habit
      (set-face-attribute 'org-habit-clear-face nil :background "#5A6680")
      (set-face-attribute 'org-habit-clear-future-face nil :background "#4C566A")
      (set-face-attribute 'org-habit-ready-face nil :background "#A3BE8C")
      (set-face-attribute 'org-habit-ready-future-face nil :background "#7A9E6A")
      (set-face-attribute 'org-habit-alert-face nil :background "#F5C518")
      (set-face-attribute 'org-habit-alert-future-face nil :background "#E0A800")
      (set-face-attribute 'org-habit-overdue-face nil :background "#BF616A")
      (set-face-attribute 'org-habit-overdue-future-face nil :background "#8C3A40"))

    ;; setup deadlines to match nano theme
    (with-eval-after-load 'org
      (set-face-attribute 'org-upcoming-distant-deadline nil :foreground "#C4A000" :weight 'normal)
      (set-face-attribute 'org-upcoming-deadline nil :foreground "#F5C518" :weight 'bold)
      (set-face-attribute 'org-imminent-deadline nil :foreground "#E0A800" :weight 'bold)
      (set-face-attribute 'org-warning nil :foreground "#BF616A" :weight 'bold) )

    (setq org-image-actual-width nil)
    (setq org-agenda-start-with-log-mode t)
    (setq org-log-done 'time)
    (setq org-log-into-drawer t)
    (setq org-extend-today-until 3
          org-use-effective-time t)
    (setq org-startup-with-inline-images t)
    (setq org-ellipsis " ▼")
    (setq org-hide-emphasis-markers t)
    (setq org-agenda-window-setup 'current-window)
    (setq org-use-speed-commands t)      
    (setq org-capture-babel-evaluate t)
    (setq org-todo-repeat-to-state "NEXT")
    (setq org-todo-keywords
          ;; need to add in "Someday Maybe status"
          (quote ((sequence "TODO(t)" "NEXT(n)" "|" "DONE(d)")
                  (sequence "WAITING(w@/!)" "HOLD(h@/!)" "|" "CANCELLED(c@/!)" "PHONE" "MEETING"))))

    (setq org-todo-keyword-faces
          (quote (("TODO" :foreground "red" :weight bold)
                  ("NEXT" :foreground "blue" :weight bold)
                  ("DONE" :foreground "forest green" :weight bold)
                  ("WAITING" :foreground "orange" :weight bold)
                  ("HOLD" :foreground "magenta" :weight bold)
                  ("CANCELLED" :foreground "forest green" :weight bold)
                  ("MEETING" :foreground "forest green" :weight bold)
                  ("PHONE" :foreground "forest green" :weight bold))))

    (setq org-todo-state-tags-triggers
          (quote (("CANCELLED" ("CANCELLED" . t))
                  ("WAITING" ("WAITING" . t)) 
                  ("HOLD" ("WAITING") ("HOLD" . t))
                  (done ("WAITING") ("HOLD"))
                  ("TODO" ("WAITING") ("CANCELLED") ("HOLD"))
                  ("NEXT" ("WAITING") ("CANCELLED") ("HOLD"))
                  ("DONE" ("WAITING") ("CANCELLED") ("HOLD")))))

    ;; s-Left & s-Right moves status
    (setq org-treat-S-cursor-todo-selection-as-state-change nil)

    (setq org-global-properties
          '(("Effort_All" . "0 0:10 0:15 0:30 0:45 1:00 1:15 1:30 1:45  2:00 2:30  3:00 3:30 4:00")))

      					; org capture

    (set-face-attribute 'org-agenda-clocking nil
                        :background 'unspecified
                        :foreground "#A3BE8C"
                        :weight 'bold
                        :extend t)
    
    (setq org-capture-templates
          '(
            ("t" "Tasks/Projects")
            ("tt" "Task" entry (file "~/Orgfiles/refile.org")
             "* TODO %?\n:PROPERTIES:\n:CATEGORY: Task\n:END:\n %U\n %a\n %i " :empty-lines 1)
            ("tp" "Project" entry (file "~/Orgfiles/refile.org")
             "* TODO %?\n:PROPERTIES:\n:CATEGORY: Proj\n:END:\n %U\n %a\n %i " :empty-lines 1)

            ("m" "Meeting" entry (file "~/Orgfiles/refile.org")
             "* MEETING re: %? :MEETING:\n%U\n** Attendees\n- \n** Meetings Notes   :NOTE:\n- \n** Next Steps\n\n" :clock-in t :clock-resume t :jump-to-captured t)
            ("p" "Phone call" entry (file "~/Orgfiles/refile.org")
             "* PHONE %? :PHONE:\n%U" :clock-in t :clock-resume t :jump-to-captured t)
            ("n" "note" entry (file "~/Orgfiles/refile.org")
             "* %? :NOTE:\n%u\n%a\n %i" :empty-lines 1)
            ("r" "Reminders")
            ("r1" "One Time Reminder" entry (file "~/Orgfiles/refile.org")
             "* %? \n %^T \n %U\n %a\n%i" :empty-lines 1)
            ("r!" "Recurring Reminder" entry (file "~/Orgfiles/refile.org")
             "* %? \n %U\n <%\\%(memq (calendar-day-of-week date) '(1 2 3 4 5))>  \n%a\n%i" :empty-lines 1)
            ("d" "Daily Metrics")
            ("dm" "Morning Metrics" table-line (file+headline "~/Orgfiles/metrics.org" "Morning")
             "| %^{Day} | %^{Weight} | %^{BP SYS} | %^{BP DIA}| %^{Sleep Time} | %^{AHI} | %^{Mood} | " :kill-buffer t)
            ("de" "Evening Metrics" table-line (file+headline "~/Orgfiles/metrics.org" "Evening")
             "| %^{Day} | %^{Output} | %^{On Schedule} | %^{Mood}| " :kill-buffer t)
            ("di" "Daily Improvement" entry (file+headline "~/Orgfiles/metrics.org" "Improvements List")
             "*** %^{Day Of The Week}\n- %?")
            ("df" "Food Log" entry (file+datetree "~/Orgfiles/FoodLog.org")
             "* %?")
            ("ds" "Symptom Tracker" entry (file+datetree "~/Orgfiles/Symptom-Tracker.org")
             "* %?")

            ("h" "Habit" entry (file "~/Orgfiles/habits.org")
             "* TODO %? %^g\n%U\n %a\n%i %^{STYLE|habit}p" :empty-lines 1)
            ("g" "Goal")
            (
             "gv"     ;key
             "Goal"  ; description
             entry   ; type  
             (file "~/Orgfiles/refile.org") ; target
             "* ~%^{Goal}~ :VALUE:\n:PROPERTIES:\n:created: %u\n:END:\n:LINKS:\n:END: %^{custom_id}"
             :prepend t ; prepend properties
             :created t ;

             )
            (
             "go"     ;key
             "Outcome"  ; description
             entry   ; type  
             (file "~/Orgfiles/refile.org") ; target
             "* %^{Goal} [/] :OUTCOME:\n:PROPERTIES:\n:created: %u\n:END:\n:LINKS:\n:goal:\n:habit:\n:project:\n:END: %^{timeline}p %^{custom_id}p"
             :prepend t ; prepend properties
             :created t ;
             )
            ("s" "System")
            ("sl" "System Log" entry
             (file+headline "~/Orgfiles/config.org" "Logs")
             "** Changes\n*** %^{Title}\n[%<%Y-%m-%d %a %H:%M>]\n:CONTEXT:\n- File/Location: %^{File or Location}\n- Reason: %^{Reason}\n:END:\n\n%?\n"
             :empty-lines 1)
            ("sf" "System Fix" entry
             (file+headline "~/Orgfiles/config.org" "Logs")
             "** Fixes\n*** %^{Title}\n[%<%Y-%m-%d %a %H:%M>]\n:CONTEXT:\n- File/Location: %^{File or Location}\n- Problem: %^{Problem}\n- Solution: %^{Solution}\n:END:\n\n%?\n"
             :empty-lines 1)
            ("si" "System Install" entry
             (file+headline "~/Orgfiles/config.org" "Logs")
             "** Installs\n*** %^{Title}\n[%<%Y-%m-%d %a %H:%M>]\n:CONTEXT:\n- Package/Tool: %^{Package or Tool}\n- Version: %^{Version}\n- Installed via: %^{apt/pip/cargo/etc}\n- Reason: %^{Reason}\n:END:\n\n%?\n"
             :empty-lines 1)
            ("c" "Cooking")
            ("cr" "Recipe" entry
             (file+headline "~/Orgfiles/recipes.org" "New Recipes to File" )
    	   (function my/recipe-capture-template)
             :empty-lines 1)



            ("l" "Lexicon" )
            (
             "ln" ; key
             "New Word"
             entry
             (file+headline "~/Orgfiles/Lexicon.org" "New Words")
             "** %^{Word}  :drill:\n*Pronunciation:*\n\n*Definition:*\n[%?]\n\nEx:\n*** Synonyms\n- ")
            (
             "lm"
             "Use More"
             entry
             (file+headline "~/Orgfiles/Lexicon.org" "Words To Use More")
             "** %^{Word}\n")

            )))

  ;; recipe category scrape + add new if not found
  (defun my/recipe-capture-template ()
    (concat
     "*** %^{Recipe Name} %^g\n"
     ":PROPERTIES:\n"
     ":ID:              %(org-id-new)\n"
     ":AUTHOR:          %^{Author}\n"
     ":LINK:            %^{Link}\n"
     ":SERVINGS:        %^{Servings}\n"
     ":SERVING_SIZE:    %^{Serving Size}\n"
     ":CALORIES:\n"
     ":FAT:\n"
     ":SATURATED_FAT:\n"
     ":SODIUM:\n"
     ":CARBS:\n"
     ":FIBER:\n"
     ":SUGAR:\n"
     ":PROTEIN:\n"
     ":END:\n"
     "**** Overview\n"
     "%?\n"
     "**** Ingredients\n"
     "**** Instructions\n"
     "**** Notes\n"))




#+END_SRC

#+RESULTS:
: my/recipe-capture-template

*** Bindings  
:PROPERTIES:
:ID:       e9be5709-f9a3-48d1-8d2b-6887877242fe
:END:
#+BEGIN_SRC emacs-lisp 
    					; hotkey bindings
  (define-key global-map (kbd "C-c c")
              (lambda () (interactive) (org-capture)))

  (define-key global-map (kbd "<f13>")
              (lambda () (interactive) (org-capture nil "tt")))
  (define-key global-map (kbd "M-<f13>")
              (lambda () (interactive) (org-capture nil "tp")))


  (define-key global-map (kbd "C-c m")
              (lambda () (interactive) (org-capture nil "m")))

  (global-set-key (kbd "C-c a") 'org-agenda)
  (global-set-key (kbd "<f5>") 'toggle-next-task-display)
  ;; promote and demote trees using hyper
  (global-set-key (kbd "<f18>") 'org-toggle-inline-images)  


    					; refile targets

  (setq org-refile-targets (quote ((nil :maxlevel . 9)
                                   (org-agenda-files :maxlevel . 9))))

    					; Use full outline paths for refile targets - we file directly with IDO
  (setq org-refile-use-outline-path t)

    					; Targets complete directly with IDO
  (setq org-outline-path-complete-in-steps nil)

    					; Allow refile to create parent tasks with confirmation
  (setq org-refile-allow-creating-parent-nodes (quote confirm))

    					; Use IDO for both buffer and file completion and ido-everywhere to t

                                         ;;;; Refile settings
    					; Exclude DONE state tasks from refile targets
  (defun bh/verify-refile-target ()
    "Exclude todo keywords with a done state from refile targets"
    (not (member (nth 2 (org-heading-components)) org-done-keywords)))

  (setq org-refile-target-verify-function 'bh/verify-refile-target)
  (require 'org-habit)
  (add-to-list 'org-modules 'org-habit)
  (setq org-habit-graph-column 50)
  (setq org-habit-preceding-days 53)


  ;; lang modes
  (add-to-list  'org-src-lang-modes '("plantuml" . plantuml))
  (add-to-list 'org-src-lang-modes '("jsx" . js))


  (setq org-plantuml-jar-path "C:/Users/tyler/AppData/Roaming")
  (global-set-key (kbd "C-c b") 'org-switchb)
  (global-set-key (kbd "C-c l") 'org-store-link)
  (global-set-key (kbd "C-c H-$") 'org-archive-subtree)

                                               ;;;;; end org mode setup ;;;;;

                                               ;;;;;;; Org  Setup ;;;;;;;;;
  ;; Do not dim blocked tasks
  (setq org-agenda-dim-blocked-tasks nil)
  (add-hook 'org-agenda-finalize-hook #'hl-line-mode)

  ;; Compact the block agenda view
  (setq org-agenda-compact-blocks t)
  ;; start norang agenda view

  ;; variable setup

  ;; disable value goals from being inherited
  (setq org-tags-exclude-from-inheritance '("VALUE" "NOTE" "Q1" "Q2" "Q3" "Q4" "MEETING"))
  ;;reverse note order disabled
  (setq org-reverse-note-order nil)
  ;; Leading 0 for military time
  (setq org-agenda-time-leading-zero t)
  ;; sticky agendas
  (setq org-agenda-sticky t)
  ;; Enforce Dependencies
  (setq org-enforce-todo-dependencies t)
  ;; Start with log mode disabled
  (setq org-agenda-start-with-log-mode nil)
  ;; setup 5 degrees of priority
  (setq org-lowest-priority ?E)
  ;; number of seperators to 0
  (setq org-cycle-seperator-lines 0)
  ;; Remove completed deadline tasks from the agenda view
  (setq org-agenda-skip-deadline-if-done t)
  ;; Remove completed scheduled tasks from the agenda view
  (setq org-agenda-skip-scheduled-if-done t)
  ;; Remove completed items from search results
  (setq org-agenda-skip-timestamp-if-done t)
  ;; Setup Time Grid
  (setq org-agenda-time-grid
        (quote ((daily today require-timed)
                (500 600 700 800 900 1000 1100 1200 1300 1400 1500 1600 1700 1800 1900 2000 2100 2200 2300)
                "......" "------------")))
  ;;search archives
  (setq org-agenda-text-search-extra-files (quote (agenda-archives)))
  ;; honor ignore options
  (setq org-agenda-tags-todo-honor-ignore-options t)

  ;; Remove empty LOGBOOK drawers on clock out
  ;; (defun bh/remove-empty-drawer-on-clock-out ()
  ;;   (interactive)
  ;;   (save-excursion
  ;;     (beginning-of-line 0)
  ;;     (org-remove-empty-drawer-at "LOGBOOK" (point))))

  ;; (add-hook 'org-clock-out-hook 'bh/remove-empty-drawer-on-clock-out 'append)

  (defun my/org-auto-exclude-function (tag)
    "Automatic task exclusion i the agenda with / RET"
    (and (cond
          ((string= tag "REVIEW")
           t)
          ((string= tag "CONFIG")))
         (concat "-" tag)))

  (setq org-agenda-auto-exclude-function 'my/org-auto-exclude-function)




#+end_src

#+RESULTS:
: my/org-auto-exclude-function
*** Goal Link Inserter
:PROPERTIES:
:ID:       5c6002cb-cb2e-46bf-af89-2c14d6070bab
:END:
#+BEGIN_SRC emacs-lisp
  (defvar my/goals-file "~/Orgfiles/Productivity/Goals/Goals.org"
    "Path to the goals file containing CUSTOM_ID targets.")

  (defvar my/goals-custom-id-cache nil
    "Alist of (HEADING . CUSTOM_ID) scraped from `my/goals-file'.")

  (defun my/goals-refresh-custom-ids ()
    "Scan `my/goals-file' and cache its CUSTOM_ID headings."
    (interactive)
    (setq my/goals-custom-id-cache
          (when (file-exists-p my/goals-file)
            (with-current-buffer (find-file-noselect my/goals-file)
              (org-map-entries
               (lambda ()
                 (let ((id (org-entry-get (point) "CUSTOM_ID"))
                       (heading (org-get-heading t t t t)))
                   (when id (cons heading id))))
               nil 'file))))
    (message "Loaded %d goal IDs" (length my/goals-custom-id-cache)))

  (defun my/goals-insert-link ()
    "Insert a link to a heading in `my/goals-file' by CUSTOM_ID, picked via completion."
    (interactive)
    (unless my/goals-custom-id-cache
      (my/goals-refresh-custom-ids))
    (let* ((choice (completing-read "Goal: " my/goals-custom-id-cache nil t))
           (id (cdr (assoc choice my/goals-custom-id-cache))))
      (when id
        (insert (format "[[file:%s::#%s][%s]]" my/goals-file id choice)))))

  ;; Populate the cache once Emacs finishes starting
  (add-hook 'emacs-startup-hook #'my/goals-refresh-custom-ids)

  (global-set-key (kbd "C-c i g") #'my/goals-insert-link)  
#+END_SRC

#+RESULTS:
: my/goals-insert-link

** Org Agenda Custom Functions & Macros
:PROPERTIES:
:ID:       5ead4de2-f567-4f75-b599-e28b6df04a71
:END:
#+BEGIN_SRC emacs-lisp
  ;; Jumping Macro Definitions

  (fset 'my/org-agenda-jump-to-stuck-projects
        (kmacro-lambda-form [?\M-< ?\C-s ?S ?t ?u ?c ?k ?  ?P ?r ?o ?j ?e ?c ?t ?s return ?\C-a] 0 "%d"))

  (fset 'my/org-agenda-jump-to-projects
        (kmacro-lambda-form [?\M-< ?\C-s ?P ?r ?o ?j ?e ?c ?t ?s ?\C-s return ?\M-b] 0 "%d"))

  (fset 'my/org-agenda-jump-to-project-next-tasks
        (kmacro-lambda-form [?\M-< ?\C-s ?P ?r ?o ?j ?e ?c ?t ?  ?N ?e ?x ?t ?  ?T ?a ?s ?k ?s return ?\C-a] 0 "%d"))

  (fset 'my/org-agenda-jump-to-standalone-tasks
        (kmacro-lambda-form [?\M-< ?\C-s ?S ?t ?a ?n ?d ?a ?l ?o ?n ?e ?  ?T ?a ?s ?k ?s return ?\C-a] 0 "%d"))

  (fset 'my/org-agenda-jump-to-project-subtasks
        (kmacro-lambda-form [?\M-< ?\C-s ?P ?r ?o ?j ?e ?c ?t ?  ?S ?u ?b ?t ?a ?s ?k ?s return ?\C-a] 0 "%d"))

  (fset 'my/org-agenda-jump-to-waiting-and-postponed-tasks
        (kmacro-lambda-form [?\M-< ?\C-s ?W ?a ?i ?t ?i ?n ?g ?  ?a ?n ?d ?  ?P ?o ?s ?t ?p ?o ?n ?e ?d ?  ?T ?a ?s ?k ?s return ?\C-a] 0 "%d"))
  (fset 'my/org-agenda-jump-to-tasks-to-archive
        (kmacro-lambda-form [?\M-< ?\C-s ?T ?a ?s ?k ?s ?  ?t ?o ?  ?A ?r ?c ?h ?i ?v ?e return ?\C-a] 0 "%d"))
  (fset 'my/org-agenda-jump-to-tasks-to-refile
        (kmacro-lambda-form [?\M-< ?\C-s ?T ?a ?s ?k ?s ?  ?t ?o ?  ?R ?e ?f ?i ?l ?e return ?\C-a] 0 "%d"))


  (fset 'my/org-make-list-todos
        (kmacro-lambda-form [return ?\C-e return ?\C-p ?\C-a ?* ?  delete ?T ?O ?D ?\S-  backspace ?o ?\S-  backspace backspace ?O ?\S-  ?\C-c ?\C-x ?p ?C ?A ?T ?E ?G ?O ?R ?Y return ?T ?a ?s ?k return ?\C-n ?\C-n ?\C-c ?! return ?\C-n ?\C-a] 0 "%d"))


  (defun my/org-convert-list-into-todos()
    "Macro expansion for repeat key"
    (interactive)
    (my/org-make-list-todos)
    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "t") 'my/org-convert-list-into-todos)
       map)))



  ;; keybinds
  (define-key org-agenda-mode-map (kbd "C-j s") 'my/org-agenda-jump-to-stuck-projects)
  (define-key org-agenda-mode-map (kbd "C-j p") 'my/org-agenda-jump-to-projects)
  (define-key org-agenda-mode-map (kbd "C-j n") 'my/org-agenda-jump-to-project-next-tasks)
  (define-key org-agenda-mode-map (kbd "C-j t") 'my/org-agenda-jump-to-standalone-tasks)
  (define-key org-agenda-mode-map (kbd "C-j u") 'my/org-agenda-jump-to-project-subtasks)
  (define-key org-agenda-mode-map (kbd "C-j w") 'my/org-agenda-jump-to-waiting-and-postponed-tasks)
  (define-key org-agenda-mode-map (kbd "C-j a") 'my/org-agenda-jump-to-tasks-to-archive)
  (define-key org-agenda-mode-map (kbd "C-j r") 'my/org-agenda-jump-to-tasks-to-refile)
  (define-key org-agenda-mode-map (kbd "<C-tab>") 'my/org-agenda-zoom-to-entry)
  (define-key org-agenda-mode-map (kbd "C-o") 'my/org-agenda-view-entry-narrowed)
  (define-key org-agenda-mode-map (kbd "H-,") 'org-agenda-remove-restriction-lock)
  (define-key org-agenda-mode-map (kbd "H-.") 'my/org-agenda-set-restriction-lock)
  (define-key org-agenda-mode-map (kbd "H-/") 'my/toggle-org-agenda-restriction)
  (define-key org-agenda-mode-map (kbd "M-s o") 'org-occur-in-agenda-files)
  (define-key org-agenda-mode-map (kbd "[") 'my/org-agenda-set-category-project)
  (define-key org-agenda-mode-map (kbd "]") 'my/org-agenda-set-category-task)
  (define-key org-agenda-mode-map (kbd "C-c C-e") 'org-export-dispatch)

  (global-set-key  (kbd "H-c t") 'my/org-convert-list-into-todos)

  ;; goto-entry macro definitions
  (fset 'my/org-agenda-zoom-to-entry
        (kmacro-lambda-form [tab ?\H-e] 0 "%d"))

  (fset 'my/org-agenda-view-entry-narrowed
        (kmacro-lambda-form [?  ?\C-x ?o ?\H-e ?\C-x ?o] 0 "%d"))



#+END_SRC

#+RESULTS:
: #[256 "\301\242\302\305\300\306#\210\301\240*\207" [[32 24 111 16777317 24 111] (0) "%d" kmacro-counter-format-start kmacro-counter execute-kbd-macro kmacro-loop-setup-function] 5 kmacro]
*** Custom habit hider
#+BEGIN_SRC emacs-lisp
;; ---- selective habit hiding support (local to agenda views) -------------


(defun my/agenda-hide-stacked-habits (hide-morning hide-evening)
"Return a skip function that hides habit entries depending on stack tags."
(lambda ()
(when (and (org-is-habit-p)
(or (and hide-morning (member "@morning" (org-get-tags)))
(and hide-evening (member "@evening" (org-get-tags)))))
;; Skip the entire subtree
(save-excursion (org-end-of-subtree t) (point)))))

#+END_SRC>
*** Custom Agenda Setup
:PROPERTIES:
:ID:       f5a2570e-8156-44cc-9e23-8fad2fdcb201
:END:
#+BEGIN_SRC emacs-lisp

  (setq org-agenda-custom-commands
        (quote (("n" "Notes" tags "NOTE"
                 ((org-agenda-overriding-header "Notes")
                  (org-tags-match-list-sublevels t)))
                ("h" "Habits" tags-todo "STYLE=\"habit\""
                 ((org-agenda-overriding-header "Habits")
                  (org-agenda-sorting-strategy
                   '(todo-state-down effort-up category-keep))))
                ("r" "Weekly Review Rollup" tags "Accomplishment|Disappointment"
                 ((org-agenda-overriding-header "Accomplishments & Disappointments")
                  (org-tags-match-list-sublevels 'indented)))
    	      ( "d" "Daily Agenda" ((agenda ""
                                              ((org-agenda-span 1))
                                              ))
  		) 	      
  	      ( "w" "Weekly Agenda" ((agenda ""
  					     ((org-agenda-span 7))
  					     ))
  		)                
  	      ("p" "Personal Agenda"
  	       ((agenda "" nil)
  		(tags-todo "-CANCELLED/!"
  			   ((org-agenda-overriding-header "Stuck Projects")
  			    (org-agenda-skip-function 'bh/skip-non-stuck-projects)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-HOLD-CANCELLED/!"
  			   ((org-agenda-overriding-header "Projects")
  			    (org-agenda-skip-function 'bh/skip-non-projects)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED/!NEXT"
  			   ((org-agenda-overriding-header (concat "Project Next Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-projects-and-habits-and-single-tasks)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(todo-state-down effort-up category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Project Subtasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Standalone Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED+WAITING|HOLD/!"
  			   ((org-agenda-overriding-header (concat "Waiting and Postponed Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-tasks)
  			    (org-tags-match-list-sublevels nil)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)))
  		(tags "-REFILE/"
  		      ((org-agenda-overriding-header "Tasks to Archive")
  		       (org-agenda-skip-function 'bh/skip-non-archivable-tasks)
  		       (org-tags-match-list-sublevels nil))))
  	       ((org-agenda-tag-filter-preset '("+PERSONAL"))))
  	      ("P" "Personal Project List"
  	       (
  		(tags-todo "-CANCELLED/!"
  			   ((org-agenda-overriding-header "Personal Project List\n---------------------\nStuck Projects")
  			    (org-agenda-skip-function 'bh/skip-non-stuck-projects)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-HOLD-CANCELLED/!"
  			   ((org-agenda-overriding-header "Projects")
  			    (org-agenda-skip-function 'bh/skip-non-projects)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED/!NEXT"
  			   ((org-agenda-overriding-header (concat "Project Next Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-projects-and-habits-and-single-tasks)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(todo-state-down effort-up category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Project Subtasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Standalone Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED+WAITING|HOLD/!"
  			   ((org-agenda-overriding-header (concat "Waiting and Postponed Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))

  			    (org-tags-match-list-sublevels nil)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)))
  		(tags "-REFILE/"
  		      ((org-agenda-overriding-header "Tasks to Archive")
  		       (org-agenda-skip-function 'bh/skip-non-archivable-tasks)
  		       (org-tags-match-list-sublevels nil))))
  	       ((org-agenda-tag-filter-preset '("+PERSONAL"))))                
  	      ("C" "Config Project List"
  	       ((tags-todo "-CANCELLED/!"
  			   ((org-agenda-overriding-header "Config Project List\n-------------------\nStuck Projects")
  			    (org-agenda-skip-function 'bh/skip-non-stuck-projects)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-HOLD-CANCELLED/!"
  			   ((org-agenda-overriding-header "Projects")
  			    (org-agenda-skip-function 'bh/skip-non-projects)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED/!NEXT"
  			   ((org-agenda-overriding-header (concat "Project Next Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-projects-and-habits-and-single-tasks)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(todo-state-down effort-up category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Project Subtasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Standalone Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED+WAITING|HOLD/!"
  			   ((org-agenda-overriding-header (concat "Waiting and Postponed Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-tasks)
  			    (org-tags-match-list-sublevels nil)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)))
  		(tags "-REFILE/"
  		      ((org-agenda-overriding-header "Tasks to Archive")
  		       (org-agenda-skip-function 'bh/skip-non-archivable-tasks)
  		       (org-tags-match-list-sublevels nil))))
  	       ((org-agenda-tag-filter-preset '("+CONFIG"))))
  	      ("c" "Config Agenda"
  	       ((agenda "" nil)
  		(tags-todo "-CANCELLED/!"
  			   ((org-agenda-overriding-header "Stuck Projects")
  			    (org-agenda-skip-function 'bh/skip-non-stuck-projects)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-HOLD-CANCELLED/!"
  			   ((org-agenda-overriding-header "Projects")
  			    (org-agenda-skip-function 'bh/skip-non-projects)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED/!NEXT"
  			   ((org-agenda-overriding-header (concat "Project Next Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-projects-and-habits-and-single-tasks)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(todo-state-down effort-up category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Project Subtasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Standalone Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED+WAITING|HOLD/!"
  			   ((org-agenda-overriding-header (concat "Waiting and Postponed Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-tasks)
  			    (org-tags-match-list-sublevels nil)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)))
  		(tags "-REFILE/"
  		      ((org-agenda-overriding-header "Tasks to Archive")
  		       (org-agenda-skip-function 'bh/skip-non-archivable-tasks)
  		       (org-tags-match-list-sublevels nil))))
  	       ((org-agenda-tag-filter-preset '("+CONFIG"))))                
  	      ("o" "Office Agenda"
  	       ((agenda "" nil)
  		(tags-todo "-CANCELLED/!"
  			   ((org-agenda-overriding-header "Stuck Projects")
  			    (org-agenda-skip-function 'bh/skip-non-stuck-projects)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-HOLD-CANCELLED/!"
  			   ((org-agenda-overriding-header "Projects")
  			    (org-agenda-skip-function 'bh/skip-non-projects)
  			    (org-tags-match-list-sublevels 't)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED/!NEXT"
  			   ((org-agenda-overriding-header (concat "Project Next Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-projects-and-habits-and-single-tasks)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(todo-state-down effort-up category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Project Subtasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Standalone Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED+WAITING|HOLD/!"
  			   ((org-agenda-overriding-header (concat "Waiting and Postponed Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-tasks)
  			    (org-tags-match-list-sublevels nil)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)))
  		(tags "-REFILE/"
  		      ((org-agenda-overriding-header "Tasks to Archive")
  		       (org-agenda-skip-function 'bh/skip-non-archivable-tasks)
  		       (org-tags-match-list-sublevels nil))))
  	       ((org-agenda-tag-filter-preset '("+SEAS"))))
  	      ("O" "Office Project List"
  	       (
  		(tags-todo "-CANCELLED/!"
  			   ((org-agenda-overriding-header "Office Project List\n--------------------\nStuck Projects")
  			    (org-agenda-skip-function 'bh/skip-non-stuck-projects)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-HOLD-CANCELLED/!"
  			   ((org-agenda-overriding-header "Projects")
  			    (org-agenda-skip-function 'bh/skip-non-projects)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED/!NEXT"
  			   ((org-agenda-overriding-header (concat "Project Next Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-projects-and-habits-and-single-tasks)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(todo-state-down effort-up category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Project Subtasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Standalone Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED+WAITING|HOLD/!"
  			   ((org-agenda-overriding-header (concat "Waiting and Postponed Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-tasks)
  			    (org-tags-match-list-sublevels nil)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)))
  		(tags "-REFILE/"
  		      ((org-agenda-overriding-header "Tasks to Archive")
  		       (org-agenda-skip-function 'bh/skip-non-archivable-tasks)
  		       (org-tags-match-list-sublevels nil))))
  	       ((org-agenda-tag-filter-preset '("+SEAS"))))                
  	      (" " "Agenda"
  	       ((agenda "" nil)
  		(tags "REFILE"
  		      ((org-agenda-overriding-header "Tasks to Refile")
  		       (org-tags-match-list-sublevels nil)))
  		(tags-todo "-CANCELLED/!"
  			   ((org-agenda-overriding-header "Stuck Projects")
  			    (org-agenda-skip-function 'bh/skip-non-stuck-projects)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-HOLD-CANCELLED/!"
  			   ((org-agenda-overriding-header "Projects")
  			    (org-agenda-skip-function 'bh/skip-non-projects)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED/!NEXT"
  			   ((org-agenda-overriding-header (concat "Project Next Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-projects-and-habits-and-single-tasks)
  			    (org-tags-match-list-sublevels t)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(todo-state-down effort-up category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Project Subtasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-REFILE-CANCELLED-WAITING-HOLD/!"
  			   ((org-agenda-overriding-header (concat "Standalone Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-project-tasks)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-with-date bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-sorting-strategy
  			     '(category-keep))))
  		(tags-todo "-CANCELLED+WAITING|HOLD/!"
  			   ((org-agenda-overriding-header (concat "Waiting and Postponed Tasks"
  								  (if bh/hide-scheduled-and-waiting-next-tasks
  								      ""
  								    " (including WAITING and SCHEDULED tasks)")))
  			    (org-agenda-skip-function 'bh/skip-non-tasks)
  			    (org-tags-match-list-sublevels nil)
  			    (org-agenda-todo-ignore-scheduled bh/hide-scheduled-and-waiting-next-tasks)
  			    (org-agenda-todo-ignore-deadlines bh/hide-scheduled-and-waiting-next-tasks)))
  		(tags "-REFILE/"
  		      ((org-agenda-overriding-header "Tasks to Archive")
  		       (org-agenda-skip-function 'bh/skip-non-archivable-tasks)
  		       (org-tags-match-list-sublevels nil))))
  	       nil))))


#+END_SRC

#+RESULTS:

*** Helper functions
:PROPERTIES:
:ID:       3b875b4d-6226-4d92-af1c-6a5f3d66fa08
:END:
#+begin_src emacs-lisp
  ;; start norang helpers

  (defun my/project-prefix ()
    "create a prefix of pipes, slahses and dashes to visualies projects an their subprojects"
    (let ((level (org-current-level)))
      (cond
       ((> level 2) (concat "|" (make-string (* (- level 2) 2) ?-) "\\ "))
       (t ""))))

  (defun bh/skip-non-archivable-tasks ()
    "Skip trees that are not available for archiving"
    (save-restriction
      (widen)
      ;; Consider only tasks with done todo headings as archivable candidates
      (let ((next-headline (save-excursion (or (outline-next-heading) (point-max))))
            (subtree-end (save-excursion (org-end-of-subtree t))))
        (if (member (org-get-todo-state) org-todo-keywords-1)
            (if (member (org-get-todo-state) org-done-keywords)
                (let* ((daynr (string-to-number (format-time-string "%d" (current-time))))
                       (a-month-ago (* 60 60 24 (+ daynr 1)))
                       (last-month (format-time-string "%Y-%m-" (time-subtract (current-time) (seconds-to-time a-month-ago))))
                       (this-month (format-time-string "%Y-%m-" (current-time)))
                       (subtree-is-current (save-excursion
                                             (forward-line 1)
                                             (and (< (point) subtree-end)
                                                  (re-search-forward (concat last-month "\\|" this-month) subtree-end t)))))
                  (if subtree-is-current
                      subtree-end ; Has a date in this month or last month, skip it
                    nil))  ; available to archive
              (or subtree-end (point-max)))
          next-headline))))  



  (defun bh/find-project-task ()
    "Move point to the parent (project) task if any"
    (save-restriction
      (widen)
      (let ((parent-task (save-excursion (org-back-to-heading 'invisible-ok) (point))))
        (while (org-up-heading-safe)
          (when (member (nth 2 (org-heading-components)) org-todo-keywords-1)
            (setq parent-task (point))))
        (goto-char parent-task)
        parent-task)))


  (defun bh/is-project-p ()
    "Any task with a todo keyword subtask"
    (save-restriction
      (widen)
      (let ((has-subtask)
            (subtree-end (save-excursion (org-end-of-subtree t)))
            (is-a-task (member (nth 2 (org-heading-components)) org-todo-keywords-1)))
        (save-excursion
          (forward-line 1)
          (while (and (not has-subtask)
                      (< (point) subtree-end)
                      (re-search-forward "^\*+ " subtree-end t))
            (when (member (org-get-todo-state) org-todo-keywords-1)
              (setq has-subtask t))))
        (and is-a-task has-subtask))))

  (defun bh/is-project-subtree-p ()
    "Any task with a todo keyword that is in a project subtree.
               Callers of this function already widen the buffer view."
    (let ((task (save-excursion (org-back-to-heading 'invisible-ok)
                                (point))))
      (save-excursion
        (bh/find-project-task)
        (if (equal (point) task)
            nil
          t))))

  (defun bh/is-task-p ()
    "Any task with a todo keyword and no subtask"
    (save-restriction
      (widen)
      (let ((has-subtask)
            (subtree-end (save-excursion (org-end-of-subtree t)))
            (is-a-task (member (nth 2 (org-heading-components)) org-todo-keywords-1)))
        (save-excursion
          (forward-line 1)
          (while (and (not has-subtask)
                      (< (point) subtree-end)
                      (re-search-forward "^\*+ " subtree-end t))
            (when (member (org-get-todo-state) org-todo-keywords-1)
              (setq has-subtask t))))
        (and is-a-task (not has-subtask)))))

  (defun bh/is-subproject-p ()
    "Any task which is a subtask of another project"
    (let ((is-subproject)
          (is-a-task (member (nth 2 (org-heading-components)) org-todo-keywords-1)))
      (save-excursion
        (while (and (not is-subproject) (org-up-heading-safe))
          (when (member (nth 2 (org-heading-components)) org-todo-keywords-1)
            (setq is-subproject t))))
      (and is-a-task is-subproject)))

  (defun bh/list-sublevels-for-projects-indented ()
    "Set org-tags-match-list-sublevels so when restricted to a subtree we list all subtasks.
                 This is normally used  skipping functions where this variable is already local to the agenda."
    (if (marker-buffer org-agenda-restrict-begin)
        (setq org-tags-match-list-sublevels 'indented)
      (setq org-tags-match-list-sublevels nil))
    nil)

  (defun bh/list-sublevels-for-projects ()
    "Set org-tags-match-list-sublevels so when restricted to a subtree we list all subtasks.
                 This is normally used by skipping functions where this variable is already local to the agenda."
    (if (marker-buffer org-agenda-restrict-begin)
        (setq org-tags-match-list-sublevels t)
      (setq org-tags-match-list-sublevels nil))
    nil)

  (defvar bh/hide-scheduled-and-waiting-next-tasks t)

  (defun toggle-next-task-display ()
    (interactive)
    (setq bh/hide-scheduled-and-waiting-next-tasks (not bh/hide-scheduled-and-waiting-next-tasks))
    (when  (equal major-mode 'org-agenda-mode)
      (org-agenda-redo))
    (message "%s WAITING and SCHEDULED NEXT Tasks" (if bh/hide-scheduled-and-waiting-next-tasks "Hide" "Show")))

  (defun bh/skip-stuck-projects ()
    "Skip trees that are not stuck projects"
    (save-restriction
      (widen)
      (let ((next-headline (save-excursion (or (outline-next-heading) (point-max)))))
        (if (bh/is-project-p)
            (let* ((subtree-end (save-excursion (org-end-of-subtree t)))
                   (has-next ))
              (save-excursion
                (forward-line 1)
                (while (and (not has-next) (< (point) subtree-end) (re-search-forward "^\\*+ NEXT " subtree-end t))
                  (unless (member "WAITING" (org-get-tags-at))
                    (setq has-next t))))
              (if has-next
                  nil
                next-headline)) ; a stuck project, has subtasks but no next task
          nil))))

  (defun bh/skip-non-stuck-projects ()
    "Skip trees that are not stuck projects"
    ;; (bh/list-sublevels-for-projects-indented)
    (save-restriction
      (widen)
      (let ((next-headline (save-excursion (or (outline-next-heading) (point-max)))))
        (if (bh/is-project-p)
            (let* ((subtree-end (save-excursion (org-end-of-subtree t)))
                   (has-next ))
              (save-excursion
                (forward-line 1)
                (while (and (not has-next) (< (point) subtree-end) (re-search-forward "^\\*+ NEXT " subtree-end t))
                  (unless (member "WAITING" (org-get-tags-at))
                    (setq has-next t))))
              (if has-next
                  next-headline
                nil)) ; a stuck project, has subtasks but no next task
          next-headline))))

  (defun bh/skip-non-projects ()
    "Skip trees that are not projects"
    ;; (bh/list-sublevels-for-projects-indented)
    (if (save-excursion (bh/skip-non-stuck-projects))
        (save-restriction
          (widen)
          (let ((subtree-end (save-excursion (org-end-of-subtree t))))
            (cond
             ((bh/is-project-p)
              nil)
             ((and (bh/is-project-subtree-p) (not (bh/is-task-p)))
              nil)
             (t
              subtree-end))))
      (save-excursion (org-end-of-subtree t))))

  (defun bh/skip-non-tasks ()
    "Show non-project tasks.
               Skip project and sub-project tasks, habits, and project related tasks."
    (save-restriction
      (widen)
      (let ((next-headline (save-excursion (or (outline-next-heading) (point-max)))))
        (cond
         ((bh/is-task-p)
          nil)
         (t
          next-headline)))))

  (defun bh/skip-project-trees-and-habits ()
    "Skip trees that are projects"
    (save-restriction
      (widen)
      (let ((subtree-end (save-excursion (org-end-of-subtree t))))
        (cond
         ((bh/is-project-p)
          subtree-end)
         ((org-is-habit-p)
          subtree-end)
         (t
          nil)))))

  (defun bh/skip-projects-and-habits-and-single-tasks ()
    "Skip trees that are projects, tasks that are habits, single non-project tasks"
    (save-restriction
      (widen)
      (let ((next-headline (save-excursion (or (outline-next-heading) (point-max)))))
        (cond
         ((org-is-habit-p)
          next-headline)
         ((and bh/hide-scheduled-and-waiting-next-tasks
               (member "WAITING" (org-get-tags-at)))
          next-headline)
         ((bh/is-project-p)
          next-headline)
         ((and (bh/is-task-p) (not (bh/is-project-subtree-p)))
          next-headline)
         (t
          nil)))))

  (defun bh/skip-project-tasks-maybe ()
    "Show tasks related to the current restriction.
               When restricted to a project, skip project and sub project tasks, habits, NEXT tasks, and loose tasks.
               When not restricted, skip project and sub-project tasks, habits, and project related tasks."
    (save-restriction
      (widen)
      (let* ((subtree-end (save-excursion (org-end-of-subtree t)))
             (next-headline (save-excursion (or (outline-next-heading) (point-max))))
             (limit-to-project (marker-buffer org-agenda-restrict-begin)))
        (cond
         ((bh/is-project-p)
          next-headline)
         ((org-is-habit-p)
          subtree-end)
         ((and (not limit-to-project)
               (bh/is-project-subtree-p))
          subtree-end)
         ((and limit-to-project
               (bh/is-project-subtree-p)
               (member (org-get-todo-state) (list "NEXT")))
          subtree-end)
         (t
          nil)))))

  (defun bh/skip-project-tasks ()
    "Show non-project tasks.
               Skip project and sub-project tasks, habits, and project related tasks."
    (save-restriction
      (widen)
      (let* ((subtree-end (save-excursion (org-end-of-subtree t))))
        (cond
         ((bh/is-project-p)
          subtree-end)
         ((org-is-habit-p)
          subtree-end)
         ((bh/is-project-subtree-p)
          subtree-end)
         (t
          nil)))))

  (defun bh/skip-non-project-tasks ()
    "Show project tasks.
               Skip project and sub-project tasks, habits, and loose non-project tasks."
    (save-restriction
      (widen)
      (let* ((subtree-end (save-excursion (org-end-of-subtree t)))
             (next-headline (save-excursion (or (outline-next-heading) (point-max)))))
        (cond
         ((bh/is-project-p)
          next-headline)
         ((org-is-habit-p)
          subtree-end)
         ((and (bh/is-project-subtree-p)
               (member (org-get-todo-state) (list "NEXT")))
          subtree-end)
         ((not (bh/is-project-subtree-p))
          subtree-end)
         (t
          nil)))))

  (defun bh/skip-projects-and-habits ()
    "Skip trees that are projects and tasks that are habits"
    (save-restriction
      (widen)
      (let ((subtree-end (save-excursion (org-end-of-subtree t))))
        (cond
         ((bh/is-project-p)
          subtree-end)
         ((org-is-habit-p)
          subtree-end)
         (t
          nil)))))

  (defun bh/skip-non-subprojects ()
    "Skip trees that are not projects"
    (let ((next-headline (save-excursion (outline-next-heading))))
      (if (bh/is-subproject-p)
          nil
        next-headline)))



#+end_src

#+RESULTS:
| #[0 \301\211\207 [imenu-create-index-function org-imenu-get-tree] 2] | org-transclusion-mode | org-mode-visual-fill | #[nil ((add-hook 'after-save-hook #'my/org-babel-tangle-config)) nil] | #[nil ((display-line-numbers-mode 0)) nil] | org-mode | valign-mode | #[0 \300\301\302\303\304$\207 [add-hook change-major-mode-hook org-fold-show-all append local] 5] | #[0 \300\301\302\303\304$\207 [add-hook change-major-mode-hook org-babel-show-result-all append local] 5] | org-babel-result-hide-spec | org-babel-hide-all-hashes | org-mode-setup | nano-modeline-org-mode |


*** Org Modern
:PROPERTIES:
:ID:       b4b087a7-adcf-4d21-aa63-7e60a7df3cd4
:END:
#+BEGIN_SRC emacs-lisp
  (set-fontset-font t '(#x25A0 . #x25FF) "Segoe UI Symbol")

  (use-package org-modern
    :hook (org-mode . org-modern-mode)
    :config
    (global-org-modern-mode)
    (setq org-modern-star 'replace
    	org-modern-cycle-stars nil  ;; level 6+ all reuse the last pair below, not cycle back to pair 1
  	org-modern-replace-stars
  	'(
  	  "◈"    ;; level 1 — white diamond containing black small diamond
  	  "◇"    ;; level 2 — white diamond
  	  "■"    ;; level 3 — black square
  	  "□"    ;; level 4 — white square
  	  "▪"    ;; level 5 — black small square
  	  "◌")  ;; level 6+ — dotted circle (reused for all deeper levels)

  	
  	org-modern-table nil    
  	org-modern-list '((?- . "•") (?+ . "◦") (?* . "▸"))
  	org-modern-checkbox nil ;; keep default checkboxes unless you want to style them
  	org-modern-tag t
  	org-modern-priority t
  	org-modern-todo t
  	org-modern-timestamp t)
    (setq org-modern-todo-faces
  	'(("TODO"      . (:background "#BF616A" :foreground "white"))
      	  ("NEXT"      . (:background "#81A1C1" :foreground "white"))
      	  ("DONE"      . (:background "#A3BE8C" :foreground "white"))
      	  ("WAITING"   . (:background "#D08770" :foreground "white"))
      	  ("HOLD"      . (:background "#B48EAD" :foreground "white"))
      	  ("CANCELLED" . (:background "#A3BE8C" :foreground "white"))
      	  ("MEETING"   . (:background "#A3BE8C" :foreground "white"))
      	  ("PHONE"     . (:background "#A3BE8C" :foreground "white"))))
    )


  ;; still need to suppress line numbers in org
  (dolist (mode '(org-mode-hook))
    (add-hook mode (lambda () (display-line-numbers-mode 0))))
#+END_SRC

#+RESULTS:

*** Structure Templates
:PROPERTIES:
:ID:       383633db-e515-4f5b-84ad-dafe8fa2cee8
:END:
#+begin_src emacs-lisp
  ;; This is needed as of Org 9.2
                                          ;  (require 'org-tempo)
                                          ;  (with-eval-after-load 'org-tempo



  ;; C-C C-, to use a structure template
  (add-to-list 'org-structure-template-alist '("sh" . "src shell"))
  (add-to-list 'org-structure-template-alist '("el" . "src emacs-lisp")) 
  (add-to-list 'org-structure-template-alist '("py" . "src python"))
  (add-to-list 'org-structure-template-alist '("jsx" . "src jsx"))
#+end_src

#+RESULTS:
: ((jsx . src jsx) (py . src python) (el . src emacs-lisp) (sh . src shell) (a . export ascii) (c . center) (C . comment) (e . example) (E . export) (h . export html) (l . export latex) (q . quote) (s . src) (v . verse))



*** Category Icons
:PROPERTIES:
:ID:       be7c69ec-84e8-4500-8f0b-1fdee7f5fa3d
:END:
#+BEGIN_SRC emacs-lisp
  (load "org-agenda-category-icons")
  (org-agenda-category-icons!

   :material
   (repeat Recurring)
   (group Meeting)
   (cake Birthday)
   (cake Anniversary)
   (event Event)
   (web Website)
   (people Meeting)


   :octicon
   (tools Habit)

   :faicon
   (cogs Config)
   (book Lexicon)
   (check-circle Task)
   (code Odin)
   (list-ul Project)
   (list-ul Proj)
   (phone Phone)
   (car Vehicle)
   (plus-square Health)
   (pencil-square-o Note)
   (exclamation-circle Reminder)
   (flag Holiday)
   (info Note)
   (star Goals)
   (star Goal)
   (gift Reward)
   (folder-open Refile)
   (map-marker Review)
   (archive Archive))


#+END_SRC

#+RESULTS:



#+RESULTS:

*** Language Setup
:PROPERTIES:
:ID:       4306efee-4192-42a5-80ed-edd9e74ed204
:END:
#+begin_src emacs-lisp
  (org-babel-do-load-languages
   'org-babel-load-languages
   '((emacs-lisp .t )
     (js .t)
     (shell . t)
     (dot . t)
     (gnuplot . t)
     (plantuml . t)
     (python .t)))

#+end_src

#+RESULTS:


*** Auto-Tangle Config Files
:PROPERTIES:
:ID:       49d55645-40af-4e64-b6bf-f584927e67f4
:END:
#+begin_src emacs-lisp
  ;;auto tangle my emacs config file
  (defun my/mirror-tangled-files-to-dotfiles (&rest _)
    "Copy the tangled early-init.el/init.el into the dotfiles repo's
   configbackups folder, since ~/.emacs.d isn't itself git-tracked.
   Fires after ANY org-babel-tangle call, not just save-triggered ones."
    (dolist (fname '("init.el" "early-init.el"))
      (let ((src (expand-file-name fname "~/.emacs.d")))
        (when (file-exists-p src)
          (copy-file src
                     (expand-file-name (concat "configbackups/" fname) "~/dotfiles")
                     t)))))
  (advice-add 'org-babel-tangle :after #'my/mirror-tangled-files-to-dotfiles)

  (defun my/org-babel-tangle-config()
    (when (string-equal (buffer-file-name)
                        (expand-file-name "~/dotfiles/configbackups/main-config.org"))
      ;; dynamic scoping
      (let ((org-confirm-babel-evaluate nil))
        (org-babel-tangle))))
  (add-hook 'org-mode-hook (lambda () (add-hook 'after-save-hook #'my/org-babel-tangle-config)))
  (message "Tangling enabled")

  ;; ;;auto tangle my emacs config file
  ;; (defun my/mirror-tangled-files-to-dotfiles ()
  ;;   "Copy the tangled early-init.el/init.el into the dotfiles repo's
  ;; configbackups folder, since ~/.emacs.d isn't itself git-tracked.
  ;; Historically a manual copy; now happens automatically on every tangle."
  ;;   (dolist (fname '("init.el" "early-init.el"))
  ;;     (let ((src (expand-file-name fname "~/.emacs.d")))
  ;;       (when (file-exists-p src)
  ;;         (copy-file src
  ;;                    (expand-file-name (concat "configbackups/" fname) "~/dotfiles")
  ;;                    t))))) ;; t = overwrite without confirmation

  ;; (defun my/org-babel-tangle-config()
  ;;   (when (string-equal (buffer-file-name)
  ;;                       (expand-file-name "~/dotfiles/configbackups/main-config.org"))
  ;;     ;; dynamic scoping
  ;;     (let ((org-confirm-babel-evaluate nil))
  ;;       (org-babel-tangle)
  ;;       (my/mirror-tangled-files-to-dotfiles))))
  ;; (add-hook 'org-mode-hook (lambda () (add-hook 'after-save-hook #'my/org-babel-tangle-config)))
  ;; (message "Tangling enabled")
#+end_src

#+RESULTS:
: Tangling enabled

  ;;auto tangle my emacs config file
  (defun my/org-babel-tangle-config()
    (when (string-equal (buffer-file-name)
                        (expand-file-name "~/dotfiles/configbackups/main-config.org"))
  					; might need to condituionally use this:
                                          ;(expand-file-name"c:/Users/Tyler/emacsone/OrgFiles/emacsconf.org"))
      ;; dynamic scoping
      (let ((org-confirm-babel-evaluate nil))
        (org-babel-tangle))))
  (add-hook 'org-mode-hook (lambda () (add-hook 'after-save-hook #'my/org-babel-tangle-config)))
  (message "Tangling enabled")
#+end_src

#+RESULTS:
| (lambda nil (add-hook 'after-save-hook #'my/org-babel-tangle-config)) | #[0 \300\301\302\303\304$\207 [add-hook change-major-mode-hook org-show-all append local] 5] | #[0 \300\301\302\303\304$\207 [add-hook change-major-mode-hook org-babel-show-result-all append local] 5] | org-babel-result-hide-spec | org-babel-hide-all-hashes |

*** Visual Fill Mode
:PROPERTIES:
:ID:       0873020a-7c6e-4d36-8d59-65daa46c8b6a
:END:
#+begin_src emacs-lisp
  ;; (defun org-mode-visual-fill()
  ;;   (setq visual-fill-column-width 110 visual-fill-column-center-text t)
  ;;   (visual-fill-column-mode 1))


  ;; (use-package visual-fill-column
  ;;   :hook (org-mode . org-mode-visual-fill))


  (use-package olivetti
    :hook ((org-mode . olivetti-mode)
  	 (elfeed-show-mode . olivetti-mode))
    :custom
    (olivetti-body-width 140))


#+end_src

#+RESULTS:

*** Pomodoro
:PROPERTIES:
:APPT_WARNTIME: 1
:ID:       b04c4878-7920-437b-84ef-4bd9cef80d90
:END:
#+BEGIN_SRC emacs-lisp 
  (setq org-clock-sound "~/Orgfiles/Kids_cheering.wav")
#+END_SRC

#+RESULTS:
: ~/Orgfiles/Kids_cheering.wav

#+RESULTS:
: org-agenda-remove-restriction-lock
** My Org Functions
:PROPERTIES:
:ID:       f5abd1af-30c8-4465-9f40-831e9ace99fb
:END:
#+BEGIN_SRC emacs-lisp

  (defun my/agenda-add-note-heading ()
    "Add a child NOTE heading to the org entry at point in the agenda."
    (interactive)
    (let ((marker (or (org-get-at-bol 'org-marker)
                      (org-get-at-bol 'org-hd-marker))))
      (unless marker
        (user-error "No org entry at point"))
      (let ((note-text (read-string "Note heading: "))
            note-pos)
        (org-with-point-at marker
          (org-back-to-heading t)
          (let ((level (1+ (org-current-level))))
            (org-end-of-subtree t t)
            (setq note-pos (point-marker))
            (insert (make-string level ?*) " " note-text " :NOTE:\n"
                    ":PROPERTIES:\n:CATEGORY: Note\n:END:\n"
                    (format-time-string "[%Y-%m-%d %a]\n\n"))
            (save-buffer)))
        (setq my/last-agenda-note-marker note-pos))))

  (defun my/agenda-jump-to-last-note ()
    "Jump to the last note heading added via my/agenda-add-note-heading."
    (interactive)
    (unless (and (boundp 'my/last-agenda-note-marker)
                 my/last-agenda-note-marker)
      (user-error "No note added yet this session"))
    (switch-to-buffer (marker-buffer my/last-agenda-note-marker))
    (goto-char my/last-agenda-note-marker)
    (org-show-context 'default)
    (forward-line 5))


  ;; org-agenda note keybinds
  (define-key org-agenda-mode-map (kbd "z") #'my/agenda-add-note-heading)
  (define-key org-agenda-mode-map (kbd "Z") #'my/agenda-jump-to-last-note)
  (define-key org-agenda-mode-map (kbd "C-c C-z") #'my/agenda-add-note-heading)
  (define-key org-agenda-mode-map (kbd "C-c C-Z") #'my/agenda-jump-to-last-note)
  (define-key org-agenda-mode-map (kbd "H-z") #'my/agenda-jump-to-last-note)


#+END_SRC

#+RESULTS:
: my/agenda-jump-to-last-note

** Section Loaded Message
:PROPERTIES:
:ID:       209dd53b-ac06-4d13-8d9a-11619d1ca194
:END:
#+BEGIN_SRC emacs-lisp
  (message "Org Config Setup Section Loaded")
#+END_SRC
* Org-Roam Setup
:PROPERTIES:
:ID:       dfce8654-f011-4183-a2e2-3147938f8285
:END:
#+BEGIN_SRC emacs-lisp


  (use-package org-roam
    :ensure t
    :init
    (setq org-roam-v2-act t)
    (setq org-roam-file-exclude-regexp
        	'("\\.stversions/"
            "-conflict-"
            "~[0-9]\\{8\\}-[0-9]\\{6\\}\\.org$"
            "~$"
            "\\.git/"))
    (setq consult-org-roam-grep-func #'consult-ripgrep)
    (setq org-roam-node-display-template "${title:*} ${tags:60}")
    :custom
    (org-roam-directory "~/RoamNotes")
    (org-roam-completion-everywhere t)
    ( org-agenda-todo-list-sublevels nil)        

    :bind
    (("C-c n l" . org-roam-buffer-toggle)
     ("C-c n f" . org-roam-node-find)
     ("C-c n i" . org-roam-node-insert)
     ("C-c n c" . org-id-get-create)
     ("C-c n a" . org-roam-alias-add)
     ("C-c n t" . org-roam-tag-add)
     ("C-c n r" . org-roam-ref-add)
     ("C-c n s" . consult-org-roam-search)
     ("C-c n x a" . org-roam-alias-remove)
     ("C-c n x r" . org-roam-ref-remove)
     ("C-c n x t" . org-roam-tag-remove)
     ("C-c n F g" . my/GAME-find)
     ("C-c n F f" . my/find-fix)
     ("C-c n F m" . my/MOC-find)
     ("C-c n I" . org-roam-node-insert-immediate)
     ("C-c n C-o" . citar-open-notes)
     ("C-c n C-i" . citar-insert-citation)
     ("H-i" . org-roam-node-insert-immediate)
     ("H-]" . 'consult-org-roam-forward-links)
     ("H-[" . 'consult-org-roam-backlinks)

     :map org-mode-map
     ("C-c n b" . org-mark-ring-goto)

     :map org-roam-dailies-map
     ("Y" . org-roam-dailies-capture-yesterday)
     ("T" . org-roam-dailies-capture-tomorrow))

    :bind-keymap
    ("C-c n d" . org-roam-dailies-map)
    :config
    (require 'org-roam-dailies)
    (org-roam-db-autosync-mode))

  (defun case-insensitive-org-roam-node-read (orig-fn &rest args)
    (let ((completion-ignore-case t))
      (apply orig-fn args)))

  (advice-add 'org-roam-node-read :around #'case-insensitive-org-roam-node-read)


  ;; for org-roam-buffer-toggle
  ;; Recommendation in the official manual
  (add-to-list 'display-buffer-alist
               '("\\*org-roam\\*"
                 (display-buffer-in-direction)
                 (direction . right)
                 (window-width . 0.33)
                 (window-height . fit-window-to-buffer)))



  ;;  Bind this to C-c n In
  (defun org-roam-node-insert-immediate (arg &rest args)
    (interactive "P")
    (let ((args (cons arg args))
          (org-roam-capture-templates (list (append (car org-roam-capture-templates)
                                                    '(:immediate-finish t)))))
      (apply #'org-roam-node-insert args)))  


  (defun my/find-MOC-only(node)
    "find Map of Content (MOC) files only"
    (let ((tags (org-roam-node-tags node)))
      (member "MOC" tags)))


  (defun my/MOC-find ()
    "Find moc files"
    (interactive)
    (org-roam-node-find t nil 'my/find-MOC-only))

  (defun my/find-fix()
    "Find troubleshooting and fixes for common problems"
    (interactive)
    (org-roam-node-find t nil 'my/find-FIX-only))

  (defun my/find-FIX-only(node)
    "Find entries tagged as troubleshooting"
    (let ((tags(org-roam-node-tags node)))
      (member "troubleshooting" tags)))

  (defun my/find-games-only(node)
    "Find entries tagged as games"
    (let ((tags (org-roam-node-tags node)))
      (member "Game" tags)))

  (defun my/GAME-find()
    "Find game files"
    (interactive)
    (org-roam-node-find t nil 'my/find-games-only))


  (with-eval-after-load "org-roam"
    (setq org-roam-capture-templates
          '(("d" "default" plain
             "%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                "#+title: ${title}\n#+date: %U\n")
             :unnarrowed t)
    	  
  	  ("t" "troubleshooting" plain
  	   "* Symptom\n%?\n\n* Root Cause\n\n\n* Ruled Out\n\n\n* Fix                                                              :UNCONFIRMED:\n# Tags: VERIFIED | PARTIAL | WORKAROUND | UNCONFIRMED\n\n\n* Related\n\n"
  	   :target (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                "#+title: ${title}\n#+date: %<[%Y-%m-%d %a %H:%M]>\n#+filetags: :troubleshooting:\n")
  	   :unnarrowed t)
      	  
            ("g" "Games")

            ("ga" "Add Game" plain
             "\n* Details\n\nGame Name: ${title}\nDesigner: %^{Designer}\nYear: %^{Year}\nPublisher: %^{Publisher}\nBGG Link: %^{BGG Link}\n\n* Description\n\n* First Impression\n\n%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                "#+title:${title}\n#+filetags: :Game:\n#+date: %U\n")
             :unnarrowed t)

            ("r" "Recipe" plain
             "\n* General Info\n\nSource: %^{Source}\nPage #: %^{Page}\n\n* Nutrition\n<macros%?\n\n* Recipe\n** Ingredients\n-\n\n** Instructions\n"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                "#+title:${title}\n#+filetags: :Recipe:\n#+date: %U\n"))

            ("b" "Book Notes" plain
             "\n* Source\n\nAuthor: %^{Author}\nTitle: ${title}\nYear: %^{Year}\n\n* Summary\n\n%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                "#+title: ${title}\n#+date: %U\n")
             :unnarrowed t)

            ("p" "Person")

            ("pf" "Family" plain
             "\n* Relationship\n- Family — %^{Relation, e.g. Mom/Dad/Brother}\n\n* Interactions / Notes\n-\n\n%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                ":PROPERTIES:\n:EMAIL: %^{Email}\n:PHONE: %^{Phone}\n:BIRTHDAY: %^{Birthday}\n:END:\n#+title: ${title}\n#+date: %U\n#+filetags: :family:contact:\n")
             :unnarrowed t)

            ("pp" "Personal Friend" plain
             "\n* Relationship\n- %^{How you know them / category note}\n\n* Interactions / Notes\n-\n\n%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                ":PROPERTIES:\n:EMAIL: %^{Email}\n:PHONE: %^{Phone}\n:END:\n#+title: ${title}\n#+date: %U\n#+filetags: :friend:contact:\n")
             :unnarrowed t)

            ("pw" "SEAS Coworker" plain
             "\n* Relationship\n- SEAS internal — %^{Role/context}\n\n* Interactions / Notes\n-\n\n%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                ":PROPERTIES:\n:EMAIL: %^{Email}\n:PHONE: %^{Phone}\n:ROLE: %^{Role}\n:COMPANY: SEAS Productions\n:END:\n#+title: ${title}\n#+date: %U\n#+filetags: :seas:coworker:contact:\n")
             :unnarrowed t)

            ("pc" "SEAS Client" plain
             "\n* Relationship\n- Client-side contact for %^{Show/Project}\n\n* Interactions / Notes\n-\n\n* Shows / Projects\n-\n\n%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                ":PROPERTIES:\n:EMAIL: %^{Email}\n:PHONE: %^{Phone}\n:ROLE: %^{Role}\n:COMPANY: %^{Company}\n:END:\n#+title: ${title}\n#+date: %U\n#+filetags: :seas:client:contact:\n")
             :unnarrowed t)

            ("pv" "SEAS Vendor" plain
             "\n* Relationship\n- Vendor/freelancer, engaged per-project — %^{Specialty, e.g. CMP}\n\n* Interactions / Notes\n-\n\n* Shows / Projects\n-\n\n%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                ":PROPERTIES:\n:EMAIL: %^{Email}\n:PHONE: %^{Phone}\n:ROLE: %^{Role}\n:COMPANY: %^{Company}\n:END:\n#+title: ${title}\n#+date: %U\n#+filetags: :seas:vendor:contact:\n")
             :unnarrowed t)

            ("pd" "Designer / Reference Figure" plain
             "\n* Game Designs\n-\n\n%?"
             :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                                ":PROPERTIES:\n:ROAM_REFS: %^{BGG or reference URL}\n:END:\n#+title: ${title}\n#+date: %U\n#+filetags: :Designer:reference:\n")
             :unnarrowed t))))

  ;; (setq org-roam-capture-templates
  ;;       '(("d" "default" plain
  ;;          "%?"
  ;;          :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org" "#+title: ${title}\n#+date: %U\n")
  ;;          :unnarrowed t)
  ;;         ("g" "Games")
  ;;         ("ga" "Add Game" plain
  ;;          "\n* Details\n\nGame Name: ${title}\nDesigner: %^{Designer}\nYear: %^{Year}\nPublisher: %^{Publisher}\nBGG Link: %^{BGG Link}\n\n* Description\n\n* First Impression\n\n%?"
  ;;          :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org" "#+title:${title}\n#+filetags: :Game:\n#+date: %U\n")
  ;;          :unnarrowed t)
  ;;         ("r" "Recipe" plain
  ;;          "\n* General Info\n\nSource: %^{Source}\nPage #: %^{Page}\n\n* Nutrition\n<macros%?\n\n* Recipe\n** Ingredients\n-\n\n** Instructions\n"
  ;; 	   :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org" "#+title:${title}\n#+filetags: :Recipe:\n#+date: %U\n"               ))
  ;;         ("b" "Book Notes" plain
  ;;          "\n* Source\n\nAuthor: %^{Author}\nTitle: ${title}\nYear: %^{Year}\n\n* Summary\n\n%?"
  ;;          :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org" "#+title: ${title}\n#+date: %U\n")
  ;;          :unnarrowed t))))


  (setq org-roam-mode-sections
        (list #'org-roam-backlinks-section
              #'org-roam-reflinks-section
              #'org-roam-unlinked-references-section
              ))


  (use-package org-roam-ui
    :bind ("C-H-4" . org-roam-ui-open)
    :config
    (setq org-roam-ui-sync-theme t
          org-roam-ui-follow t
          org-roam-ui-update-on-save t
          org-roam-ui-open-on-start t))


#+END_SRC

#+RESULTS:
: org-roam-ui-open
** Citar and citar-org-roam Setup
:PROPERTIES:
:ID:       b37c36dc-d6c2-4018-afb0-f034f84f4128
:END:
#+BEGIN_SRC emacs-lisp
  ;; machine: any (shared config)
  (setq org-cite-insert-processor 'citar
        org-cite-follow-processor 'citar
        org-cite-activate-processor 'citar)

    ;; machine: any (shared config)
    (use-package citar
      :ensure t  ;; or :ensure t, depending on your package manager
      :custom
      (citar-bibliography '("~/Orgfiles/bibliography/library.bib"))
      (org-cite-global-bibliography '("~/Orgfiles/bibliography/library.bib")))

    (use-package citar-org-roam
      :ensure t
      :after (citar org-roam)
      :config
      (citar-org-roam-mode))
#+END_SRC

#+RESULTS:
: t

** Section Loaded Message
:PROPERTIES:
:ID:       51b97ae6-431c-4eeb-873e-cf9c02f89552
:END:
#+BEGIN_SRC emacs-lisp
  (message "Org-Roam Section Loaded")
#+END_SRC
* Org-Remark
:PROPERTIES:
:ID:       5b46c563-5cf5-4fd7-a89e-43fd2de17548
:END:
#+BEGIN_SRC emacs-lisp

  (use-package org-remark)
  (org-remark-global-tracking-mode 1)
  (define-key global-map (kbd "C-c r m") #'org-remark-mark)

  (with-eval-after-load 'org-remark
    (define-key org-remark-mode-map (kbd "C-c r o") #'org-remark-open) 
    (define-key org-remark-mode-map (kbd "C-c r b m") #'org-remark-mark-yellow) 
    (define-key org-remark-mode-map (kbd "C-c r b o") #'org-remark-mark-orange-bg) 
    (define-key org-remark-mode-map (kbd "C-c r b b") #'org-remark-mark-blue-bg) 
    (define-key org-remark-mode-map (kbd "C-c r b g") #'org-remark-mark-grey-bg) 
    (define-key org-remark-mode-map (kbd "C-c r b l b") #'org-remark-mark-light-blue-bg) 
    (define-key org-remark-mode-map (kbd "C-c r f o") #'org-remark-mark-orange-fg) 
    (define-key org-remark-mode-map (kbd "C-c r f c") #'org-remark-mark-cyan-fg) 
    (define-key org-remark-mode-map (kbd "C-c r f b") #'org-remark-mark-blue-fg) 
    (define-key org-remark-mode-map (kbd "C-c r f g") #'org-remark-mark-grey-fg) 
    (define-key org-remark-mode-map (kbd "C-c r f p") #'org-remark-mark-pink-fg)
    (define-key org-remark-mode-map (kbd "C-c r t") #'org-remark-mark-typo)
    (define-key org-remark-mode-map (kbd "C-c r c") #'org-remark-change)
    
    (define-key org-remark-mode-map (kbd "C-c r ]") #'org-remark-view-next) 
    (define-key org-remark-mode-map (kbd "C-c r [") #'org-remark-view-prev) 
    (define-key org-remark-mode-map (kbd "C-c r d") #'org-remark-remove))


  (org-remark-create "typo"
                     '(:underline (:color "#8f0075" :style wave))
                     '(help-echo "Fix the typo"))
  (org-remark-create "grey-bg"
                     '(doom-modeline-battery-normal))
  (org-remark-create "orange-bg-bold"
                     '(isearch))
  (org-remark-create "orange-bg"
                     '(:background "chocolate" :foreground "cornsilk"))
  (org-remark-create "blue-fg"
                     '(gnus-group-mail-2))
  (org-remark-create "orange-fg"
                     '(alert-high-face))
  (org-remark-create "cyan-fg"
                     '(:foreground "turquoise"))
  (org-remark-create "grey-fg"
                     '(file-name-shadow))
  (org-remark-create "pink-fg"
                     '(gnus-group-news-4))
  (org-remark-create "blue-bg"
                     '(smerge-refined-changed))
  (org-remark-create "light-blue-bg"
                     '(avy-goto-char-timer-face))

      (which-key-add-keymap-based-replacements org-remark-mode-map
      "C-c r b" "background (highlight)"
      "C-c r f" "foreground (text color)")


    (with-eval-after-load 'org-remark
    (setopt org-remark-notes-auto-delete :auto-delete))

#+END_SRC

#+RESULTS:
: :auto-delete

** Section Loaded Message
:PROPERTIES:
:ID:       495eb7e5-77b1-4d99-8ad8-1708116055df
:END:
#+BEGIN_SRC emacs-lisp
  (message "Org-remark Section Loaded")
#+END_SRC
* Org-Translcude
:PROPERTIES:
:ID:       4e283b37-92d8-4aba-ab47-17db8a752b43
:END:
#+BEGIN_SRC emacs-lisp
  (use-package org-transclusion)
  (add-hook 'org-mode-hook #'org-transclusion-mode)
  (global-set-key (kbd "s-t") 'org-transclusion-mode)

  (fset 'my/org-transclusion-make
        (kmacro-lambda-form [?\C-  ?\C-c ?\C-n ?\M-w ?\C-p ?\C-y ?\C-c ?\C-p ?\C-c ?\C-p ?\M-x ?o ?r ?g ?- ?t ?r ?a ?n ?s ?c ?l ?u ?s ?i ?o ?n ?- ?d ?e ?a tab return ?\C-p ?\C-k] 0 "%d"))



  (defun my/transclusion-refresh ()
    "Toggle `org-transclusion-mode' off/on - I can embed this into yas-snippets  using backticks around teh function call and render the transclusion. Sick..."
    (when (bound-and-true-p org-transclusion-mode)
      (run-at-time 0 nil
                   (lambda ()
                     (org-transclusion-mode 0)
                     (org-transclusion-mode 1)))))



#+END_SRC

#+RESULTS:
: #[256 "\301\242\302\305\300\306#\210\301\240*\207" [[67108896 3 14 134217847 16 25 3 16 3 16 134217848 111 114 103 45 116 114 97 110 115 99 108 117 115 105 111 110 45 100 101 97 tab return 16 11] (0) "%d" kmacro-counter-format-start kmacro-counter execute-kbd-macro kmacro-loop-setup-function] 5 kmacro]


#+END_SRC

#+RESULTS:
: #[256 "\301\242\302\305\300\306#\210\301\240*\207" [[67108896 3 14 134217847 16 25 3 16 3 16 134217848 111 114 103 45 116 114 97 110 115 99 108 117 115 105 111 110 45 100 101 97 tab return 16 11] (0) "%d" kmacro-counter-format-start kmacro-counter execute-kbd-macro kmacro-loop-setup-function] 5 kmacro]

** Section Loaded Message
:PROPERTIES:
:ID:       001676c1-dfda-4b44-bc5a-e1be75daf6c6
:END:
#+BEGIN_SRC emacs-lisp
  (message "Org-Transclude Loaded")
#+END_SRC
* Org-Present
:PROPERTIES:
:ID:       3f5ca25c-ecbc-4bf8-a38a-be2fd0a7cb69
:END:
#+BEGIN_SRC emacs-lisp
  (use-package org-present)
  (eval-after-load "org-present"
    '(progn
       (add-hook 'org-present-mode-hook
                 (lambda ()
                   (org-present-big)
                   (org-display-inline-images)
                   (hl-line-mode)
                   ;; (org-present-hide-cursor)
                   ;; (org-present-read-only)
                   ))

       (add-hook 'org-present-mode-quit-hook
                 (lambda ()
                   (org-present-small)
                   (org-remove-inline-images)
                   (hl-line-mode)
                   ;; (org-present-show-cursor)
                   ;; (org-present-read-write)
                   ))))


  (message "Org-present Loaded")
#+END_SRC

#+RESULTS:
| lambda | nil | (org-present-small) | (org-remove-inline-images) | (hl-line-mode)            |                          |
| lambda | nil | (org-present-small) | (org-remove-inline-images) | (hl-line-mode)            | (org-present-read-write) |
| lambda | nil | (org-present-small) | (org-remove-inline-images) | (org-present-read-write)  |                          |
| lambda | nil | (org-present-small) | (org-remove-inline-images) | (org-present-show-cursor) | (org-present-read-write) |

* Org-ql
:PROPERTIES:
:ID:       e3609623-8b92-4762-bb34-0f7ae237cea9
:END:
#+BEGIN_SRC emacs-lisp
  (setq org-directory "~/Orgfiles")
  (global-set-key (kbd "C-H-M-f") 'org-ql-find)
  (global-set-key (kbd "C-H-M-s") 'org-ql-search)
  (global-set-key (kbd "C-H-M-v") 'org-ql-view)

  ;; example of an org-ql-search
                                          ;  (org-ql-search (org-agenda-files)
                                          ;    '(and
                                          ;      (todo "TODO")
                                          ;      (tags "GAMEDESIGN")))

  (message "org-ql loaded")
#+END_SRC

#+RESULTS:
: 1

* ORG Export
** SEAS HTML Export
:PROPERTIES:
:ID:       8f601aae-1a92-4ba5-b623-ff32deda19de
:END:
#+begin_src emacs-lisp
  (with-eval-after-load 'ox-html

    (setq org-html-htmlize-output-type 'css
          org-html-head-include-default-style nil
          org-html-head-include-scripts nil
          org-html-postamble nil
          org-html-preamble nil
          org-html-checkbox-type 'html
          org-html-head
          (concat "<style>\n"
                  (with-temp-buffer
                    (insert-file-contents "~/Projects/seas-design-system.css")
                    (buffer-string))
                  "\n</style>"
                  "<script>
    document.addEventListener('DOMContentLoaded', function() {
      document.querySelectorAll('input[type=\"checkbox\"]').forEach(function(cb) {
        cb.removeAttribute('disabled');
      });
    });
    </script>"))

    (setq org-publish-project-alist
          '(("seas-project-html"
             :base-directory "~/Projects/seas-some-project/"
             :publishing-directory "~/Projects/seas-some-project/output/"
             :publishing-function org-html-publish-to-html
             :html-head "<link rel=\"stylesheet\" href=\"seas-design-system.css\">"
             :html-head-include-default-style nil
             :recursive t)
            ("seas-project-assets"
             :base-directory "~/Projects/seas-some-project/"
             :publishing-directory "~/Projects/seas-some-project/output/"
             :publishing-function org-publish-attachment
             :base-extension "css\\|png\\|jpg\\|svg")
            ("seas-project" :components ("seas-project-html" "seas-project-assets")))))


  ;; css reloader
  (defun my/reload-css ()
    "Reload seas-design-system.css into org-html-head without restarting Emacs."
    (interactive)
    (setq org-html-head
          (concat "<style>\n"
                  (with-temp-buffer
                    (insert-file-contents "~/Projects/seas-design-system.css")
                    (buffer-string))
                  "\n</style>"
                  "<script>
  document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('input[type=\"checkbox\"]').forEach(function(cb) {
      cb.removeAttribute('disabled');
    });
  });
  </script>"))
    (message "SEAS CSS reloaded."))

#+end_src

#+RESULTS:
: my/reload-css

* CALFW
:PROPERTIES:
:ID:       d315bdcb-e930-429e-833a-397fc768c3b6
:END:
#+BEGIN_SRC emacs-lisp

  (require 'cl-lib)
  (defalias 'loop 'cl-loop)
  (defalias 'return 'cl-return)


  (require 'calfw)
  (setq calfw-render-line-breaker 'calfw-render-line-breaker-none)
  (with-eval-after-load 'calfw
    (require 'calfw-org)
    (require 'calfw-cal)
    (require 'calfw-gcal))

  ;; Remove holidays for calfw


  (setq holiday-hebrew-holidays nil)     ;; Rosh HaShanah, Yom Kippur, Sukkot, etc.
  (setq holiday-islamic-holidays nil)    ;; Ramadan, Eid, etc.
  (setq holiday-christian-holidays nil)  ;; Easter, Ash Wednesday, etc.
  (setq holiday-bahai-holidays nil)      ;; Bahá'í holidays
  (setq holiday-oriental-holidays nil)   ;; Chinese New Year, etc.
  (setq holiday-solar-holidays nil)      ;; Equinoxes/solstices, if you don't want those either

  ;; Keep holiday-general-holidays (Labor Day, New Year's, etc.)
  ;; and holiday-local-holidays / holiday-other-holidays if you want to add your own.

  ;; Recompute calendar-holidays from the (now trimmed) category variables.
  ;; This mirrors what calendar.el does internally in calendar-holiday-list setup.
  (setq calendar-holidays
        (append holiday-general-holidays
                holiday-local-holidays
                holiday-other-holidays
                holiday-christian-holidays
                holiday-hebrew-holidays
                holiday-islamic-holidays
                holiday-bahai-holidays
                holiday-oriental-holidays
                holiday-solar-holidays))



  (with-eval-after-load 'calfw
    (ignore-errors
      ;; Title and headers
      (set-face-attribute 'calfw-title-face nil
                          :foreground "#88C0D0" :weight 'bold)
      (set-face-attribute 'calfw-header-face nil
                          :foreground nord-green :background nord0 :weight 'bold)

      ;; Grid
      (set-face-attribute 'calfw-grid-face nil
                          :foreground "#81A1C1")

      ;; Weekend headers
      (set-face-attribute 'calfw-sunday-face nil
    			:foreground nord-orange :background nord0 :weight 'bold)
      
      (set-face-attribute 'calfw-saturday-face nil
                          :foreground nord-orange :background nord0 :weight 'bold)

      
      ;; Day cells
      (set-face-attribute 'calfw-day-title-face nil
                          :background nord0)
      (set-face-attribute 'calfw-default-day-face nil
                          :background nord0 :weight 'bold)

      ;; Today
      (set-face-attribute 'calfw-today-title-face nil
                          :background "#3B4252" :weight 'bold)
      (set-face-attribute 'calfw-today-face nil
                          :foreground "#88C0D0" :weight 'bold)

      ;; Content and periods
      (set-face-attribute 'calfw-default-content-face nil
                          :foreground "#D8DEE9")
      (set-face-attribute 'calfw-periods-face nil
                          :foreground "#A3BE8C")

      ;; Holidays and annotations
      (set-face-attribute 'calfw-holiday-face nil
                          :foreground nord-red :background nord0 :weight 'bold)
      (set-face-attribute 'calfw-annotation-face nil
                          :foreground "#EBCB8B")

      ;; Disabled/out of range days
      (set-face-attribute 'calfw-disable-face nil
                          :foreground "#4C566A")

      ;; Hidden calendars
      (set-face-attribute 'calfw-calendar-hidden-face nil
                          :foreground "#4C566A" :strike-through t)

      ;; Toolbar
      (set-face-attribute 'calfw-toolbar-face nil
                          :foreground "#3B4252" :background "#3B4252")
      (set-face-attribute 'calfw-toolbar-button-off-face nil
                          :foreground "#616E88" :background "#3B4252")
      (set-face-attribute 'calfw-toolbar-button-on-face nil
                          :foreground "#88C0D0" :background "#3B4252" :weight 'bold)))

  (defun my/calfw-sources ()
    "Org sources shared across every calfw view (single or quarterly)."
    (list
     (calfw-org-create-file-source "SEAS" "~/Orgfiles/SEAS.org" "#a0a0a0")
     (calfw-org-create-file-source "PERSONAL" "~/Orgfiles/tasks.org" "Orange")
     (calfw-org-create-file-source "Moving" "~/Orgfiles/Moving.org" nord-red)))

  (global-set-key (kbd "<f17>") 'my/open-calendar)
  (define-key calfw-calendar-mode-map (kbd "s") 'org-save-all-org-buffers)

  (message "calfw loaded")
#+END_SRC

#+RESULTS:
: calfw loaded

** Calfw Single View
:PROPERTIES:
:ID:       8bb050d2-d2a2-4f6b-9b30-633fd433adf8
:END:
#+BEGIN_SRC emacs-lisp
(defun my/open-calendar ()
      (interactive)
      (calfw-open-calendar-buffer
       :contents-sources (my/calfw-sources)))
#+END_SRC

#+RESULTS:
: my/open-calendar

** Calfw Quarterly View
:PROPERTIES:
:ID:       2d755815-4edc-416a-8cbd-db66018445a8
:END:
#+BEGIN_SRC emacs-lisp
  (with-eval-after-load 'calfw
    (require 'calfw-org)

    (defun my/cfw-month-offset (month year n)
      "Return a cfw:date for MONTH/YEAR advanced by N months.
     Day is fixed at 1 to avoid end-of-month rollover surprises."
      (let ((total (+ (1- month) n)))
        (calfw-date (1+ (mod total 12)) 1 (+ year (floor total 12)))))


    (defun my/open-quarterly-calendar-view ()
      "Open a 3-panel quarterly calfw view in the current frame.

     Layout:
       - Left:        current month (wide panel)
       - Top-right:   current month + 1
       - Bottom-right current month + 2

     Each panel is an independent cfw: buffer, so navigating one
     does not move the others."
      (interactive)
      (delete-other-windows)
      (let* ((today (calendar-current-date))
             (month (nth 0 today))
             (year (nth 2 today))
             (sources (my/calfw-sources))
             (left-buf (get-buffer-create "*cal-month-0*"))
             (tr-buf (get-buffer-create "*cal-month-1*"))
             (br-buf (get-buffer-create "*cal-month-2*"))
             (right-window (split-window-right)))
        ;; Left panel: current month, in the originally selected window
        (calfw-open-calendar-buffer
         :buffer left-buf
         :date today
         :contents-sources sources)
        ;; Right column, split top/bottom
        (select-window right-window)
        (let ((bottom-window (split-window-below)))
          (calfw-open-calendar-buffer
           :buffer tr-buf
           :date (my/cfw-month-offset month year 1)
           :contents-sources sources)
          (select-window bottom-window)
          (calfw-open-calendar-buffer
           :buffer br-buf
           :date (my/cfw-month-offset month year 2)
           :contents-sources sources))
        ;; leave focus on the current-month panel
        (select-window (get-buffer-window left-buf))))

    ;; (global-set-key (kbd "C-H-M-q") 'my/open-quarterly-calendar-view)

    (message "calfw-quarterly-view loaded"))

(defun my/find-frame-on-monitor (monitor-name)
  "Return a live, non-minibuffer-only frame currently displayed on MONITOR-NAME, or nil."
  (seq-find (lambda (f)
              (and (not (eq (frame-parameter f 'minibuffer) 'only))
                   (let ((attrs (frame-monitor-attributes f)))
                     (and attrs (string= (cdr (assq 'name attrs)) monitor-name)))))
            (frame-list)))


(defun my/get-or-create-maximized-frame-on-slot (slot)
  "Return a maximized frame on SLOT's monitor, reusing one if it exists."
  (let* ((machine (system-name))
         (slots (cdr (assoc machine my/monitor-slots-alist)))
         (monitor (cdr (assoc slot slots)))
         (existing (my/find-frame-on-monitor monitor)))
    (if existing
        existing
      (let* ((monitors (display-monitor-attributes-list))
             (target (seq-find (lambda (m) (string= (cdr (assq 'name m)) monitor))
                                monitors))
             (workarea (cdr (assq 'workarea target)))
             (frame (make-frame
                     `((left . (+ ,(nth 0 workarea)))
                       (top . (+ ,(nth 1 workarea)))
                       (fullscreen . maximized)
                       (alpha . (60 . 45))))))
        (my/apply-theme-and-faces frame)
        frame))))

(defun my/open-quarterly-calendar-dashboard ()
  "Open the quarterly calendar + agenda dashboard across three monitors."
  (interactive)
  (let* ((today (calendar-current-date))
         (month (nth 0 today))
         (year (nth 2 today))
         (sources (my/calfw-sources))
         (top-frame (my/get-or-create-maximized-frame-on-slot 3))    ; top Dell
         (bottom-frame (my/get-or-create-maximized-frame-on-slot 1)) ; bottom Dell
         (side-frame (my/get-or-create-maximized-frame-on-slot 2))   ; ASUS vertical
         (org-agenda-window-setup 'current-window))

    ;; --- Top ultrawide: scratch | cal-month-0 ---
    (with-selected-frame side-frame
      (delete-other-windows)
      (switch-to-buffer "*scratch*")
      (select-window (split-window-right))
      (calfw-open-calendar-buffer
       :buffer (get-buffer-create "*cal-month-0*")
       :date today
       :contents-sources sources))

    ;; --- Bottom ultrawide: cal-month-1 | cal-month-2 ---
    (with-selected-frame bottom-frame
      (delete-other-windows)
      (calfw-open-calendar-buffer
       :buffer (get-buffer-create "*cal-month-1*")
       :date (my/cfw-month-offset month year 1)
       :contents-sources sources)
      (select-window (split-window-right))
      (calfw-open-calendar-buffer
       :buffer (get-buffer-create "*cal-month-2*")
       :date (my/cfw-month-offset month year 2)
       :contents-sources sources))

    ;; --- ASUS vertical: Personal (top) / SEAS (bottom) agendas ---
    (with-selected-frame top-frame
      (delete-other-windows)
      (org-agenda nil "P")
      (select-window (split-window-below))
      (org-agenda nil "O"))

    (select-frame-set-input-focus top-frame)))

;; (global-set-key (kbd "C-H-M-q") 'my/open-quarterly-calendar-dashboard)



  
  (defun my/refresh-all-calfw-buffers ()
    "Refresh every live calfw calendar buffer without disturbing
   which window/buffer currently has focus."
    (interactive)
    (let ((current (selected-window))
          (count 0))
      (dolist (buf (buffer-list))
        (with-current-buffer buf
          (when (eq major-mode 'calfw-calendar-mode)
            (calfw-refresh-calendar-buffer t)
            (setq count (1+ count)))))
      (when (window-live-p current)
        (select-window current))
      (message "Refreshed %d calfw buffer%s" count (if (= count 1) "" "s"))))

  ;; (global-set-key (kbd "C-H-M-r") 'my/refresh-all-calfw-buffers)

  (message "calfw-quarterly-refresh loaded")
#+END_SRC

#+RESULTS:
: calfw-quarterly-refresh loaded


* Ledger-Mode
:PROPERTIES:
:ID:       141f13ad-6fd3-4178-9394-6626ebcef3cd
:END:
#+BEGIN_SRC emacs-lisp
  (use-package ledger-mode
    :ensure t
    :init
    (setq ledger-clear-whole-transactions 1)
    :bind (
           :map ledger-mode-map
           ("C-M-n" . ledger-navigate-next-uncleared)
           ("C-M-p" . ledger-navigate-previous-uncleared))
    :mode "\\.dat\\'")

  (setq ledger-reports
        '(("bal"            "%(binary) -f %(ledger-file) bal")
          ("bal this month" "%(binary) -f %(ledger-file) bal -p %(month) -S amount")
          ("bal this year"  "%(binary) -f %(ledger-file) bal -p 'this year'")
          ("net worth"      "%(binary) -f %(ledger-file) bal Assets Liabilities")
          ("account"        "%(binary) -f %(ledger-file) reg %(account)")))

  (message "ledger-mode loaded")

#+END_SRC

#+RESULTS:
| bal            | %(binary) -f %(ledger-file) bal                       |
| bal this month | %(binary) -f %(ledger-file) bal -p %(month) -S amount |
| bal this year  | %(binary) -f %(ledger-file) bal -p 'this year'        |
| net worth      | %(binary) -f %(ledger-file) bal Assets Liabilities    |
| account        | %(binary) -f %(ledger-file) reg %(account)            |

* Tramp
:PROPERTIES:
:ID:       89c1c96b-f2ba-49dd-be16-06693a0c9cf3
:END:
#+BEGIN_SRC emacs-lisp
  ;; TRAMP Configuration
  (use-package tramp
    :ensure nil
    :config
    (setq tramp-default-method "ssh")
    (setq tramp-verbose 6)
    (setq tramp-completion-reread-directory-timeout nil)
    (setq remote-file-name-inhibit-cache nil)

    ;; vs cleanup
    (setq vc-handled-backends nil) ;; on remote, disable vc entirely

    (setq remote-file-name-inhibit-auto-save-visited t)
    (setq dired-free-space nil)
    
    (setq tramp-ssh-controlmaster-options
          (concat "-o ControlMaster=auto "
                  "-o ControlPath='~/.ssh/tramp.%%r@%%h:%%p' "
                  "-o ControlPersist=yes"))
    ;; Synology setup
    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "100.85.137.126") "login-args"
                       '(("-tt") ("-l" "%u") ("-p" "%p") ("%h"))))
    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "100.85.137.126") "remote-shell" "/bin/sh"))
    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "100.85.137.126") "tramp-direct-async-process" t))
    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "100.85.137.126") "shell-file-name" "/bin/sh"))
    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "100.85.137.126") "perl" nil))
    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "100.85.137.126") "ls" "/bin/ls"))

    ;; Unraid setup
    (add-to-list 'tramp-connection-properties
                 (list (regexp-quote "100.88.52.8") "login-args"
                       '(("-tt") ("-l" "%u") ("-p" "%p") ("%h"))))


    (add-hook 'kill-emacs-hook 'tramp-dump-connection-properties)
    ;; Speed up TRAMP by skipping version control checks on remote files
    (setq vc-ignore-dir-regexp
          (format "%s\\|%s" vc-ignore-dir-regexp tramp-file-name-regexp)))

  ;; speed up directory loading on remote machines by removing hooks


  (setq dired-free-space nil)

  ;; Setup local shell variables for tramp connections
  (with-eval-after-load 'tramp
    (connection-local-set-profile-variables
     'remote-bash-profile
     '((explicit-shell-file-name . "/bin/bash")
       (explicit-bash-args . ("--login" "-i"))))

    (connection-local-set-profiles
     '(:application tramp)
     'remote-bash-profile))


  (defun my/remote-buffer-setup ()
    (when (file-remote-p default-directory)
      (setq-local vc-handled-backends nil)
      (setq-local doom-modeline-vcs-max-length 0)))

  (add-hook 'find-file-hook #'my/remote-buffer-setup -90)
  (add-hook 'dired-mode-hook #'my/remote-buffer-setup -90)
    ;;; end speed up

  ;; NAS Bookmark
  (defun my/open-nas ()
    "Open Synology NAS in dired via TRAMP"
    (interactive)
    (dired "/ssh:tyler@100.85.137.126:/volume1/"))

  (defun my/open-unraid ()
    "Open Unraid server in dired via TRAMP"
    (interactive)
    (find-file "/ssh:root@100.88.52.8:/mnt/user/"))

  (defun my/tramp-cleanup-all-connections ()
    "Persist cache to disk, then flush all Tramp internal objects."
    (interactive)
    (tramp-dump-connection-properties)
    (tramp-cleanup-all-connections))

  (global-set-key (kbd "C-c t c") #'my/tramp-cleanup-all-connections)

#+END_SRC

#+RESULTS:
: my/tramp-cleanup-all-connections

* IDE Setup

** Project.el
:PROPERTIES:
:ID:       2d2433b8-aa6b-4d99-8638-0668ebf5b603
:END:
#+BEGIN_SRC emacs-lisp
  (message "loading project.el")  
  (use-package project
    :ensure nil ;; don't try to install it's built in
    :demand t
    :bind-keymap ("C-c p" . project-prefix-map)
    :config
    (setq project-switch-commands
  	'((consult-fd "Find file" ?f)
  	  (consult-ripgrep "Ripgrep" ?r)
  	  (project-eshell "Eshell" ?e)
  	  (my/project-vterm "Vterm" ?v)
  	  (magit-project-status "Magit" ?m))))
    

  (defun my/project-try-local (dir)
    "If DIR or a parent contains a .project marker file, treat that as project root."
    (when-let ((root (locate-dominating-file dir ".project")))
      (cons 'transient root)))

  (add-hook 'project-find-functions #'my/project-try-local)

  (defun my/project-vterm ()
    (interactive)
    (let ((default-directory (project-root (project-current t))))
      (vterm)))
#+END_SRC

#+RESULTS:
: my/project-vterm

** Magit
:PROPERTIES:
:ID:       4becc3d1-48ee-4d51-83e1-80cf546efed1
:END:
#+begin_src emacs-lisp
  (use-package magit
    :commands (magit-status magit-get-current-branch)
    :bind (("C-x g". magit-status))
    :custom
    (magit-display-buffer-function #'magit-display-buffer-same-window-except-diff-v1))
  (setq magit-clone-default-directory "~/Projects/")

  ;; Bindings



#+end_src

#+RESULTS:
: ~/Projects/

** Git Gutter
:PROPERTIES:
:ID:       3d59ec80-9623-4101-9ec2-7a71a4a84204
:END:
#+BEGIN_SRC emacs-lisp
  (use-package git-gutter
    :hook (prog-mode . git-gutter-mode)
    :config
    (setq git-gutter:update-interval 0.02))
  (use-package git-gutter-fringe
    :config
    (define-fringe-bitmap 'git-gutter-fr:added [224] nil nil '(center repeated))
    (define-fringe-bitmap 'git-gutter-fr:modified [224] nil nil '(center repeated))
    (define-fringe-bitmap 'git-gutter-fr:deleted [128 192 224 240] nil nil 'bottom))

#+END_SRC

#+RESULTS:
: t

** Forge
:PROPERTIES:
:ID:       bf6e4882-8e57-4f4c-b238-99d19e3e0383
:END:
#+begin_src emacs-lisp
  ;;  (use-package forge)
#+end_src

** Git from Clipboard
:PROPERTIES:
:ID:       74003e46-5651-4447-bf9a-682901416ab2
:END:
#+begin_src emacs-lisp

  (defun ar/git-clone-clipboard-url ()
    "Clone git URL in clipboard asynchronously and open in dired when finished."
    (interactive)
    (cl-assert (string-match-p "^\\(http\\|https\\|ssh\\)://" (current-kill 0)) nil "No URL in clipboard")
    (let* ((url (current-kill 0))
           (download-dir (expand-file-name "~/Downloads/"))
           (project-dir (concat (file-name-as-directory download-dir)
                                (file-name-base url)))
           (default-directory download-dir)
           (command (format "git clone %s" url))
           (buffer (generate-new-buffer (format "*%s*" command)))
           (proc))
      (when (file-exists-p project-dir)
        (if (y-or-n-p (format "%s exists. delete?" (file-name-base url)))
            (delete-directory project-dir t)
          (user-error "Bailed")))
      (switch-to-buffer buffer)
      (setq proc (start-process-shell-command (nth 0 (split-string command)) buffer command))
      (with-current-buffer buffer
        (setq default-directory download-dir)
        (shell-command-save-pos-or-erase)
        (require 'shell)
        (shell-mode)
        (view-mode +1))
      (set-process-sentinel proc (lambda (process state)
                                   (let ((output (with-current-buffer (process-buffer process)
                                                   (buffer-string))))
                                     (kill-buffer (process-buffer process))
                                     (if (= (process-exit-status process) 0)
                                         (progn
                                           (message "finished: %s" command)
                                           (dired project-dir))
                                       (user-error (format "%s\n%s" command output))))))
      (set-process-filter proc #'comintoutput-filter)))
#+end_src

** Rainbow Deliminators
:PROPERTIES:
:ID:       66b4c6bd-5d1a-4b72-872b-729b6e7161f2
:END:
#+begin_src emacs-lisp
  (use-package rainbow-delimiters
    :hook (prog-mode . rainbow-delimiters-mode))

#+end_src

#+RESULTS:
| rainbow-delimiters-mode |

** Electric Pair mode
:PROPERTIES:
:ID:       eaf71d0b-8499-4f14-a68e-2db979d1b803
:END:
#+BEGIN_SRC emacs-lisp
  (add-hook 'prog-mode-hook 'electric-pair-mode)
  (add-hook 'prog-mode-hook 'electric-indent-mode)
  (global-set-key (kbd "C-c s (") 'electric-pair-mode)


#+END_SRC

#+RESULTS:
| electric-indent-mode | electric-pair-mode | my-display-numbers-hook  | rainbow-delimiters-mode |
|                      |                    |                                                  |
** Paredit
:PROPERTIES:
:ID:       0717566a-30a5-4bda-944d-f5e02c7909f9
:END:
#+BEGIN_SRC emacs-lisp
  (use-package paredit
    :ensure t
    :hook ((emacs-lisp-mode . paredit-mode)
           (lisp-mode . paredit-mode)
           (scheme-mode . paredit-mode)
           (clojure-mode . paredit-mode)
           (racket-mode . paredit-mode)))
#+END_SRC
** Line Numbers
:PROPERTIES:
:ID:       ed5c5f11-67b8-474f-b94a-bd3772d03520
:END:
#+begin_src emacs-lisp
  (setq display-line-numbers-type 'relative)

  ;; (defun my-display-numbers-hook ()
  ;;   (display-line-numbers-mode t)
  ;; (add-hook 'prog-mode-hook 'my-display-numbers-hook)
  ;; (add-hook 'text-mode-hook 'my-display-numbers-hook)
  ;; (dolist (mode '(org-mode-hook))
  ;;   (add-hook mode (lambda () (display-line-numbers-mode 0)))))

  (dolist (mode '(text-mode-hook
                  prog-mode-hook
                  conf-mode-hook))
    (add-hook mode (lambda () (display-line-numbers-mode 1))))

  ;; Override some modes which derive from the above
  (dolist (mode '(org-mode-hook))
    (add-hook mode (lambda () (display-line-numbers-mode 0))))


#+end_src

#+RESULTS:
** subword mode
:PROPERTIES:
:ID:       1ec8be4e-2e68-457d-9e6e-dcd148dba301
:END:
#+BEGIN_SRC emacs-lisp
  ;;removed for symbol searching
  ;; (add-hook 'prog-mode-hook 'subword-mode)
#+END_SRC

#+RESULTS:
| subword-mode | my-display-numbers-hook | electric-indent-mode | electric-pair-mode | rainbow-delimiters-mode | git-gutter-mode |

** Commenting
:PROPERTIES:
:ID:       01a3016a-806d-4b6f-b8c8-a587aceb561b
:END:
#+begin_src emacs-lisp
  (use-package evil-nerd-commenter
    :bind ("M-;" . evilnc-comment-or-uncomment-lines))
#+end_src

** Mini-map
:PROPERTIES:
:ID:       c1992169-4e5a-4d50-9fce-5dbc74a06168
:END:
#+BEGIN_SRC emacs-lisp
  (use-package minimap)

  (setq minimap-window-location 1)

  (global-set-key (kbd "C-c s m")  'minimap-mode)
#+END_SRC

#+RESULTS:
: minimap-mode

** Languages
*** HTML & CSS
Some possible html modes to look into:
http://xahlee.info/emacs/emacs/emacs_html.html
http://xahlee.info/emacs/emacs/xah-css-mode.html
**** web-mode
:PROPERTIES:
:ID:       7e44cde1-b98e-484b-9543-c789cc502ca2
:END:
#+BEGIN_SRC emacs-lisp
  (use-package web-mode)
  (setq web-mode-enable-current-column-highlight t)
  (setq web-mode-enable-current-element-highlight t)
                                          ; hook into web mode for file types
  (add-to-list 'auto-mode-alist '("\\.phtml\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.tpl\\.php\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.[agj]sp\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.as[cp]x\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.erb\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.mustache\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.djhtml\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.html?\\'" . web-mode))
  ;;using rsjx mode
  ;;(add-to-list 'auto-mode-alist '("\\.js\\'" . web-mode))
                                          ;(add-to-list 'auto-mode-alist '("\\.css?\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.xml\\'" . web-mode))
  ;; using rsjx mode
  ;;(add-to-list 'auto-mode-alist '("\\.jsx\\'" . web-mode))
  (add-to-list 'auto-mode-alist '("\\.ts\\'" . web-mode))



                                          ; set company completions vocab to css and html

  (setq web-mode-enable-engine-detection t)

#+END_SRC

#+RESULTS:
: t
**** emmet-mode
:PROPERTIES:
:ID:       a2f9cdf2-4e11-4b8d-b3c0-bc39834658d2
:END:
#+BEGIN_SRC emacs-lisp
  (use-package emmet-mode
    :bind (
           :map emmet-mode-keymap
           ("H-n" . emmet-next-edit-point)
           ("H-p" . emmet-prev-edit-point)))
                                          ; use emmet in all web-mode docs
  (add-hook 'web-mode-hook 'emmet-mode)
  (add-hook 'css-mode-hook 'emmet-mode)

                                          ; enable mode switching between css and java
  (add-hook 'web-mode-before-auto-complete-hooks
            '(lambda ()
               (let ((web-mode-cur-language
                      (web-mode-language-at-pos)))
                 (if (string= web-mode-cur-language "php")
                     (yas-activate-extra-mode 'php-mode)
                   (yas-deactivate-extra-mode 'php-mode))
                 (if (string= web-mode-cur-language "css")
                     (setq emmet-use-css-transform t)
                   (setq emmet-use-css-transform nil)))))




#+END_SRC

#+RESULTS:
| lambda | nil | (let ((web-mode-cur-language (web-mode-language-at-pos))) (if (string= web-mode-cur-language php) (yas-activate-extra-mode 'php-mode) (yas-deactivate-extra-mode 'php-mode)) (if (string= web-mode-cur-language css) (setq emmet-use-css-transform t) (setq emmet-use-css-transform nil))) |
**** css-mode
:PROPERTIES:
:ID:       d5afb792-1a3f-48c1-970c-39b73a1d947a
:END:
#+BEGIN_SRC emacs-lisp

#+END_SRC
*** Language Server
:PROPERTIES:
:ID:       2fff3225-1990-4f91-8c05-89f53c4fa5e1
:END:
#+begin_src emacs-lisp
                                          ; breadcrumb setup

  (defun lsp-mode-setup ()
    (setq lsp-headerline-breadcrumb-segments '(path-up-to-project file symbols))
    (lsp-headerline-breadcrumb-mode))

  (use-package lsp-mode
    :commands (lsp lsp-deffered)
    :hook (lsp-mode . lsp-mode-setup)
    :init
    (setq lsp-keymap-prefix "C-c l")
    :config
    (lsp-enable-which-key-integration t))
                                          ; turn on lsp ui

  (use-package lsp-ui
    :after lsp
    :hook (lsp-mode . lsp-ui-mode)
    :config
    (setq lsp-ui-doc-position 'bottom))

  (use-package treemacs
    :config
    (setq treemacs-select-when-already-in-treemacs 'close)
    (treemacs-project-follow-mode t)
    (setq treemacs-project-follow-cleanup nil))

  (defun my/treemacs-add-project-el-project ()
    "Add the current project.el project to the treemacs workspace and reveal it."
    (interactive)
    (let* ((root (if-let ((proj (project-current)))
                     (project-root proj)
                   default-directory)))
      (unless (treemacs-is-path root :in-workspace (treemacs-current-workspace))
        (treemacs-add-project-to-workspace root))
      (treemacs-select-window)
      (treemacs-display-current-project-exclusively)))

  (define-key project-prefix-map (kbd "t") #'my/treemacs-add-project-el-project)

  (use-package lsp-treemacs
    :after lsp)
  (setq treemacs-select-when-already-in-treemacs 'close)




#+end_src

#+RESULTS:
: close
*** TypeScript
:PROPERTIES:
:ID:       faa395e4-1208-4514-b629-9e5662caff8b
:END:
#+begin_src emacs-lisp
  (use-package typescript-mode
    :mode "\\.ts\\'"
    :hook (typescript-mode . lsp-deferred)
    :config
    (setq typescript-indent-level 2))
#+end_src
*** Javascript
:PROPERTIES:
:ID:       380f0cf1-bf9f-4ec5-9fd4-f99cc8d0ae50
:END:
Following setup from here: https://www.chadstovern.com/javascript-in-emacs-revisited/
#+BEGIN_SRC emacs-lisp
  (use-package rjsx-mode
    :mode ("\\.js\\'"
           "\\.jsx\\'")
    :config
    (setq js2-mode-show-parse-errors nil
          js2-mode-show-strict-warnings nil
          js2-basic-offset 2
          js-indent-level 2)
    ;; (setq-local flycheck-disabled-checkers (cl-union flycheck-disabled-checkers
    ;;                                                  '(javascript-jshint))) ; jshint doesn't work for JSX
    (show-paren-mode 1)
    (electric-pair-mode 1))

  (use-package add-node-modules-path
    :defer t
    :hook (((js2-mode rjsx-mode) . add-node-modules-path)))

  ;; prettify

  (use-package prettier-js
    :defer t
    :diminish prettier-js-mode
    :hook (((js2-mode rjsx-mode) . prettier-js-mode)))

  ;; setup lsp mode
  (use-package lsp-mode
    :defer t
    :diminish lsp-mode
    :hook (((js2-mode rjsx-mode) . lsp))
    :commands lsp
    :config
    (setq lsp-auto-configure t
          lsp-auto-guess-root t
          ;; don't set flymake or lsp-ui so the default linter doesn't get trampled
          lsp-diagnostic-package :none))



  (use-package lsp-ui
    :defer t
    :config
    (setq lsp-ui-sideline-enable t
          ;; disable flycheck setup so default linter isn't trampled
          lsp-ui-flycheck-enable nil
          lsp-ui-sideline-show-symbol nil
          lsp-ui-sideline-show-hover nil
          lsp-ui-sideline-show-code-actions nil
          lsp-ui-peek-enable nil
          lsp-ui-imenu-enable nil
          lsp-ui-doc-enable nil))


  (defun my-js-comint-keys ()
    "My Keys for sending to the js-comint repl"
    (interactive)
    (local-set-key (kbd "C-x C-e") 'js-send-last-sexp)
    (local-set-key (kbd"C-c b") 'js-send-buffer)
    ;;(local-set-key (kbd"C-c r") 'js-send-region)
    (local-set-key (kbd"C-c C-r") 'js-send-region-and-go))




  (require 'js-comint)
  (setq inferior-js-program-command "node --interactive")
  (setenv "NODE_NO_READLINE" "1")
  (add-hook 'rjsx-mode-hook 'my-js-comint-keys)
  (add-hook 'rjsx-mode-hook 'emmet-mode)


  (with-eval-after-load 'flycheck
    (flycheck-add-next-checker 'javascript-eslint '(t . javascript-jscs)))





#+END_SRC

#+RESULTS:

*** Ligatures
:PROPERTIES:
:ID:       cc4f329c-d456-4869-90dd-ef49ef1a4a21
:END:
#+BEGIN_SRC emacs-lisp
  ;; This assumes you've installed the package via MELPA.
  (use-package ligature
    :config
    ;; Enable the "www" ligature in every possible major mode
    (ligature-set-ligatures 't '("www"
                                 "1/2"
                                 "1/4"))
    ;; Enable traditional ligature support in eww-mode, if the
    ;; `variable-pitch' face supports it
    (ligature-set-ligatures 'eww-mode '("ff" "fi" "ffi"))
    ;; Enable all Cascadia Code ligatures in programming modes
    (ligature-set-ligatures 'prog-mode '("|||>" "<|||" "<==>" "<!--" "####" "~~>" "***" "||=" "||>"
                                         ":::" "::=" "=:=" "===" "==>" "=!=" "=>>" "=<<" "=/=" "!=="
                                         "!!." ">=>" ">>=" ">>>" ">>-" ">->" "->>" "-->" "---" "-<<"
                                         "<~~" "<~>" "<*>" "<||" "<|>" "<$>" "<==" "<=>" "<=<" "<->"
                                         "<--" "<-<" "<<=" "<<-" "<<<" "<+>" "</>" "###" "#_(" "..<"
                                         "..." "+++" "/==" "///" "_|_" "www" "&&" "^=" "~~" "~@" "~="
                                         "~>" "~-" "**" "*>" "*/" "||" "|}" "|]" "|=" "|>" "|-" "{|"
                                         "[|" "]#" "::" ":=" ":>" ":<" "$>" "==" "=>" "!=" "!!" ">:"
                                         ">=" ">>" ">-" "-~" "-|" "->" "--" "-<" "<~" "<*" "<|" "<:"
                                         "<$" "<=" "<>" "<-" "<<" "<+" "</" "#{" "#[" "#:" "#=" "#!"
                                         "##" "#(" "#?" "#_" "%%" ".=" ".-" ".." ".?" "+>" "++" "?:"
                                         "?=" "?." "??" ";;" "/*" "/=" "/>" "//" "__" "~~" "(*" "*)"
                                         "\\\\" "://"))
    ;; Enables ligature checks globally in all buffers. You can also do it
    ;; per mode with `ligature-mode'.
    (global-ligature-mode t))
#+END_SRC

#+RESULTS:
: t

*** Haxe
:PROPERTIES:
:ID:       cf8c4af6-d044-429d-8c50-2d583063be07
:END:
#+BEGIN_SRC emacs-lisp
  ;; This isn't really a package, it just provides a `haxe-mode' to work with
  (use-package haxe-mode
    :mode ("\\.hx\\'" . haxe-mode)
    :no-require t
    :init
    (require 'js)
    (define-derived-mode haxe-mode js-mode "Haxe"
      "Haxe syntax highlighting mode. This is simply using js-mode for now."))

  (use-package battle-haxe
    :hook (haxe-mode . battle-haxe-mode)
    :bind (("S-<f4>" . #'pop-global-mark) ;To get back after visiting a definition
           :map battle-haxe-mode-map
           ("<f5>" . #'battle-haxe-goto-definition)
           ("<f12>" . #'battle-haxe-helm-find-references))
    :custom
    (battle-haxe-yasnippet-completion-expansion t "Keep this if you want yasnippet to expand completions when it's available.")
    (battle-haxe-immediate-completion nil "Toggle this if you want to immediately trigger completion when typing '.' and other relevant prefixes."))
#+END_SRC

#+RESULTS:

*** elisp
:PROPERTIES:
:ID:       dd6871d4-0db6-4cd2-bbfd-c72f1094a580
:END:
#+BEGIN_SRC emacs-lisp

  (use-package highlight-function-calls
    :hook (emacs-lisp-mode . highlight-function-calls-mode))

  (use-package highlight-quoted
    :hook (emacs-lisp-mode . highlight-quoted-mode))


  (defun my/sexp-newline ()
    "Move to end of current sexp and insert newline."
    (interactive)
    (up-list)
    (newline-and-indent))



  (bind-key "s-<return>" 'my/sexp-newline)
#+END_SRC

#+RESULTS:
: my/sexp-newline

** Better Completions
:PROPERTIES:
:ID:       01b1fec0-f20f-4cea-9dd8-b5646edc3bcd
:END:
#+begin_src emacs-lisp
  (use-package company
    :after lsp-mode
    :hook ((lsp-mode web-mode) . company-mode)
    :bind (
           :map company-active-map
           ( "<tab>" . company-complete-selection)
           ( "<C-tab>" . company-yasnippet)
           :map lsp-mode-map
           ("<tab>" . company-indent-or-complete-common)))
  (setq company-minimum-prefix-length 2)



  (use-package company-web
    :after company)


  (defun my-web-mode-hook ()
    (set (make-local-variable 'company-backends) '(company-css company-web-html company-yasnippet company-files)))  

  (add-hook 'web-mode-hook 'my-web-mode-hook)

#+end_src

#+RESULTS:
| my-web-mode-hook | emmet-mode |

** Debugging
Dap mode here: https://www.youtube.com/watch?v=0bilcQVSlbM

** Section Loaded Message
:PROPERTIES:
:ID:       8337a141-c181-4a8d-b772-c4ea6d4afffb
:END:
#+BEGIN_SRC emacs-lisp
  (message "IDE Setup Section Loaded")
#+END_SRC

* Macros, Functions and Keybinds
A section for my personal macors functions and keybinds
** Buffer List
:PROPERTIES:
:ID:       c18687bf-026b-4ab4-a170-a4abe7cc0331
:END:
#+BEGIN_SRC emacs-lisp
(global-set-key (kbd "C-x C-b") 'ibuffer)
#+END_SRC
** Dabbrev
:PROPERTIES:
:ID:       07e37727-a9d5-4a26-8fb5-b8a8ad78d705
:END:
#+BEGIN_SRC emacs-lisp

  (defun my/dabbrev-expand-lisp ()
    "dabbrev-expand that sees through ' and #' function prefixes."
    (interactive)
    (let ((dabbrev-abbrev-char-regexp "\\sw\\|\\s_\\|'\\|#")
          (dabbrev-abbrev-skip-leading-regexp "[#']*"))
      (dabbrev-expand nil)))

  (define-key emacs-lisp-mode-map (kbd "M-/") #'my/dabbrev-expand-lisp)
  (define-key lisp-interaction-mode-map (kbd "M-/") #'my/dabbrev-expand-lisp)

  (global-set-key (kbd "C-M-=") 'dabbrev-completion)
#+END_SRC

#+RESULTS:
: dabbrev-completion
** Emdash
:PROPERTIES:
:ID:       7581efc3-ee0b-4d7a-9637-f74d7daa4167
:END:
#+BEGIN_SRC emacs-lisp
  (defun my/insert-em-dash ()
    "Insert an em-dash at point"
    (interactive)
    (insert 8212))

  (global-set-key (kbd "H--") #'my/insert-em-dash)
#+END_SRC

#+RESULTS:
: my/insert-em-dash

** next-tag - "C-x t" - Move inside the next tag set in web-mode
:PROPERTIES:
:ID:       c901aad3-abc1-4b1f-a690-905fee116e2a
:END:
#+BEGIN_SRC emacs-lisp
  (defun next-tag()
    (interactive)
    (web-mode-element-next)
    (web-mode-tag-end))



  (global-set-key  (kbd "C-x t") 'next-tag)

  ;; timer controls
  (global-set-key (kbd "H-t t") 'org-timer-set-timer)
  (global-set-key (kbd "H-t s") 'org-timer-start)
  (global-set-key (kbd "H-t x") 'org-timer-stop)
  (global-set-key (kbd "H-t z") 'org-timer-pause-or-continue)


  ;; set ctrl z to undo
  (global-set-key (kbd "C-z") 'undo)

#+END_SRC

#+RESULTS:
: undo

#+RESULTS::
: next-tag
** Window Movement
Managing Windows, buffers etc
*** Window Movement Keymap
| action                              | key        |
|-------------------------------------+------------|
| other-window                        | M-+       |
| 'ace-window                         | M-[        |
| 'treemacs-select-window             | M-]        |
| 'treemacs                           | C-c s t    |
| 'delete-window                      | H-<escape> |
| 'delete-other-windows               | H-1        |
| 'split-window-below                 | H-2        |
| 'split-window-right                 | H-3        |
| 'buffer-menu                        | C-H        |
| 'buffer-quick-switch                | H-b        |
| 'list-bookmarks                     | C-H-g      |
| 'dired-jump                         | C-H-t      |
| 'kill-current-buffer                | H-k        |
| 'kill-current-buffer-and-window     | H-C-k      |
| 'clone-indirect-buffer-other-window | H-C-i      |
| 'buffer-menu                        | H-C-b      |

*** General movement
**** Functions and Bindings
:PROPERTIES:
:ID:       39826ac9-1a56-4a88-8afe-786599f858d1
:END:
#+BEGIN_SRC emacs-lisp
  ;; Macros & commands
                                          ;(fset 'buffer-quick-switch
                                          ;     (kmacro-lambda-form [?\C-x ?b return] 0 "%d"))

  (defun buffer-cycle-hidden()
    "cycle through previously hidden buffers"
    (interactive)
    (switch-to-buffer (last-buffer)))

  (defun buffer-last()
    "quickly switch to the previous buffer"
    (interactive)
    (switch-to-buffer (other-buffer)))

  (defun kill-current-buffer-and-window ()
    "Kill the current buffer then clear the window"
    (interactive)
    (progn
      (kill-current-buffer)
      (delete-window)))


  ;; Bindings
  (global-set-key (kbd "M-[") 'other-window)
  (global-set-key (kbd "M-]") 'other-frame)
  (global-set-key (kbd "C-M-]") 'ace-window)
  (global-set-key (kbd "<C-H-tab>") 'treemacs-select-window)
  (global-set-key (kbd "C-M-[") 'treemacs)
  (global-set-key (kbd "H-<escape>") 'delete-window)
  (global-set-key (kbd "C-H-<escape>") 'delete-frame)
  (global-set-key (kbd "H-1") 'delete-other-windows)
  (global-set-key (kbd "C-H-M-<escape>") 'delete-other-frames)
  (global-set-key (kbd "H-2") 'split-window-below)
  (global-set-key (kbd "H-3") 'split-window-right)
  (global-set-key (kbd "H-b") 'buffer-last)
  (global-set-key (kbd "M-H-b") 'buffer-cycle-hidden)
  (global-set-key (kbd "C-H-g") 'list-bookmarks)
  (global-set-key (kbd "C-H-t") 'dired-jump)
  (global-set-key (kbd "H-k") 'kill-current-buffer)
  (global-set-key (kbd "C-H-k") 'kill-current-buffer-and-window)
  (global-set-key (kbd "H-+") 'other-frame)
  (global-set-key (kbd "C-H-i") 'clone-indirect-buffer-other-window)




#+END_SRC

#+RESULTS:
: clone-indirect-buffer-other-window





#+RESULTS:
: bookmark-jump
*** Homebrew Harpoon
:PROPERTIES:
:ID:       bb9c239f-818d-4409-ad8f-16b9397bde82
:END:
#+BEGIN_SRC emacs-lisp


  ;; ascii codes for registers
  ;; a = 97
  ;; s = 115
  ;; d = 100
  ;; f = 102



  (defun current-buffer-is-harpooned (marker)
    (and marker
         (markerp marker)
         (eq (marker-buffer marker) (current-buffer))))

  (defun harpoon-jump (register)
    (let ((current (get-register active-harpoon)))
      (when (current-buffer-is-harpooned current)
        (point-to-register active-harpoon)))
    (jump-to-register register)
    (setq active-harpoon register))

  (defun harpoon-set (register)
    (point-to-register register)
    (setq active-harpoon register))

  (defun harpoon-status ()
    "Show and optionally jump to harpooned buffers with live preview."
    (interactive)
    (let* ((slots '(("a" . 97) ("s" . 115) ("d" . 100) ("f" . 102)))
           (candidates (mapcar (lambda (slot)
                                 (let ((r (get-register (cdr slot))))
                                   (format "%s: %s" (car slot)
                                           (if (markerp r)
                                               (buffer-name (marker-buffer r))
                                             "empty"))))
                               slots))
           (choice (consult--read
                    (lambda (str pred action)
                      (if (eq action 'metadata)
                          '(metadata (display-sort-function . identity)
                                     (cycle-sort-function . identity))
                        (complete-with-action action candidates str pred)))
                    :prompt "Harpoon: "
                    :preview-key 'any
                    :state
                    (lambda (action candidate)
                      (when (and candidate (eq action 'preview))
                        (let* ((key (substring candidate 0 1))
                               (register (cdr (assoc key slots)))
                               (r (when register (get-register register))))
                          (when (markerp r)
                            (let ((buf (marker-buffer r)))
                              (when buf
                                (display-buffer buf
  					      '(display-buffer-use-some-window
  						(inhibit-same-window . t))))))))))))
      (when choice
        (let* ((key (substring choice 0 1))
               (register (cdr (assoc key slots))))
          (when register (harpoon-jump register))))))




  ;; Then just bind directly with lambdas
  (global-set-key (kbd "H-a") (lambda () (interactive) (harpoon-jump 97)))
  (global-set-key (kbd "H-s") (lambda () (interactive) (harpoon-jump 115)))
  (global-set-key (kbd "H-d") (lambda () (interactive) (harpoon-jump 100)))
  (global-set-key (kbd "H-f") (lambda () (interactive) (harpoon-jump 102)))

  (global-set-key (kbd "C-H-a") (lambda () (interactive) (harpoon-set 97)))
  (global-set-key (kbd "C-H-s") (lambda () (interactive) (harpoon-set 115)))
  (global-set-key (kbd "C-H-d") (lambda () (interactive) (harpoon-set 100)))
  (global-set-key (kbd "H-g") #'harpoon-status)


  ;; (global-set-key (kbd "C-H-f") (lambda () (interactive) (harpoon-set 102)))
  ;; (global-set-key (kbd "H-g") 'harpoon-status)


  ;; (defun harpoon-status ()
  ;; "Show and optionally jump to harpooned buffers."
  ;; (interactive)
  ;; (let* ((slots '(("a" . 97) ("s" . 115) ("d" . 100) ("f" . 102)))
  ;;        (candidates (mapcar (lambda (slot)
  ;;                              (let ((r (get-register (cdr slot))))
  ;;                                (format "%s: %s" (car slot)
  ;;                                        (if (markerp r)
  ;;                                            (buffer-name (marker-buffer r))
  ;;                                          "empty"))))
  ;;                            slots))
  ;;        (choice (completing-read
  ;;                 "Harpoon: "
  ;;                 (lambda (str pred action)
  ;;                   (if (eq action 'metadata)
  ;;                       '(metadata (display-sort-function . identity)
  ;;                                  (cycle-sort-function . identity))
  ;;                     (complete-with-action action candidates str pred))))))
  ;;   (when choice
  ;;     (let* ((key (substring choice 0 1))
  ;;            (register (cdr (assoc key slots))))
  ;;       (when register (harpoon-jump register))))))


  ;; (global-set-key (kbd "H-a") 'harpoon-a)
  ;; (global-set-key (kbd "C-H-a") 'set-harpoon-a)
  ;; (global-set-key (kbd "H-s") 'harpoon-s)
  ;; (global-set-key (kbd "C-H-s") 'set-harpoon-s)
  ;; (global-set-key (kbd "H-d") 'harpoon-d)
  ;; (global-set-key (kbd "C-H-d") 'set-harpoon-d)
  ;; (global-set-key (kbd "H-f") 'harpoon-f)
  ;; (global-set-key (kbd "C-H-f") 'set-harpoon-f)

#+END_SRC

#+RESULTS:
: harpoon-status


#+RESULTS:
: set-harpoon-f
*** Posframe repositioning
This code augments the mini-buffer position which uses pos-frame. It does two core things. On my normal/default wide screen setup we move the minibuffer to the left so that we can keep embark commands showing in a right-side buffer and read them.

When we move to the vertical monitor it repositions the minibuffer to be default middle of screen height. 
#+BEGIN_SRC emacs-lisp

#+END_SRC>
** Cursor Movement
Move the cursor
*** Cursor Movement Keymap
|-------------------------------------+-----|
| Action                              | Key |
|-------------------------------------+-----|
| insert-line-above-and-go            | M-o |
| indent-new-comment-line             | C-; |
|-------------------------------------+-----|
| xref-find-references                | H-] |
| xref-go-back                        | H-[ |
| goto-line                           | H-g |
| wrap-sexp-backward-with-parenthesis | C-( |
| swap-point-and-mark (macro)         | M-m |
| my/pop-local-mark-ring              | H-m |
| back-to-indentation                 | M-i |



*** Function Definitions
:PROPERTIES:
:ID:       64d7df9d-7da2-4069-be77-287472410c7f
:END:
#+BEGIN_SRC emacs-lisp
  (defun my/pop-local-mark-ring ()
    "Move cursor to last mark position of current buffer, repeat calls will cycle"
    (interactive)
    (set-mark-command t))

  (defun my/pop-global-mark-ring()
    "move cursor to last mark in global ring, repeat calls will cycle"
    (interactive)
    (pop-global-mark)
    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "m") 'my/pop-global-mark-ring)
       map))
    )

  (global-set-key (kbd "C-H-M-m") 'consult-global-mark)

  (defun my/shrink-current-window()
    "Custom window shrink command with repetable key"
    (interactive)
    (shrink-window-horizontally 5)

    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "b") 'my/shrink-current-window)
       (define-key map (kbd "{") 'my/shrink-current-window)
       (define-key map (kbd "n") 'my/enlarge-current-window)       
       (define-key map (kbd "}") 'my/enlarge-current-window)
       (define-key map (kbd "g") 'my/shrink-current-window-vertically)
       (define-key map (kbd "(") 'my/shrink-current-window-vertically)
       (define-key map (kbd "h") 'my/enlarge-current-window-vertically)
       (define-key map (kbd ")") 'my/enlarge-current-window-vertically)

       map))
    )

  (defun my/enlarge-current-window()
    "Custom window shrink command with repetable key"
    (interactive)
    (enlarge-window-horizontally 5)
    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "b") 'my/shrink-current-window)
       (define-key map (kbd "{") 'my/shrink-current-window)
       (define-key map (kbd "n") 'my/enlarge-current-window)       
       (define-key map (kbd "}") 'my/enlarge-current-window)
       (define-key map (kbd "g") 'my/shrink-current-window-vertically)
       (define-key map (kbd "(") 'my/shrink-current-window-vertically)
       (define-key map (kbd "h") 'my/enlarge-current-window-vertically)
       (define-key map (kbd ")") 'my/enlarge-current-window-vertically)
       map))
    )

  (defun my/shrink-current-window-vertically()
    "Custom vertical window shrink"
    (interactive)
    (shrink-window 3)
    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "b") 'my/shrink-current-window)
       (define-key map (kbd "{") 'my/shrink-current-window)
       (define-key map (kbd "n") 'my/enlarge-current-window)       
       (define-key map (kbd "}") 'my/enlarge-current-window)
       (define-key map (kbd "g") 'my/shrink-current-window-vertically)
       (define-key map (kbd "(") 'my/shrink-current-window-vertically)
       (define-key map (kbd "h") 'my/enlarge-current-window-vertically)
       (define-key map (kbd ")") 'my/enlarge-current-window-vertically)
       map))
    )

  (defun my/enlarge-current-window-vertically()
    "Custom vertical window shrink"
    (interactive)
    (enlarge-window 3)
    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "b") 'my/shrink-current-window)
       (define-key map (kbd "{") 'my/shrink-current-window)
       (define-key map (kbd "n") 'my/enlarge-current-window)       
       (define-key map (kbd "}") 'my/enlarge-current-window)
       (define-key map (kbd "g") 'my/shrink-current-window-vertically)
       (define-key map (kbd "(") 'my/shrink-current-window-vertically)
       (define-key map (kbd "h") 'my/enlarge-current-window-vertically)
       (define-key map (kbd ")") 'my/enlarge-current-window-vertically)
       map))
    )

  (defun my/insert-line-above-and-go ()
    ;;insert a line above the current one and move the cursor there
    (interactive)
    (previous-line nil)
    (move-end-of-line nil)
    (electric-newline-and-maybe-indent)
    (indent-relative-first-indent-point))


  (defun wrap-sexp-backward-with-parenthesis()
    "wrap the current exppression backwards with parenthesis"
    (interactive)
    (backward-sexp)
    (mark-sexp) 
    (insert-parentheses))


  (global-set-key (kbd "M-<f23>") 'my/shrink-current-window)

#+END_SRC

#+RESULTS:
: wrap-sexp-backward-with-parenthesis

: wrap-sexp-backward-with-parenthesis
*** Key binding
:PROPERTIES:
:ID:       8ed62867-fadf-4417-a63b-785d5961a428
:END:
#+BEGIN_SRC emacs-lisp

  (global-set-key (kbd "M-o") 'my/insert-line-above-and-go)
  ;; move C-j to C-; indent-new-comment-line
  (global-set-key (kbd "C-;") 'indent-new-comment)                      
  (global-set-key (kbd "H-]") 'xref-find-references)
  (global-set-key (kbd "H-[") 'xref-go-back)
  (global-set-key (kbd "C-{") 'my/shrink-current-window)
  (global-set-key (kbd "C-}") 'my/enlarge-current-window)
  (global-set-key (kbd "C-)") 'my/enlarge-current-window-vertically)
  (global-set-key (kbd "C-(") 'my/shrink-current-window-vertically)


  ;;  (global-set-key (kbd "H-g") 'goto-line)
  ;; find a new binding
  ;; (global-set-key (kbd "C-(") 'wrap-sexp-backward-with-parenthesis)
  ;; swap point and mark
  (global-set-key (kbd "M-m")  (kmacro-lambda-form [?\C-u ?\C-x ?\C-x] 0 "%d"))
  ;; cycle marks
  (global-set-key (kbd "H-m") 'my/pop-local-mark-ring)
  (global-set-key (kbd "C-H-m") 'my/pop-global-mark-ring)
  ;; rebind back-to-indentation to "M-i" NOTE this unbinds!! tab-to-tab-stop
  (global-set-key (kbd "M-i") 'back-to-indentation)



#+End_SRC

#+RESULTS:
: back-to-indentation

** Editing Text
*** Function Definitions
:PROPERTIES:
:ID:       23f460e9-661e-4b42-b8c2-49539f40cf26
:END:
#+BEGIN_SRC emacs-lisp
  (defun duplicate-current-line()
    "Duplicates the entire line under point. Repetable with 'd' "
    (interactive)
    (back-to-indentation)
    (kill-line)
    (yank)
    (newline)
    (indent-for-tab-command)
    (yank)
    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "y") 'duplicate-current-line)
       map)))

  (defun duplicate-line-up-to-point()

    "Duplicates a line from start of indentation up to point. May be repeated with single 'd' presses."
    (interactive)
    (set-mark-command nil)
    (back-to-indentation)
    (kill-ring-save (region-beginning) (region-end))
    (end-of-line)
    (newline)
    ;; example of single key repeat functionality
    (yank)
    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "y") 'duplicate-line-up-to-point)
       map)))



  (defun kill-word-at-point()
    "Kill the full word at point"
    (interactive)
    (kill-word 1)
    (backward-kill-word 1))

  (defun kill-line-at-point()
    "Kill full lilne at point"
    (interactive)
    (back-to-indentation)
    (kill-line))


#+END_SRC

#+RESULTS:
: kill-line-at-point
*** Keybinds
:PROPERTIES:
:ID:       60945c6b-bacd-40f2-aebd-0b78fd9e1c2e
:END:
#+BEGIN_SRC emacs-lisp

  (global-set-key (kbd "C-M-i") 'indent-region)
  (global-set-key (kbd "M-DEL") 'kill-word-at-point)
  (global-set-key (kbd "M-k") 'kill-line-at-point)
  (global-set-key (kbd "s-k") 'kill-sentence)
  (global-set-key (kbd "s-y") 'duplicate-current-line)
  (global-set-key (kbd "H-y") 'duplicate-line-up-to-point)
  (global-set-key (kbd "<f10>") 'ispell)

#+END_SRC

#+RESULTS:

** Jump to Scratch
:PROPERTIES:
:ID:       53b4cb47-6c71-4ad6-9dc9-82dc6b770646
:END:
#+BEGIN_SRC emacs-lisp
  (defun my/jump-to-scratch ()
    "Jump to a scratch buffer. If multiple scratch buffers exist, prompt to choose."
    (interactive)
    (let ((scratch-buffers
           (seq-filter
            (lambda (buf)
              (string-match-p "^\\*scratch[0-9]*\\*$" (buffer-name buf)))
            (buffer-list))))
      (cond
       ((null scratch-buffers)
        (switch-to-buffer "*scratch*"))
       ((= (length scratch-buffers) 1)
        (switch-to-buffer (car scratch-buffers)))
       (t
        (switch-to-buffer
         (completing-read "Scratch buffer: "
                          (mapcar #'buffer-name scratch-buffers)
                          nil t))))))

  (global-set-key (kbd "H-;") #'my/jump-to-scratch)
#+END_SRC

#+RESULTS:
: my/jump-to-scratch

** Load config workspace
:PROPERTIES:
:ID:       b9dfbe16-bebf-47dd-8b20-07e6cebe7523
:END:
#+BEGIN_SRC emacs-lisp
  (defun my/open-config-workspace ()
    "
  Open a 3-pane config editing workspace:
  Left  : literate org config (full height)
  Right top    : *scratch*
  Right bottom : *Messages* and *Warnings* side by side
  "
    (interactive)
    (delete-other-windows)
    ;; Left pane — full height org config
    (let ((config-window (selected-window)))
      (find-file "~/dotfiles/configbackups/main-config.org")
      ;; Split vertically to create right column
      (split-window-right)
      (other-window 1)
      ;; Right top — scratch
      (switch-to-buffer "*scratch*")
      ;; Split right column horizontally for bottom row
      (split-window-below)
      (other-window 1)
      ;; Right bottom — split left/right for Messages & Warnings
      (switch-to-buffer "*Messages*")
      (split-window-right)
      (other-window 1)
      (switch-to-buffer (get-buffer-create "*Warnings*"))
      ;; Return focus to config
      (select-window config-window)))

  (global-set-key (kbd "M-s-c") #'my/open-config-workspace)

#+END_SRC

#+RESULTS:
: my/open-config-workspace

** Keymap backup & restore
:PROPERTIES:
:ID:       11aedc39-5cda-42de-8890-366feeaeb60e
:END:
#+BEGIN_SRC emacs-lisp
  (defun my/backup-global-keymap ()
    "Save a snapshot of the current global keymap."
    (interactive)
    (setq my/keymap-backup (copy-keymap (current-global-map)))
    (message "Global keymap backed up."))

  (defun my/restore-global-keymap ()
    "Restore the global keymap from the last backup."
    (interactive)
    (if (boundp 'my/keymap-backup)
        (progn
          (use-global-map my/keymap-backup)
          (message "Global keymap restored."))
      (message "No keymap backup found.")))

  (global-set-key (kbd "C-c k b") #'my/backup-global-keymap)
  (global-set-key (kbd "C-c k r") #'my/restore-global-keymap)
#+END_SRC

** Narrowing
Narrowing is a powerful feature for focus and restricting macros, searching, replacing etc. Lets make it easier to do more frequently.

*** Keymap
| Action                | Key     | Note                                                             |
|-----------------------+---------+------------------------------------------------------------------|
| Widen                 | H-w     |                                                                  |
| Narrow to Defun       | H-e     |                                                                  |
| narrow to Region      | C-H-e   |                                                                  |
| Narrow to Page        | C-M-H-e | Use C-x C-p to mark a page boundry                               |
| Org Narrow to Element | H-e     | These are bound to the org-mode-map to over-ride defun and block |
| Org Narrow to Subtree | C-H-e   |                                                                  |
| Org Narrow to Block   | C-M-H-e |                                                                  |


*** Bindings
:PROPERTIES:
:ID:       03003ec9-bb8d-463c-99c7-f8a4aaab699b
:END:
#+BEGIN_SRC emacs-lisp
  (global-set-key (kbd "H-w") 'widen)
  (global-set-key (kbd "H-r") 'narrow-to-region)
  (global-set-key (kbd "H-e") 'narrow-to-defun)
  (global-set-key (kbd "C-H-r") 'narrow-to-page)



#+END_SRC

#+RESULTS:
: narrow-to-page

** Org Agenda
*** my/org-agenda-lock
:PROPERTIES:
:ID:       940c1ee5-ee8e-486c-adc2-f538f5bf1fcc
:END:
#+BEGIN_SRC emacs-lisp
  (defvar my/agenda-restriction-active nil
    "Whether the agenda restriction lock is currently active.")

  (defun my/toggle-org-agenda-restriction ()
    "Toggle agenda restriction lock on and off."
    (interactive)
    (setq my/agenda-restriction-active (not my/agenda-restriction-active))
    (if my/agenda-restriction-active
        (my/org-agenda-set-restriction-lock)
      (org-agenda-remove-restriction-lock)))

  (defun my/org-agenda-set-restriction-lock ()
    "Set agenda restriction lock and refresh."
    (interactive)
    (org-agenda-set-restriction-lock-from-agenda nil)
    (org-agenda-redo))
#+END_SRC
** Org
:PROPERTIES:
:ID:       0f29bb8f-2a5a-48ce-b4b9-804f8c6cff63
:END:
#+BEGIN_SRC emacs-lisp

  (defun my/next-error()
    "create a repetable next-error command"
    (interactive)
    (next-error)
    (set-temporary-overlay-map
     (let ((map (make-sparse-keymap)))
       (define-key map (kbd "o") 'my/next-error)
       map))
    )

  (defun my/org-occur()
    "Run occur and let additional presses of o trigger next-error results"
    (interactive)
    (progn
      (call-interactively 'org-occur)
      (my/next-error)))


  (with-eval-after-load 'org
    (defun my/set-meeting-context ()
      "Prompt for CONTEXT property, defaulting to current heading text."
      (interactive)
      (let* ((heading (org-get-heading t t t t))
             (input (read-string "Context: " heading)))
        (org-set-property "CONTEXT" input)))

    (defun my/insert-meeting-todo ()
      "Insert a TODO heading then prepend CONTEXT property if found."
      (interactive)
      (org-meta-return)
      (org-todo "TODO")
      (let ((context (org-entry-get (point) "CONTEXT" t)))
        (when context
  	(insert "[" context "] "))))

    (define-key org-mode-map (kbd "C-c i t") #'my/insert-meeting-todo)
    (define-key org-mode-map (kbd "C-c C-x m") #'my/set-meeting-context))

#+END_SRC
** Productivity
:PROPERTIES:
:ID:       2ddf35b7-015b-4225-be88-02e046981a7f
:END:
#+BEGIN_SRC emacs-lisp

#+END_SRC
*** Star Rating Insert
:PROPERTIES:
:ID:       0a04d023-9ef9-4942-aef0-77dbd0e3163e
:END:
#+BEGIN_SRC emacs-lisp


#+END_SRC

#+RESULTS:
: hydra-insert-rating/body
** Section Loaded Message
:PROPERTIES:
:ID:       908f9de9-69d0-4d33-8567-990bb00fcd28
:END:
#+BEGIN_SRC emacs-lisp
  (message "Macros, Functions and Keybinds loaded")
#+END_SRC
* dired
** Keymap
| key    | flag meaning               |
|--------+----------------------------|
| m      | mark file                  |
| u      | unmark file                |
| U      | unmark all files           |
| % -> m | Regex mark matching files  |
| *      | Mark by type               |
| (      | expand or collapse details |
| k      | kill marked files          |
| g      | reload buffer              |
| * t    | invert marked files        |
| S      | create symbolic link       |

** config
:PROPERTIES:
:ID:       b0d1363e-9d3e-489f-95b2-45898964e575
:END:
#+BEGIN_SRC emacs-lisp
  					; list directories first
  (setq dired-listing-switches "-agho --group-directories-first")
  (setq dired-dwim-target t)

  (defun my/dired-cd-to-shell ()
    "Send a `cd` for the current dired directory to the most recent shell buffer."
    (interactive)
    (let ((dir (dired-current-directory))
          (shell-buf (get-buffer "*shell*")))
      (if (not shell-buf)
          (message "No *shell* buffer found — start one with M-x shell first.")
        (with-current-buffer shell-buf
          (goto-char (point-max))
          (insert (format "cd \"%s\"" (directory-file-name dir)))
          (comint-send-input))
        (pop-to-buffer shell-buf))))




#+END_SRC
#+RESULTS:
: t
** SIngle Dired Buffer
:PROPERTIES:
:ID:       b83bb515-fefa-4492-a6f8-991e6d94c399
:END:
#+BEGIN_SRC emacs-lisp
  ;;  (use-package dired-single)
#+END_SRC

#+RESULTS:

** Icons
:PROPERTIES:
:ID:       f31dbebd-21f1-480a-9d07-ce328ca8592a
:END:
#+BEGIN_SRC emacs-lisp

  (use-package all-the-icons)
  (add-to-list 'all-the-icons-extension-icon-alist
               '("m3u"  all-the-icons-material "playlist_play"  :face all-the-icons-orange))
  (add-to-list 'all-the-icons-extension-icon-alist
               '("org_archive" all-the-icons-faicon "archive" :face all-the-icons-orange))

  ;; Explicitly map each all-the-icons font family to its Unicode PUA
  ;; range so Emacs doesn't rely on automatic per-face font fallback
  ;; (which is what was failing inside fixed-pitch org-table faces).
  ;; Use 'append here — 'prepend is only needed on NS/Mac ports.

  (set-fontset-font t 'unicode (font-spec :family "all-the-icons")   nil 'append)
  (set-fontset-font t 'unicode (font-spec :family "file-icons")      nil 'append)
  (set-fontset-font t 'unicode (font-spec :family "FontAwesome")     nil 'append)
  (set-fontset-font t 'unicode (font-spec :family "Material Icons")  nil 'append)
  (set-fontset-font t 'unicode (font-spec :family "github-octicons") nil 'append)
  (set-fontset-font t 'unicode (font-spec :family "Weather Icons")   nil 'append)



  (use-package all-the-icons-dired
    :hook (dired-mode . all-the-icons-dired-mode))
#+END_SRC

#+RESULTS:
| my/remote-buffer-setup | all-the-icons-dired-mode |

** dired-buffer-to-tree
:PROPERTIES:
:ID:       69b4c852-7acf-44f3-8b21-bff9d5ba12b6
:END:
#+BEGIN_SRC emacs-lisp
  (defvar my/dired-tree-ignore-regexp
    (regexp-opt '("node_modules" ".git" "__pycache__" ".venv" "dist" "build"))
    "Directory names to skip when building a tree listing.")

  (defun my/dired-tree--walk (dir prefix out)
    (let* ((entries (directory-files dir nil "^[^.]" t))
           (entries (sort entries #'string-lessp))
           (n (length entries)))
      (cl-loop for entry in entries
               for i from 1
               for full = (expand-file-name entry dir)
               for last = (= i n)
               for connector = (if last "└── " "├── ")
               for child-prefix = (concat prefix (if last "    " "│   "))
               do
               (unless (and (file-directory-p full)
                            (string-match-p my/dired-tree-ignore-regexp entry))
                 (push (concat prefix connector entry) (car out))
                 (when (file-directory-p full)
                   (my/dired-tree--walk full child-prefix out))))))

  (defun my/dired-tree-to-buffer ()
    "Build a plain-text nested tree of the current dired directory and put it in a buffer, ready to copy into an AI client."
    (interactive)
    (let* ((dir (dired-current-directory))
           (out (list nil))
           (buf (get-buffer-create "*dired-tree*")))
      (my/dired-tree--walk dir "" out)
      (with-current-buffer buf
        (erase-buffer)
        (insert (format "%s\n" (file-name-nondirectory (directory-file-name dir))))
        (insert (mapconcat #'identity (nreverse (car out)) "\n"))
        (goto-char (point-min)))
      (pop-to-buffer buf)
      (kill-new (with-current-buffer buf (buffer-string)))
      (message "Tree copied to kill-ring")))
#+END_SRC

** Hide Dot Files
:PROPERTIES:
:ID:       47265328-f0d2-4251-9df5-b82627756dc8
:END:
- hotkey: . (dot)
#+BEGIN_SRC emacs-lisp
  (defun my-dired-mode-hook ()
    "My `dired' mode hook."
    ;; To hide dot-files by default
    (dired-hide-dotfiles-mode))

  ;; To toggle hiding
  (with-eval-after-load 'dired
    (define-key dired-mode-map (kbd "C-c o") #'my/dired-cd-to-shell)
    (define-key dired-mode-map "." #'dired-hide-dotfiles-mode) )
  ;;  (add-hook 'dired-mode-hook #'my-dired-mode-hook)

#+END_SRC

#+RESULTS:
: dired-hide-dotfiles-mode
** Section Loaded Message
:PROPERTIES:
:ID:       0077b2c4-5197-4016-b3a8-77ed64a2acfd
:END:
#+BEGIN_SRC emacs-lisp
  (message "dired loaded")
#+END_SRC
* Testing
:PROPERTIES:
:ID:       91bcf1f0-89ea-4c1c-bfa0-2aea6cecb463
:END:
#+BEGIN_SRC emacs-lisp

  (global-set-key (kbd "<f6>") 'browse-url-of-buffer)

  (global-set-key (kbd "s-l") 'my/org-insert-clean-link)
  (global-set-key (kbd "C-c i c") 'indent-to-column)
  (global-set-key (kbd "M-SPC") 'cycle-spacing)   

  ;; macros to set agenda categories
  (fset 'my/org-agenda-set-category-project
        (kmacro-lambda-form [?\C-c ?\C-x ?p ?C ?A ?T ?E ?G ?O ?R ?Y return ?P ?r ?o ?j return ?r] 0 "%d"))
  (fset 'my/org-agenda-set-category-task
        (kmacro-lambda-form [?\C-c ?\C-x ?p ?C ?A ?T ?E ?G ?O ?R ?Y return ?T ?a ?s ?k return ?r] 0 "%d"))
  
  (global-set-key (kbd "M-+") 'embark-dwim)
  (global-set-key (kbd "C-M-+") 'embark-act)

  (use-package consult-dir
    :ensure t
    :bind (("C-x C-d" . consult-dir)
           :map minibuffer-local-completion-map
           ("C-x C-d" . consult-dir)
           ("C-x C-j" . consult-dir-jump-file)))

  (setq consult-dir-project-list-function #'consult-dir-projectile-dirs)

#+END_SRC

#+RESULTS:
: consult-dir-projectile-dirs
** Zone (screensaver)
:PROPERTIES:
:ID:       38a06a4b-d396-4bc7-8da5-ad6fcd506314
:END:
#+BEGIN_SRC emacs-lisp
(require 'zone)
(zone-when-idle 300) ;; seconds of idle time before it kicks in
#+END_SRC

#+RESULTS:
: [nil 0 300 0 t zone nil idle 0 nil]

** Elisp font lock hooks
:PROPERTIES:
:ID:       f54c0c9c-0e4f-4ef0-a921-bd102217f318
:END:
#+BEGIN_SRC emacs-lisp
  (defun my/elisp-font-lock-additions ()
    (font-lock-add-keywords nil
  			  '(("\\<\\(t\\|nil\\)\\>" . font-lock-constant-face))))

  (add-hook 'emacs-lisp-mode-hook #'my/elisp-font-lock-additions)
#+END_SRC

#+RESULTS:
| my/elisp-font-lock-additions | paredit-mode |

** Org Bookmark Heading
:PROPERTIES:
:ID:       4db82b34-8b18-4d74-9a73-2b1282c7ddc8
:END:
#+BEGIN_SRC emacs-lisp sdfs   
  (require 'org-bookmark-heading)
  (global-set-key (kbd "C-x r b") 'consult-bookmark)

#+END_SRC

#+RESULTS:
: consult-bookmark

** Org-Drill Fix
:PROPERTIES:
:ID:       9a2f8e56-e9d3-4e34-9861-9abd71476402
:END:
#+BEGIN_SRC emacs-lisp
  (defun org-drill-time-to-inactive-org-timestamp (time)
    "Convert TIME into org-mode timestamp."
    (format-time-string
     (concat "[" (cdr org-time-stamp-formats) "]")
     time))
#+END_SRC

#+RESULTS:
: org-drill-time-to-inactive-org-timestamp

** Adding to path for empv
:PROPERTIES:
:ID:       de9cceed-47a7-4fb5-b851-3ecc12ea1ba2
:END:
#+BEGIN_SRC emacs-lisp
  (setenv "PATH" (concat "/home/tyler/miniconda3/bin:" (getenv "PATH")))
  (add-to-list 'exec-path "/home/tyler/miniconda3/bin")
#+END_SRC

#+RESULTS:
| /home/tyler/miniconda3/bin | /home/tyler/.local/bin | /usr/local/sbin | /usr/local/bin | /usr/sbin | /usr/bin | /sbin | /bin | /usr/games | /usr/local/games | /snap/bin | /snap/bin | /snap/emacs/3690/usr/bin | /snap/emacs/current/usr/libexec/emacs/30.2/x86_64-pc-linux-gnu |

** Dictionary
:PROPERTIES:
:ID:       91d73180-6a8d-4dc6-98d9-d9eb68e371e1
:END:
#+BEGIN_SRC emacs-lisp
  (use-package dictionary
    :custom
    (dictionary-server "dict.org")
    (dictionary-use-single-buffer t))

  (global-set-key (kbd "C-c d") #'dictionary-lookup-definition)
#+END_SRC

#+RESULTS:
: dictionary-lookup-definition

** Directory Tree Stamper
:PROPERTIES:
:ID:       9a3ead06-424a-4ceb-97a2-fcc7b316f0ff
:END:
#+BEGIN_SRC emacs-lisp
  (require 'transient)

  (defvar my/insert-map (make-sparse-keymap)
    "Keymap for personal insert commands (C-c i prefix).")
  (define-key global-map (kbd "C-c M-i") my/insert-map)

  (defun my/insert-char (char)
    "Insert a unicode character."
    (insert (string char)))

  (transient-define-prefix my/box-primitives-transient ()
    "Single box-drawing character insertion."
    [["Light lines"
      ("-" "─ horizontal" (lambda () (interactive) (my/insert-char ?─)))
      ("|" "│ vertical"   (lambda () (interactive) (my/insert-char ?│)))]
     ["Corners"
      ("1" "┌ top-left"     (lambda () (interactive) (my/insert-char ?┌)))
      ("2" "┐ top-right"    (lambda () (interactive) (my/insert-char ?┐)))
      ("3" "└ bottom-left"  (lambda () (interactive) (my/insert-char ?└)))
      ("4" "┘ bottom-right" (lambda () (interactive) (my/insert-char ?┘)))]
     ["Junctions"
      ("f" "├ left-T"    (lambda () (interactive) (my/insert-char ?├)))
      ("r" "┤ right-T"   (lambda () (interactive) (my/insert-char ?┤)))
      ("d" "┬ top-T"     (lambda () (interactive) (my/insert-char ?┬)))
      ("u" "┴ bottom-T"  (lambda () (interactive) (my/insert-char ?┴)))
      ("+" "┼ cross"     (lambda () (interactive) (my/insert-char ?┼)))]])

  (transient-define-prefix my/box-drawing-transient ()
    "Insert box-drawing chunks for trees and diagrams."
    [["Tree"
      ("f" "├── mid item"     (lambda () (interactive) (insert "├── ")))
      ("l" "└── last item"    (lambda () (interactive) (insert "└── ")))
      ("m" "│   continuation" (lambda () (interactive) (insert "│   ")))
      ("s" "    blank indent" (lambda () (interactive) (insert "    ")))]
     ["Primitives"
      ("p" "single chars..." my/box-primitives-transient)]])

  (define-key my/insert-map (kbd "t") #'my/box-drawing-transient)

  (which-key-add-keymap-based-replacements my/insert-map
    "t" "box drawing")


#+END_SRC

#+RESULTS:

** Mixed Pitch Mode toggler
:PROPERTIES:
:ID:       0f64e0bd-c5ac-4130-b034-c5821c3d98aa
:END:
#+BEGIN_SRC emacs-lisp
  (defun my/toggle-mixed-pitch-mode ()
    "Function to toggle fixed pitch mode and force a redraw to clear glyphs"
    (interactive)
    (mixed-pitch-mode 0)
    (mixed-pitch-mode 1)
    (message "Mixed Pitch Mode toggled"))

  (global-set-key (kbd "<f5>") #'my/toggle-mixed-pitch-mode)
#+END_SRC

** Section Loaded Message
:PROPERTIES:
:ID:       282cc289-8b2c-4c0c-9cb1-6b8feef1a0ab
:END:
#+BEGIN_SRC emacs-lisp
  (message "Testing section loaded")
#+END_SRC


* Elfeed
:PROPERTIES:
:ID:       ac1816aa-a5a1-44a9-850c-49a348c98b72
:END:
#+BEGIN_SRC emacs-lisp
  (message "Elfeed loading...")

  (use-package elfeed
    :ensure t
    :config
    (setq elfeed-db-directory "~/Orgfiles/elfeed/db")
    (setq elfeed-search-filter "@1-week-ago +unread")
    (setq elfeed-search-title-max-width 100)
    (setq elfeed-search-date-format '("%Y-%m-%d" 10 :left)))

  (use-package elfeed-org
    :ensure t
    :after (elfeed org)
    :config
    (setq rmh-elfeed-org-files '("~/Orgfiles/elfeed/feeds.org"))
    (elfeed-org))

  (defun my/mpv-play-thing-at-point ()
    "Play URL at point with mpv as video."
    (interactive)
    (let ((url (or (thing-at-point 'url t)
                   (get-text-property (point) 'shr-url)
                   (get-text-property (point) 'help-echo))))
      (if url
          (progn
            (message "Playing: %s" url)
            (start-process "mpv" nil "mpv" url))
        (message "No URL found at point"))))

  ;; sparse map for elfeed

  (defun my/elfeed-filter-all ()
    (interactive)
    (elfeed-search-set-filter "@1-week-ago +unread"))

  (defun my/elfeed-filter-youtube ()
    (interactive)
    (elfeed-search-set-filter "@1-month-ago +unread +youtube"))

  (defun my/elfeed-filter-reddit-homelab ()
    (interactive)
    (elfeed-search-set-filter "@1-week-ago +unread +reddit homelab"))

  (defun my/elfeed-filter-reddit-emacs ()
    (interactive)
    (elfeed-search-set-filter "@1-week-ago +unread +reddit emacs"))




  (defvar my/elfeed-map (make-sparse-keymap)
    "Keymap for elfeed commands.")

  (global-set-key (kbd "C-c f") my/elfeed-map)

  (define-key my/elfeed-map (kbd "f") #'elfeed)
  (define-key my/elfeed-map (kbd "u") #'elfeed-update)
  (define-key my/elfeed-map (kbd "o") #'my/mpv-play-thing-at-point)
  (define-key my/elfeed-map (kbd "y") #'my/elfeed-filter-youtube)
  (define-key my/elfeed-map (kbd "a") #'my/elfeed-filter-all)
  (define-key my/elfeed-map (kbd "r h") #'my/elfeed-filter-reddit-homelab)
  (define-key my/elfeed-map (kbd "r e") #'my/elfeed-filter-reddit-emacs)
  (message "Elfeed loaded")

  (use-package elfeed-tube
    :after elfeed
    :config
    (elfeed-tube-setup)
    (setq elfeed-tube-mpv-options
        	'("--cache=yes"
        	  "--force-window=yes"
        	  "--fullscreen=yes"
        	  "--screen=2"))
    (setq elfeed-tube-invidious-url "https://invidious.io")
    (add-hook 'elfeed-new-entry-hook #'elfeed-tube-fetch))

  ;; RET - play now, replace or start
(define-key elfeed-show-mode-map (kbd "<return>")
  (lambda () (interactive)
    (if (and (fboundp 'mpv-live-p) (mpv-live-p))
        (let* ((video-id (elfeed-tube--entry-video-id elfeed-show-entry))
               (url (concat "https://youtube.com/watch?v=" video-id)))
          (mpv--enqueue `("loadfile" ,url "replace") #'ignore))
      (elfeed-tube-mpv (point)))))

;; n - just navigate, no mpv
(define-key elfeed-show-mode-map (kbd "n") #'elfeed-show-next)

;; e - enqueue without navigating
(define-key elfeed-show-mode-map (kbd "e")
  (lambda () (interactive)
    (let* ((video-id (elfeed-tube--entry-video-id elfeed-show-entry))
           (url (concat "https://youtube.com/watch?v=" video-id)))
      (mpv--enqueue `("loadfile" ,url "append") #'ignore)
      (message "Enqueued: %s" (elfeed-entry-title elfeed-show-entry)))))

#+END_SRC

#+RESULTS:
: #[nil ((let* ((video-id (elfeed-tube--entry-video-id elfeed-show-entry)) (url (concat "https://youtube.com/watch?v=" video-id))) (mpv--enqueue `("loadfile" ,url "append") #'ignore) (message "Enqueued: %s" (elfeed-entry-title elfeed-show-entry)))) nil nil nil nil]

* Load Music
:PROPERTIES:
:ID:       6baab66b-1a21-4dad-8b9e-91c4944037fe
:END:
#+BEGIN_SRC emacs-lisp

  (defun play-retro-sax ()
    "Launch a retro sax 3 hour video"
    (interactive)
    (browse-url "https://www.youtube.com/watch?v=001hXHNo-3w"))

  (defun play-high-bpm-trance ()
    "Launch high bpm trance video"
    (interactive)
    (browse-url "https://youtube/q7gv9B4Kw44"))

  ;; Old version of my inserter 
  ;; (defun my/insert-youtube-luink-into-m3u ()
  ;;   "insert link from clipboard into correct format"
  ;;   (interactive)
  ;;   (let* ((url (read-string "Youtube URL:"))
  ;;          (band-name (read-string "Band:"))
  ;;          (song-name (read-string "Song:"))
  ;;          (m3u-entry (concat "\n" url "(:title \""  band-name " - " song-name "\":youtube t)"))
  ;;          (m3u-to-append (read-file-name "Append to:" "~/Orgfiles/Music/")))

  ;;     (append-to-file m3u-entry nil m3u-to-append)
  ;;     (message (concat m3u-entry " added!"))))

  (defun my/insert-youtube-link-into-m3u ()
    "Insert link from clipboard into correct format."
    (interactive)
    ;; Initialize history variables if they don't exist
    (unless (boundp 'band-name-history)
      (setq band-name-history nil))
    (unless (boundp 'song-name-history)
      (setq song-name-history nil))

    (let* ((url (read-string "Youtube URL: "))
           ;; Use the last entered value from history as a default
           (band-name (read-string (format "Band (%s): " (car (reverse band-name-history))) nil nil (car (reverse band-name-history))))
           (song-name (read-string (format "Song (%s): " (car (reverse song-name-history))) nil nil (car (reverse song-name-history)))))
      
      ;; Update history with the new values if they are not already present
      (unless (member band-name band-name-history)
        (push band-name band-name-history))
      (unless (member song-name song-name-history)
        (push song-name song-name-history))

      ;; Construct the M3U entry
      (let ((m3u-entry (concat "\n" url "(:title \"" band-name " - " song-name "\":youtube t)"))
            (m3u-to-append (read-file-name "Append to:" "~/Orgfiles/Music/")))

        ;; Append to file
        (append-to-file m3u-entry nil m3u-to-append)
        ;; Show message
        (message "%s added!" m3u-entry))))



  (defhydra Music (global-map "<f7>")
    "Music Player"
    ("s" play-retro-sax "Retro-Sax")
    ("h" play-high-bpm-trance "High BPM Trance"))

  ;; EMPV Setup
  (defun my/empv-play-file-from-music()
    "Play a file via empv starting in ~/Music"
    (interactive)
    (let ((default-directory (expand-file-name "~/Orgfiles/Music/")))
      (call-interactively #'empv-play-file)))


  (use-package empv
    :ensure t
    :bind
    (
     ("")
     ("s-[" . empv-volume-down)
     ("s-]" . empv-volume-up))
    :bind-keymap
    ("C-c e" . empv-map)
    :config
    :custom
    (empv-radio-channels
     '(("SomaFM - Groove Salad"    . "http://www.somafm.com/groovesalad.pls")
       ("SomaFM - Drone Zone"      . "http://www.somafm.com/dronezone.pls")
       ("SomaFM - Sonic Universe"  . "https://somafm.com/sonicuniverse.pls")
       ("SomaFM - Metal"           . "https://somafm.com/metal.pls")
       ("SomaFM - Vaporwaves"      . "https://somafm.com/vaporwaves.pls")
       ("SomaFM - Tiki Time" . "https://somafm.com/tikitime130.pls")
       ("Jazz24" . "https://knkx-live-a.edge.audiocdn.com/6285_256k/playlist.m3u8")
       ("Retrowave Sax"            . "https://www.youtube.com/watch?v=001hXHNo-3w")
       ("I'm A Dude - 10 Hours"    . "https://www.youtube.com/watch?v=e2C1GVITrVI")
       ("Epic Sax Man - 10 hours"  . "https://www.youtube.com/watch?v=ez8m4PXksQs")
       ("Italy Cafe"               . "https://www.youtube.com/watch?v=D_9cyVNxDPk")
       ("YouTube Mix - High BPM Trance" . "https://youtube/q7gv9B4Kw44"))))

  (with-eval-after-load 'empv
    (define-key empv-map (kbd "y") 'my/insert-youtube-link-into-m3u )
    (define-key empv-map (kbd "f") 'my/empv-play-file-from-music)
    (define-key empv-map (kbd "m") 'my/mpv-play-m3u)
    (define-key empv-map (kbd "S") 'empv-playlist-save-to-file))

  (which-key-add-keymap-based-replacements empv-map
    "y" "M3U Link Inserter"
    "f" "Play from Music"
    "m" "MPV Direct - Load M3U"
    "S" "Save Playlist")

  (with-eval-after-load 'embark
    (empv-embark-initialize-extra-actions) )

  (when my/windows-p
    (setq empv-mpv-binary "mpv")
    (setq empv-socket-file "\\\\.\\pipe\\empv-socket"))

  ;; MPV task killer
  (defun my/mpv-kill-all ()
    "Force-kill all running mpv processes, regardless of how they were started."
    (interactive)
    (if (eq system-type 'windows-nt)
        (call-process "taskkill" nil nil nil "/IM" "mpv.exe" "/F")
      (call-process "pkill" nil nil nil "mpv"))
    (message "Killed all mpv processes"))


  ;; playlist player (for when empv doesn't work)

  (defun my/mpv-play-m3u ()
    "Prompt for an m3u file in the Music directory and play it audio-only."
    (interactive)
    (my/mpv-kill-all)
    (let* ((default-directory (expand-file-name "~/Orgfiles/Music/"))
           (file (read-file-name "Playlist: " default-directory nil t nil
                                 (lambda (f) (string-suffix-p ".m3u" f)))))
      (start-process "mpv" nil "mpv" "--no-video" (expand-file-name file))))


  (message "music section loaded")



#+END_SRC

#+RESULTS:
: music section loaded

* Xah Lee Implementations
Tried out Xah-fly-keys and didn't love his command mode but he has some really good helper rowing those below
** Function definitions
:PROPERTIES:
:ID:       3793ab02-0df5-49b5-ae2c-47a80fca5385
:END:
#+BEGIN_SRC emacs-lisp
  (defun xah-beginning-of-line-or-block ()
    "Move cursor to beginning of line or previous block.

  • When called first time, move cursor to beginning of char in current line. (if already, move to beginning of line.)
  • When called again, move cursor backward by jumping over any sequence of whitespaces containing 2 blank lines.
  • if `visual-line-mode' is on, beginning of line means visual line.

  URL `http://xahlee.info/emacs/emacs/emacs_keybinding_design_beginning-of-line-or-block.html'
  Version: 2018-06-04 2021-03-16 2022-03-30 2022-07-03 2022-07-06"
    (interactive)
    (let (($p (point)))
      (if (or (equal (point) (line-beginning-position))
              (eq last-command this-command))
          (when
              (re-search-backward "\n[\t\n ]*\n+" nil 1)
            (skip-chars-backward "\n\t ")
            (forward-char))
        (if visual-line-mode
            (beginning-of-visual-line)
          (if (eq major-mode 'eshell-mode)
              (progn
                (declare-function eshell-bol "esh-mode.el" ())
                (eshell-bol))
            (back-to-indentation)
            (when (eq $p (point))
              (beginning-of-line)))))))


  (defun xah-end-of-line-or-block ()
    "Move cursor to end of line or next block.

  • When called first time, move cursor to end of line.
  • When called again, move cursor forward by jumping over any sequence of whitespaces containing 2 blank lines.
  • if `visual-line-mode' is on, end of line means visual line.
  URL `http://xahlee.info/emacs/emacs/emacs_keybinding_design_beginning-of-line-or-block.html'
  Version: 2018-06-04 2021-03-16 2022-03-05"
    (interactive)
    (if (or (equal (point) (line-end-position))
            (eq last-command this-command))
        (re-search-forward "\n[\t\n ]*\n+" nil 1)
      (if visual-line-mode
          (end-of-visual-line)
        (end-of-line))))
#+END_SRC

#+RESULTS:
: xah-end-of-line-or-block

** Bindings
:PROPERTIES:
:ID:       6339b7d2-fcb4-4f54-aede-0b8c041fe43d
:END:
#+BEGIN_SRC emacs-lisp
  (define-key global-map (kbd "M-p") 'xah-beginning-of-line-or-block)
  (define-key global-map (kbd "M-n") 'xah-end-of-line-or-block)
#+END_SRC

#+RESULTS:
: xah-end-of-line-or-block

** Section Loaded Message
:PROPERTIES:
:ID:       6a9d3eb1-90d6-4b80-b20d-ffe1d4b9b0ef
:END:
#+BEGIN_SRC emacs-lisp
  (message "Xah Lee section loaded")
#+END_SRC

*
Final Loading
** Dashboard
:PROPERTIES:
:ID:       0e9bfe60-2dff-459f-9711-1b228e912290
:END:
Startup landing page. Loads late because it reads from recentf, bookmarks,
org-agenda-files, and project.el — all of which need to be initialized first.
#+BEGIN_SRC emacs-lisp

  (use-package dashboard
    :ensure t
    :after (all-the-icons org)
    :init
    (setq dashboard-startup-banner "~/EMACS Wordmark-02.png")
    (setq dashboard-center-content t)
    (setq dashboard-vertically-center-content t)
    (setq dashboard-icon-type 'all-the-icons)
    (setq dashboard-set-heading-icons t)
    (setq dashboard-set-file-icons t)
    :config
    (setq dashboard-footer-messages
          '("While any text editor can save your files, only Emacs can save your soul"
            "Vi Vi Vi, the editor of the beast"
            ))
    (setq dashboard-banner-logo-title "It's time to work.")
    
    (defvar my/dashboard-bookmark-exclusions
      '("org-capture-last-stored-marker"
        "org-refile-last-stored"
        "org-capture-last-stored"))
    (advice-add 'bookmark-all-names :filter-return
                (lambda (names)
                  (seq-remove (lambda (n) (member n my/dashboard-bookmark-exclusions))
                              names)))

    (defun my/dashboard-insert-tasks (list-size)
      "Add the list of LIST-SIZE task entries (CATEGORY=\"Task\")."
      (require 'org-agenda)
      (let ((dashboard-match-agenda-entry "CATEGORY=\"Task\""))
        (dashboard-insert-section
         "Tasks:"
         (dashboard-agenda--sorted-agenda)
         list-size
         'my-tasks
         "t"
         `(lambda (&rest _)
            (let ((file (get-text-property 0 'dashboard-agenda-file ,el))
                  (point (get-text-property 0 'dashboard-agenda-loc ,el)))
              (funcall dashboard-agenda-action file point)))
         (format "%s" el))))
    
    (defun my/dashboard-insert-projects (list-size)
      "Add the list of LIST-SIZE project entries (CATEGORY=\"Proj\")."
      (require 'org-agenda)
      (let ((dashboard-match-agenda-entry "CATEGORY=\"Proj\"")
            (dashboard-filter-agenda-entry 'dashboard-filter-agenda-by-todo))
        (dashboard-insert-section
         "Projects:"
         (dashboard-agenda--sorted-agenda)
         list-size
         'my-projects
         "p"
         `(lambda (&rest _)
            (let ((file (get-text-property 0 'dashboard-agenda-file ,el))
              	(point (get-text-property 0 'dashboard-agenda-loc ,el)))
              (funcall dashboard-agenda-action file point)))
         (format "%s" el))))



    (defun my/dashboard-filter-goal-by-90-day-deadline ()
      "Include entry if it has a deadline within the next 90 days and is not done.
              An entry is included if this function returns nil and excluded if returns point."
      (let ((deadline (org-get-deadline-time (point)))
            (cutoff (time-add (current-time) (* 86400 90))))
        (unless (and (not (org-entry-is-done-p))
                     (not (org-in-archived-heading-p))
                     deadline
                     (time-less-p deadline cutoff))
          (point))))

    (defun my/dashboard-insert-goals (list-size)
      "Add the list of LIST-SIZE goal entries with deadlines within 90 days."
      (require 'org-agenda)
      (let ((dashboard-match-agenda-entry "CATEGORY=\"Goals\"")
            (dashboard-filter-agenda-entry 'my/dashboard-filter-goal-by-90-day-deadline))
        (dashboard-insert-section
         "Goals (next 90 days):"
         (dashboard-agenda--sorted-agenda)
         list-size
         'my-goals
         "g"
         `(lambda (&rest _)
            (let ((file (get-text-property 0 'dashboard-agenda-file ,el))
              	(point (get-text-property 0 'dashboard-agenda-loc ,el)))
              (funcall dashboard-agenda-action file point)))
         (format "%s" el))))

    (defvar my/dashboard-prefontify-files
      '("~/Orgfiles/tasks.org"
        "~/Orgfiles/config.org"
        "~/Orgfiles/Productivity/Goals/Goals.org"
        "~/Orgfiles/SEAS.org")
      "Files to fully fontify before dashboard extracts entries.
          Ensures TODO keyword decorations from org-modern render correctly
          in the dashboard buffer.")

    ;;    (add-hook 'dashboard-before-initialize-hook #'my/dashboard-prefontify-files)

    (defun my/dashboard-prefontify-files ()
      "Load and fully fontify files in `my/dashboard-prefontify-files'."
      (dolist (file my/dashboard-prefontify-files)
        (when (file-exists-p file)
          (with-current-buffer (find-file-noselect file)
            (font-lock-ensure)))))


    (add-to-list 'dashboard-item-generators '(my-tasks . my/dashboard-insert-tasks)) 
    (add-to-list 'dashboard-item-generators '(my-goals . my/dashboard-insert-goals))
    (add-to-list 'dashboard-item-generators '(my-projects . my/dashboard-insert-projects))




    (setq dashboard-items '((recents . 5)
                            (bookmarks . 5)
                            (my-goals . 5)  			  
                            (my-tasks . 5)
                            (my-projects . 5)))
    (dashboard-setup-startup-hook))

  (setq initial-buffer-choice (lambda () (get-buffer-create "*dashboard*")))

  (defvar my/dashboard-initialized nil
    "Non-nil once dashboard content has been built for this session.")

  (defun my/build-dashboard ()
    "Build the dashboard buffer once."
    (unless my/dashboard-initialized
      (dashboard-open)
      (setq my/dashboard-initialized t)))

  (defun my/show-dashboard-on-new-frame (&optional frame)
    "Show the dashboard buffer in FRAME, refreshing centering on first real frame only."
    (when frame
      (run-with-timer 0.1 nil
                      (lambda ()
                        (raise-frame frame)
                        (select-frame-set-input-focus frame))))
    (switch-to-buffer (get-buffer-create "*dashboard*"))
    (if my/dashboard-initialized
        (dashboard-refresh-buffer)
      (my/build-dashboard)))



  (if (daemonp)
      (progn
        (my/build-dashboard) ;; scan recentf/bookmarks/agenda now, before any client connects
        (add-hook 'server-after-make-frame-hook #'my/show-dashboard-on-new-frame))
    (my/show-dashboard-on-new-frame))


#+END_SRC

#+RESULTS:
: #[nil ((get-buffer-create "*dashboard*")) nil]

* Setup final org settings
:PROPERTIES:
:ID:       b5c6dac1-e9af-42a0-b392-24cab22097c9
:END:
#+BEGIN_SRC emacs-lisp
  ;;Make org links open in same window
  (setq org-link-frame-setup
        '(
          (vm . vm-visit-folder-other-frame)
          (vm-imap . vm-visit-imap-folder-other-frame)
          (gnus . org-gnus-no-new-news)
          (file . find-file)
          (wl . wl-other-frame)))

  ;; set org agenda mode map commands 

  (message "Updated msg for testing")








#+END_SRC

#+RESULTS:
: my/org-agenda-set-category-task
** default frame settings
:PROPERTIES:
:ID:       aaef7e05-2e3d-4f3b-8165-5f7ff558689b
:END:
#+BEGIN_SRC emacs-lisp

  (setq default-frame-alist '(
                              (vertical-scroll-bars) 
                              (left-fringe . 10)
                              (right-fringe . 10)
                              (fullscreen . maximized)))
#+END_SRC

#+RESULTS:
: ((vertical-scroll-bars) (left-fringe . 10) (right-fringe . 10) (fullscreen . maximized))

#+BEGIN_SRC plantuml :filphoe test.png
  @startuml
  :user: -right-> (Right)
  :user: -up-> (Up)
  @enduml
#+END_SRC

#+RESULTS:
[[file:test.png]]
** Load Hyper
:PROPERTIES:
:ID:       9f05f700-407e-41c4-b2ba-f35e9234c774
:END:
#+BEGIN_SRC emacs-lisp

    ;; (defun my/enable-hyper-key()
    ;;   "Command to run hyper.sh when keyboard unplugs"
    ;;   (interactive)
    ;;   (shell-command "~/hyper.sh"))

  (defun my/enable-hyper-key ()
    "Apply xmodmap config when keyboard replugs"
    (interactive)
    (let ((display (or (getenv "DISPLAY") ":0")))
      (shell-command (format "DISPLAY=%s xmodmap ~/dotfiles/configbackups/.Xmodmap" display))))
    
    (global-set-key (kbd "s-<f10>") 'my/enable-hyper-key)

#+END_SRC
#+RESULTS:
: my/enable-hyper-key
** Load keymap
:PROPERTIES:
:ID:       a8938bc9-b5f7-4024-8790-2a7db66782fb
:END:
#+BEGIN_SRC emacs-lisp
  (my/backup-global-keymap)
#+END_SRC

#+RESULTS:
: Global keymap backed up.

** Section Loaded Message
:PROPERTIES:
:ID:       c696cf9a-6d09-45e5-b726-c36583f5503d
:END:
#+BEGIN_SRC emacs-lisp
  (message "Final Section loaded")
#+END_SRC

